# U17b：植被 / 硫磺火 / 铁门 / 桥的 DF 闭环

本地 Mac 执行；起点 `e560958`；任务书 `ai_docs/tasks/u-17b.prompt.md`，规格为 X-0 §4.3 U17、§3.3 首族、§3.1 K19/K28。未暂存、未提交、未推送。**最终门禁全部通过：167文件R∪S、3358 passed / 0 failed / 8既有skip / 1既有todo；build与drift通过，559份输入冻结前后及交付时一致。**

## 1. 实现范围

恢复 `61 / 66 / 104 / 83 / 98` 五条 DF 的真实地形，`DF_MISSING_TILES` 只移除这五条，其余 25 条按原顺序保留。地形枚举仅尾部追加，既有 ID 不变。另补 CE `DF_FOLIAGE_REGROW=63`，它是踩倒植被的必要后继，原先不在缺 tile 清单中，不算额外撤销一条缺口。

五条使用已有 U17a `spawnDungeonFeature` 事务与 `promoteTile`，没有新设火焰、驱离或机关旁路。修正锁门直接写 `OPEN_DOOR` 的旧路径：匹配钥匙通过 `TM_PROMOTES_WITH_KEY` 晋升，落下真正的 `OPEN_IRON_DOOR_INERT` 并产生目录闪光；原钥匙深度、坐标/机器号、一次性规则保留。

为闭合最终消费者，还补了三项直接必要的环境行为：

- `updateEnvironment` 的地面物品阶段落实静止物品的 `TM_PROMOTES_ON_ITEM`。再生植被覆盖物品后，物品在这个阶段把它踩倒；DF 刷新本身只即时处理生物及烧物，没有伪造“DF 铺装即让物品触发”的 CE 顺序。
- 桥变深渊后，地面物品按 CE `updateFloorItems` 坠层：药水及 D40 下坠物销毁，其他物品进入下一层的独占队列；入层使用物品专属落位判据，允许水/深渊/熔岩，排除物品阻挡、怪物/物品占位、楼梯和机器格。新层在恢复旧 RNG 种子之前落位，缓存层也消费同一队列。`spawnTurnNumber` 阻止离层补跑提前处理尚未坠入的物品。

- 下层已有火焰/熔岩的落物消费也闭合：CE Items:1256–1261要求先坠落、再烧毁、再漂流；普通卷轴受火、非护符物品受熔岩，销毁经同一个 `burnFloorItem` → `DF_ITEM_FIRE` 事务，护符不受熔岩销毁，未来时间物品继续等待。此缺口由新增测试先红证明，修复后与U17a共46项通过；另补每种落位否决的逐项反例。

`pendingFallenItemsByDepth` 纳入 Game 生命周期和全局实体图，`Item.spawnTurnNumber` 纳入 U01 字段合同；新局清空、存档/读档保留、队列到楼层只转移一次。快照验证要求新增队列字段，缺此字段的旧快照会被拒绝，未提供旧档迁移。U01/U03 原守卫没有改动，只在 `scripts/u03-state-contract.json` 登记新增状态。

## 2. CE 核对与逐条闭包

权威均为本仓库 `BrogueCE-master`，没有用网络版本覆盖本地 CE。五条目录实际位于 `src/brogue/Globals.c`，不是 `GlobalsBrogue.c` 的蓝图表。`scripts/u17b-ce-golden.mjs` 直接从 CE 的 Globals/Rogue 源码生成五行地形、九行 DF 的独立黄金数据，包含源码 SHA-256；`u21c-ce-appearance.mjs` 从 CE glyph/color 源码扩展外观黄金表。

| DF | 数据与三链 | 真实触发 → 最终消费者 / 用户证据 |
|---|---|---|
| 61 `TRAMPLED_FOLIAGE` | Globals:688，SURFACE，start/decr=0/0，flags=0，无 subsequent；tile:474，priority60，flammable，点火15→PLAIN_FIRE，promote100→63，再生 DF 为 FOLIAGE/单格/`BLOCKED_BY_OTHER_LAYERS`。 | FOLIAGE 的 ON_STEP（Globals:472；Time 生物接触、Items 物品阶段）→61→开视线→63 再生→占格生物即时再踩倒，物品在环境阶段踩倒。实际移动、离开、等待再生；墙层否决与物品占格均有守卫。 |
| 66 `ACTIVE_BRIMSTONE` | Globals:695，LIQUID，0/0，flags=0，无 subsequent；tile:425，priority40，flammable/spontaneously-ignites，点火100→67，promote10→67；INERT tile:426 的 promote800→66。 | 硫磺湖已有 INERT 起点→环境活化→玩家火杖点燃，或活性态概率晋升→67→104。800、10 的边界与中间层状态分别验证；浏览器从 INERT 等待实际活化。 |
| 104 `BRIMSTONE_FIRE` | Globals:744，SURFACE，0/0，flags=0；tile:493，priority10，T_IS_FIRE，VANISHES/STAND_IN_TILE/VISUALLY_DISTINCT，promote2500→无 DF；光为 `BRIMSTONE_FIRE_LIGHT`。 | 67 的 subsequentDF→104→统一即时着火/烧卷轴（烧物自身可再产生 ITEM_FIRE）/起火记账/光照→寿命到期只清火层，保留硫磺液层。玩家火杖经瞄准、Enter 提交，扣一充能并出现硫磺火。 |
| 83 `OPEN_IRON_DOOR_INERT` | Globals:720，DUNGEON，0/0，flags=0，`GENERIC_FLASH_LIGHT`；tile:332，priority90，阻挡表面效果，STAND_IN_TILE/VISUALLY_DISTINCT，promote=0；fireType=EMBERS、ignition50，但无 FLAMMABLE。 | LOCKED_DOOR（Globals:331）+ 匹配钥匙→Movement.useKeyAt 的晋升形状→83→可通行、铁门外观/中文名称、闪光；不会按普通木门计时回关。错钥匙、跨层、可复用/一次性、走过、存读档均验证。 |
| 98 `BRIDGE_FALLING`（DF 名为 `DF_BRIDGE_FALL_PREP`） | Globals:736，LIQUID，200/100，传播限定 BRIDGE，flags=0；tile:429，priority45，flammable/VANISHES，点火50→99，promote10000→99；99铺CHASM并再调用98；105负责桥断消息/闪光→99。 | 玩家火杖→BRIDGE.fireType=105→99→98 沿绳桥传播→后续环境晋升为CHASM→玩家/怪物既有坠层、物品新增坠层→显示深渊、实体在下层落位。石桥不带可燃位，不伪造坍塌。浏览器按键烧桥、怪物与长剑入队、JSON存读后实际跳入D2，长剑恰一件。 |

这五条 DF 均没有 `DFF_EVACUATE_CREATURES_FIRST`；守卫验证它们不会发明驱离动作。桥上的生物按坠落流程处理，火上的生物按即时接触处理。祖先 DF 的 description、flare 继续由 U17a 端口消费；五条本体 description 均为空，不能给它们虚构新消息。

**与任务措辞 / 旧注释的区别**：CE 硫磺链是 INERT↔ACTIVE 加硫磺火，不是“爆炸→黑曜石”；旧 LakeSystem 注释已纠正。83 的现有上游是钥匙锁门，不是接线铁闸，未虚构 wired 触发。98 是正在坠落的中间态，最终深渊来自99。

## 3. 独立守卫与浏览器

新增 `src/test/u_17b_carriers.test.ts`：20项，覆盖 CE全字段、只移除五条登记、三链/外观、每条实际触发、概率边界、层叠否决、错/可复用钥匙、持续开门、玩家悬浮/坠落、怪物坠落、药水破碎、D40销毁、新层/缓存层落位、入层 RNG 相位、独占所有权与 JSON 存读。

`scripts/u17b-negative.mjs` 在隔离副本中逐个破坏：四种地形缺失、铁门旧旁路、漏物品下坠、忽略未来时间、漏静止物品踩踏、漏DF刷新、漏物品时钟存档、漏落物后的火/熔岩消费。十一个变体均被专项检出；不是只证明“自己修改后的期望能过”。另有 D2 旧前提的反事实通过记录。原始结果见 [negative-summary.json](u-17b-evidence/negative-summary.json)。

使用 `develop-web-game` 技能客户端实际启动并移动；原客户端 canvas 导出仍为黑图，已经打开检查，**不计作视觉通过**。独立有头 Playwright 全页截图和真实输入记录在 [browser-results.json](u-17b-evidence/browser-results.json)：植被踩踏/空格再生、硫磺活化/瞄准施法、开锁/穿门、桥坍塌/实体入队、存读档后入D2、700px窄屏。console/page errors为空。全页图已逐张打开，含 [硫磺火](u-17b-evidence/browser-sulfur-fire.png)、[坍塌中](u-17b-evidence/browser-bridge-collapsing.png)、[下层物品](u-17b-evidence/browser-fallen-items-d2.png)。

初版新夹具的修正不算旧守卫消红：CE DF 枚举从1开始，黄金生成器补显式0哨兵；DF 的0 tile/layer规范化；改用垂直射线避免火杖继续穿过整个桥；区分DF生物刷新和环境物品触发；用正常模式验证真正的下层生成。相关失败日志保留。类型检查同时发现未导入钥匙位、新地形光表未扩充、测试访问私有字段与旧burning类型，逐项修复。

## 4. 既有守卫的前提修订（交验收复核）

原守卫先在新代码运行，保留红项；隔离副本还原 HEAD 后，原始15项全部通过。随后 V3/V5/G2 的三处30条登记计数、C3“ACTIVE_BRIMSTONE不存在”各自完成 HEAD 反事实。再发现B1的武器数据旗标夹具沿用前序随机流，生成了额外ITEM_RUNIC；完整HEAD文件16项通过后，仅明确选择CE 40%附魔分支的不触发面，保留全部原有旗标/数值断言。共20项旧目录/缺失/随机前提有反事实证据，另外 C4c D2 的旧随机落点由**仅撤销静止物品晋升**的反事实复现。证据在 `head-premises*.json`、`premise-counterfactual*.json`、`negative-D2-old-stationary-premise.json`。

- C4a-0 / C7 / R1：按CE扩充五行严格表及新的光载体许可；原地形ID、原行字段不改。139行外观黄金全部原值保留，只增五行，见 [ce-golden-extension.json](u-17b-evidence/ce-golden-extension.json)。
- C4a / C4b / U08 / U17a：旧目录宇宙用明确排除新增63或新增五tile的投影保留；新增集合由U17b独立精确断言。缺失清单的现实长度改为25，V3/V5/G2的其余缺口与行为断言保留。
- C4b C6 / C4c A3、A4、B6：保留原来“缺依赖应响亮失败/预检/不能半晋升”的断言，显式注入该测试所需要的缺tile前提并在finally恢复。真实五条闭包另由U17b正向/负向守卫验证。
- C4c D2：新增静止物品晋升改变共享RNG，旧随机驱离坐标不再稳定。只把楼梯周围设置为唯一合格目的地，原坐标、可通行、成功、驱离旗标、地形不变断言全部保留。
- B1：普通武器元数据装载检查的质量分支被显式固定为无附魔，避免从之前测试继承RNG；原三件武器flags与全部冻结数值不动，新增40%门限断言。完整HEAD反事实16/16，修正后B1+U17b 36/36。
- C3 T14：把任务本就要求恢复的 ACTIVE_BRIMSTONE 从“不得存在”改为追加ID存在；旧的自创 ACTIVE 别名与 search 包装留痕原样保留。不是删除整条源码守卫。

前提差异完整交付 [guard-premise-changes.diff](u-17b-evidence/guard-premise-changes.diff) 供验收裁决。B1原58条、C4c原82条断言经AST打印逐项比对全部保留（B1额外增加40%门限1条），见 `fixture-only-assertions.json`。未改 p1_30、U24、U01/U03、W13/W14 的测试，六文件前后SHA逐一相同，见 `protected-guards.json`；也没有修改超时或增加skip来消红。

## 5. 生成漂移与一次重捕获

入场标准 `test:drift` 通过；实现后、重捕前先保留原基线的红色 drift 日志。四种子424242、777、20260913、31337 × D1–D26，共104层。`u17b-attribution.mjs` 使用只存在于 esbuild 内存中的逐阶段变体，不改工作区或基线；每阶段记录所有实际输入哈希。跟踪标准四字段、层/体积、全格、实体/物品、player、两流RNG、五种载体计数和坠落队列。默认值为0的新增物品时间字段在旧/新语义指纹中统一省略，非零时间及队列仍记录。

| 单变量阶段 | 相邻阶段结果（104层） | 独立行为证据 |
|---|---|---|
| HEAD → s0（五条均关闭） | 全观测一致，证明变体可还原起点。 | 原基线及HEAD反事实。 |
| s0→s1：61及再生闭包 | 本样本无生成增量。 | 玩家/物品踩踏、视线、再生、跨层否决。 |
| s1→s2：66 | 5层指纹/层/全格/载体改变；未改变RNG。 | INERT环境活化与800边界。 |
| s2→s3：104 | 8层RNG改变；7层层/全格改变，2层指纹/载体改变。 | ACTIVE超时/火杖→67→104、即时着火/烧物、2500寿命边界。 |
| s3→s4：83 | 无生成增量。 | 真钥匙开门、闪光、永久开态与存读。 |
| s4→s5：98 | 无生成增量。 | 火杖→桥链→深渊→玩家/怪物/物品坠层；石桥否定面。 |
| s5→s6：钥匙入口迁回事务 | 无生成增量。 | 真实移动开锁；撤回旧写门旁路的变体红。 |
| s6→s7：物品下坠/时间/落位 | 无生成增量。 | 物品下坠、两类楼层、持久化/独占与RNG相位。 |
| s7→s8：静止物品晋升 | 100层RNG改变，85层层/全格改变，26层指纹/载体改变。 | 环境物品阶段的踩踏守卫；撤销此调用可反事实复现旧D2随机落点。 |
| s8→final：落物后火/熔岩消费 | 104层全部观测无增量；回退版本与修复前完整版本逐字段相等。 | 既有火上卷轴、熔岩上武器、护符豁免和未来时间，先红后绿；删调用变体红。 |

专项 [floor-trace-summary.json](u-17b-evidence/floor-trace-summary.json) 记录实际触发62次，**全部为FOLIAGE→DF61**，样本中未触发其他机器族；追踪本身不消费RNG。各阶段的怪物数/物种集合/物品数均不变。全部相邻阶段差异见 `generation-stages.json`；这不等同于承诺所有种子均无实体变化。

完成独立守卫及全部单变量归因后，`u17b-recapture.mjs` **只写一次** `generation_baseline.json`：27个fp字段及note改变；104层的n/species/items均未变。写入脚本先核对原始SHA与最终采样的全部依赖SHA，已写过则拒绝第二次执行。见 [recapture.json](u-17b-evidence/recapture.json)。

| 基线 | 前SHA-256 | 后SHA-256 |
|---|---|---|
| generation_baseline.json | `abc739134df98b24cab2830283395d3cd159ebf25ec0b167b6ebc0c46405875a` | `56622a15c6fb41099a2a6a2161b3e27df228b55391adbacec768d4265f4d655c` |
| p2_baseline.json | `f1690f3748c3e49c0df692cbee283fb962719d9b290131b9507282a6b504fc4b` | 同前，未改 |
| p2_2_baseline.json | `ba5cfdab37e2bdf633714ac9da1b54c5db71318967c54cffdb47da7519f51726` | 同前，未改 |
| p2_3_baseline.json | `9b3bb0c149e968f6eccae04752e13390beaf4602c6e8216aa65426432f5ecf2e` | 同前，未改 |
| u21c-ce-terrain.json（外观黄金） | `f4d1f875e7356a4cb45d8a24d0d8c4f8c63354991305378424043a3453f958aa` | `e7af07f4b5df0e709955f10be34375312b4942c4e3f1622aea05bd6a9a512f58` |


## 6. R∪S 与最终复跑声明

`u17b-audit.mjs` 从修改的生产文件建立反向导入闭包，加上DF/三链/实体副作用/新地形/读源码语义搜索。显式含 p1_30、U24、U01/U03、c4a/b/c、c7、r1、f/g、V、W13/14、U17a 与全部源码读取守卫。清单在 `tests.txt`，生成基线另走 `test:drift`。

发现轮167文件：3353通过、5失败、8既有skip、1既有todo，原始日志/JSON完整保留。红项为C3旧留痕、B1普通武器随机前提、P1-30死键，以及施工期缓存旧Game对上新测试的两个新守卫（落位RNG相位、火/熔岩消费）。后两项在最终代码独立整文件20/20通过；B1整文件HEAD16/16；P1-30要求的旧键已归档，守卫不改，P1-30+U24共19/19。

21:21启动的首个冻结批次因发现上述死键而主动终止，保存在 `prefinal-thermal-locale/`，不计作最终完成。文案归档后重采104层观测，逐字段等于此前最终数据，见 `post-recapture-same-observation.json`；**没有第二次写基线**。

**最终复跑声明**：所有实现、测试、脚本及基线修改结束后，`u17b-final-check.mjs` 于 **2026-09-25 21:28:21–22:02:55（Asia/Shanghai）** 重新冻结输入并从头顺序运行 audit→build→167文件R∪S→drift。四阶段退出码全部为0；没有沿用发现轮或中止轮作为最终通过依据。

| 最终门禁 | 结果 |
|---|---|
| R∪S审计 | 167份显式测试文件，清单 `tests.txt`；drift另列 |
| `npm run build` | 通过；仅既有大chunk提示 |
| 完整R∪S | **167文件通过；3358 passed / 0 failed / 8既有skip / 1既有todo** |
| `npm run test:drift` | **1 passed / 0 failed**，四种子104层 |
| 输入冻结 | **559文件 before=after=current，差异为空** |
| 基线 | generation仅一次重捕获；三个退役p2基线原SHA不变 |
| 交付检查 | `git diff --check`通过；index为空；HEAD仍为 `e560958169da5d1064a493d8736d4d5df7595730` |

原始退出码与时间见 [final-gates.json](u-17b-evidence/final-gates.json)，逐例结果见 [regression-final.json](u-17b-evidence/regression-final.json)，交付复核见 [delivery-verification.json](u-17b-evidence/delivery-verification.json)。最终批次启动后未再修改生产实现、测试、脚本或基线；只补写本报告、进度日志与派生证据。

工具环境最初整目录符号链接使Vite尝试写只读主检出缓存，失败日志保留；改为本工作树内复制已有node_modules后运行，没有安装依赖或修改lock。Chromium在沙箱启动被系统阻止，自动审批允许本地测试浏览器启动后完成验证。构建只有原有大chunk提示。

## 7. 保留边界

- 本轮没有恢复其余25条缺tile登记，也没有擅自接回新的autoGen/退池机器。生成期硫磺通过已存在的湖、预热环境链发生变化。
- 本轮五条都有现有可执行上游，不需要伪造来自后续族的触发。原有蓝图 `FUNGUS_FOREST→FOLIAGE` 别名仍是V4已登记差异；不宣称真菌踩踏/再生家族已实现。
- 继承U17a的生成“请求→稍后实体化”架构，没有重排U19c机器实化。DF消息/光效/地形名称经现有UI消费，未扩成全CE的tileFlavor系统。
- 病态合成地图无任何物品落位候选时保留队列，不复制CE未定义坐标；正常新层/缓存层的落位、独占和回层均有测试。
- `zh_CN.legacy.json` 在HEAD已有冲突标记；本轮仅把停止使用的熔岩消息键附入该归档，未顺带改写其既有内容。运行时使用的是 `zh_CN.json`。
- 未做CE/Web全局同种子逐回合逐RNG差分，不能把五条闭包、现有连通性/可解性回归升级为全项目CE完成声明。
