# U04c：机器格单一来源（X-0 K31）

2026-09-25；续作 U03 的同一工作树。用户明确授权生成变化，要求先记录漂移、只切换机器格来源归因、独立守卫，再重捕获基线；不做旧存档兼容。权威是本树 `BrogueCE-master`。

**K31 已修复。** 生成不再把 `∪MachineResult.cells` 当作机器格。生成、返层、保存和读档均以 `grid.machineNumber !== 0` 为依据；快照中的集合是冗余投影，缺失、重复或不一致时拒绝读取。P1-37 AD3a 的原期望及整个文件均未修改。

## 1. CE 合同与消费者核对

CE 没有一份独立的“机器 interior 并集”供运行期判定使用。`Rogue.h:1105–1113` 定义 `IS_IN_MACHINE = IS_IN_ROOM_MACHINE | IS_IN_AREA_MACHINE`；蓝图逐格维护这两个旗标及 `machineNumber`。web 已用非零机器号表示两类旗标的并集，本轮沿用该表达，不凭机器号恢复 room/area 分类。

| 来源/消费者 | CE 当前源码 | web 当前路径及处理 |
|---|---|---|
| interior 初始标记 | `Architect.c:1228–1234` 分配机器号、逐 interior 格写旗标/编号 | `BlueprintEngine.applyBlueprint` 原写口保留；`MachineResult.cells` 注释明确是施工 interior |
| 房间外 feature | `Architect.c:1482–1488` 成功实例也写旗标/编号；包括不产实体的 feature | `BlueprintEngine.ts:1593–1602` 的逐格写口/featureSpawns 保留；投影扫描全图，不限于 interior 或 itemSpawns |
| 区域机/清 interior | `Architect.c:1691–1699` 对本机器非 `TM_IS_WIRED | TM_IS_CIRCUIT_BREAKER` 格同时清旗标和编号 | 原 `BP_NO_INTERIOR_FLAG` 执行保留；派生集合不再把已清号的普通格加回来 |
| 热图、普通物品 | `Items.c:463–535` 泛洪/冷却/采样；**实际机器排除在 630–632** | `Game.populateLevel → ItemSpawnHeatMap.build` 注入统一投影，`ItemSpawnHeatMap.ts:316` 排除；原热量/拓扑/冷却算法不变 |
| 食物/力量药水、随机落物兜底 | `Items.c:729–734 → Architect.c:3822–3839`，3830 排除逐格旗标 | `randomMatchingLocation` 的 Game 回调读派生集合；`becomeAllyWith` 随机物品兜底只读该格编号，不再并入第二套 footprint |
| 楼梯/入层/坠落落位 | `Architect.c:3712/3738`；`RogueMain.c:822/829/849/858` | 楼梯牌堆本已直接排非零编号；entry/fall 读同源缓存；返层先重建投影再落位 |
| 随机传送 | `Monsters.c:1155–1204`，1175–1177 过滤 `IS_IN_MACHINE` | `CreaturePlacement.teleportCandidates` 仅读格编号，删除 `PlacementWorld.machineCells` 的独立回退来源；定向 blink/beckoning 不因本轮额外禁止机器格 |
| 钥匙 | `Items.c:4036–4044`：深度/钥匙旗标门，坐标或该格机器号匹配；`Movement.c:649` 消耗地点；`Architect.c:1523–1529` 绑定 | `keyMatchesLocation`、`matchingKeyEntry`、蓝图 keyLoc 均继续读编号/绑定；未改钥匙编号和消耗规则 |
| 其他格号消费者 | `Time.c:1186–1187/1234/1278` wired 激活；`Monsters.c:1108/1175`；`Architect.c` 候选/门墙/桥过滤 | `Promotion`、`LoopMap`、`LakeSystem`、`WallDoorFinish`、`AutoGenerator`、蓝图资格/回滚、Game 怪群/增援/建筑避让均已读网格编号，不另造集合，未改各自过滤条件 |
| 持久化 | `RogueMain.c:650/758` 保存/恢复每格机器号，并保留地图旗标 | `snapshotLevel` 从该层 grid 派生；`isSnapshot` 检查冗余投影；`loadSnapshot` 从恢复后的各层 grid 派生，不消费 RNG |

全量读写引用清单见 [machine-reference-inventory.txt](u-04c-evidence/machine-reference-inventory.txt)。上述路径按 CE 各自的资格条件消费同一格身份，不把“明确允许进入机器的位移/掉落”改成全局禁入。CE 原文与 X-0 行号概述不一致处采用真实执行分支，尤其热图排除并不在 463–535 内。

## 2. 实现及断言边界

新增 [MachineCells.ts](../../src/engine/Map/MachineCells.ts) 的纯投影和快照一致性检查。`Game.machineCells` 保留为派生缓存：蓝图写入/回滚完成后、每次替换当前 grid 时重建；当前层和缓存层序列化也直接读取各自 grid。生产机器号写口仍由蓝图构建/回滚与网格恢复负责，运行期没有新增机器号写口。

[本轮 Game 差异](u-04c-evidence/game-change.patch) 基于**续作开始时的 U03 文件**，不把同树已有 U03 改动当作 U04c。另仅改 `CreaturePlacement` 的双来源排除、蓝图 `cells` 注释、U03 字段生命周期说明。未改 CE、蓝图数据、物品目录、随机算法、配置或锁文件。

既有测试仅三处合同相关修改：

- U03 的逐 Cell 字段/环境队列往返用例，撤掉人为伪造的 `machineCells={456}`，按网格编号验证投影；完整 Cell/队列/引用隔离断言保留。
- W11 的第二个机器格从“只向 Set 加坐标”改为给 `(13,3)` 写编号8，原落点排除期望不变；删除已经移除的可选参数和无用导入。

- `blueprint_center` 不再以“旧center坐标上绝无普通物品”替代 CE 机器格合同；按真实人口产物对象及网格编号区分普通落物、声明feature与非法直投。金币还必须消费真实热图候选；同一对象只可解释一次。原center几何、全部物品可通行、feature一份指令一次消费及目录非空转断言均保留。完整归因见下节。

其他既有测试逐文件 SHA256 与本轮开始相同，尤其 P1-37。保护证明见 [boundary.json](u-04c-evidence/boundary.json)。没有旧档迁移、默认补号、对不一致集合自动修复；拒绝前不替换世界或改 RNG。

## 3. 漂移先留证、单变量归因、再重捕获

1. 修改前 [drift-before](u-04c-evidence/drift-before.txt) 通过，基线原件另存 [baseline-before.json](u-04c-evidence/baseline-before.json)。
2. 修改后、重采前 [drift-after-change](u-04c-evidence/drift-after-change.txt) 也通过。**这不表示生成无变化**：现有基线只记录 `fp/n/species/items`，不记录逐实体落位或 RNG 累计计数。
3. [u04c-attribution.mjs](../../scripts/u04c-attribution.mjs) 在三个隔离进程执行同一四种子×26层序列：A＝本轮开始的原 Game；B＝A 只将 `populateLevel` 的 `∪mr.cells` 换为全图编号扫描；C＝最终实现。唯一替换文本存于 [single-variable.patch.txt](u-04c-evidence/single-variable.patch.txt)，真实工作树未往返切换旧实现。
4. A 的基线字段与原基线 **0 差异**。B 与 C 的全部输出逐字段相同：生成前网格指纹、蓝图集合、完整世界快照哈希、物品/怪物位置、两流 RNG 状态及计数。不是只比较四个滚动字段。原始 [A](u-04c-evidence/generation-control.json)、[B](u-04c-evidence/generation-sourceOnly.json)、[C](u-04c-evidence/generation-final.json) 和 [归因汇总](u-04c-evidence/drift-attribution.json) 均保留。

| A → C，104层 | 有变化的层数 |
|---|---:|
| `fp/n/species/items` | 0 |
| 机器格集合（排序后比较） | 94 |
| 物品位置 | 88 |
| 物品种类/名称（ID仍相同） | 2 |
| 怪物位置 | 3 |
| 实质 RNG 累计计数 | 45 |
| 层结束时两流 RNG 的内部状态 | 0 |

完整序列化哈希104层不同，其中含机器格数组顺序变化，不能把104层都描述为地形或玩法变化。U02b 的生成流隔离使局部生成耗骰变化可以反映在累计计数，而恢复后的 live stream 内部状态不变。四种子的首次物品落位变化分别为 `424242/D2、777/D1、20260913/D1、31337/D4`；首次累计计数变化为 `D7、D26、D18、D12`。

物品种类变化也已显式核对：[item-identity-drift.json](u-04c-evidence/item-identity-drift.json)。`424242/D8` 的 Confusion potion 变为 Speed potion；`20260913/D18` 的 Axe 变为 Wand of Beckoning、Incineration potion 变为 Paralysis potion。三实例 ID 均未变，B版完整复现这些变化；不把基线四字段不变解读为物品内容不变。

原故障 `424242/D2` 的蓝图为 `trap_paralysis_revealed` 和两台 `ce_58_bloodwort`：interior 并集45，逐格编号15，30格已被 CE 式清号流程释放。修复后生成即15格，保存/读取/返回仍与 grid 精确相等。外部 feature 的相反方向由真实 `777/D3` 等样本与独立合成蓝图共同覆盖。

重采发生于以下独立守卫通过后，由 [u04c-recapture.mjs](../../scripts/u04c-recapture.mjs) 检查门禁结果再写入。重采原有104行数值完全相同，只更新 `note` 注明来源与实际漂移证据；没有改基线测试、添加宽容区间或隐藏差异。[recapture.json](u-04c-evidence/recapture.json)：

- 前 SHA256：`c9f36fb51f8c0d8c3ac4d94b0ff6fbdb0889332915e508ccd6421495bc9fd704`
- 后 SHA256：`572230efb156eb658bec5f5a7668bd3c847a6fafc167e7540cc94c87fb8e64e7`

## 4. 独立守卫与浏览器

新增 [u_04c_machine_cells.test.ts](../../src/test/u_04c_machine_cells.test.ts) 共7项：真实区域机清旗/保留wired、成功外部feature及钥匙正反匹配、原故障生成即正确及跨层连续/恢复等价、真实蓝图向热图传递精确集合、热图/随机落物/entry/fall/teleport消费者，以及当前/缓存层缺集合/多格/少格/重复格损坏档拒绝。期望由独立坐标扫描或确定蓝图布局给出，不用生产投影函数给自己做 oracle。纯投影另验网格和双流 RNG 不变。

[u04c-negative-check.mjs](../../scripts/u04c-negative-check.mjs) 只在临时副本把来源恢复为 `∪mr.cells`，原故障与真实外部 feature 两项均失败，见 [negative-check.json](u-04c-evidence/negative-check.json)。预期失败不计入最终正向门禁通过数。

重采前额外运行：P1-37/U03/W11共60项；B4b/V6共33项（含钥匙可解性）；C3/U05a共66项（含15种子×26层、390层非机器区连通性及产物唯一性）。[guard日志](u-04c-evidence/guards-fixed.txt)、[P1-37等](u-04c-evidence/targeted.txt)、[B4b/V6](u-04c-evidence/pre-recapture-guards.txt)、[C3/U05a](u-04c-evidence/pre-recapture-topology-ownership.txt)；最终结果另取完整冻结批次，不拼接这些定向结果。

应用 develop-web-game 技能，运行原 Playwright client 和 [本轮浏览器脚本](../../scripts/u04c-browser-check.mjs)。`424242/D2` 真实菜单保存→刷新→继续→楼梯返回D1，当前/缓存机器格始终分别为15/5；完成行动检查点的完整状态比较仅排除菜单新增日志与 `savedAt`，直接返回和读档后返回相等。见 [browser.json](u-04c-evidence/browser.json)。已查看 [D2读档](u-04c-evidence/browser-loaded-d2.png) 与 [D1返回](u-04c-evidence/browser-return-d1.png)，console/page errors为空。原 client 的 [文本状态](u-04c-evidence/client/state-0.json)有效，但canvas导出为黑图，不将该图称作视觉通过，使用整页截图验收。

**浏览器边界另行留痕：** 首次夹具直接调用楼梯行动后即保存，`needsRender` 未触发发现集合更新；读档后UI更新使 `visibleMonsterIds` 从空变为 `[24]`。等待渲染时间本身不能修正这一点。过梯后再实际等待一回合，形成完整行动检查点，完整比较通过。本轮未改渲染/发现逻辑，也不声称“立即过梯保存”的所有可见集合已通过；原差异见 [首跑日志](u-04c-evidence/browser-prefinal.txt)、[首字段定位](u-04c-evidence/browser-diagnostic.txt)。机器格在两种路径中都正确，U03/K31不再混为一项。

中途检查记录：新fall夹具原误期望选原点，而现有搜索从radius1开始，改用唯一合法邻格验证；未改生产落位算法。W11夹具调整后首次build发现无用DCOLS导入并移除。最初定向命令中两条文件名未匹配，实际收集B4b/V6两文件，随后明确补跑正确的C3/U05a两文件，未冒称首批已收集四文件。浏览器脚本修正前的首次冻结批次主动停止，保留于 `prefinal-first-freeze/`，不作为最终结果。

## 5. 完整回归暴露的 center 旧合同

第二批完整冻结门禁中，154个文件实际全部收集；build/drift通过，3178项通过、2项失败，失败仅为 `blueprint_center` c/e，原始结果完整保存在 [prefinal-center-contract](u-04c-evidence/prefinal-center-contract/final-check.json)。该批不是最终通过结果。

旧断言把“物品位于某台机器曾使用的center坐标”直接等同于“自创中心宝藏/未经声明直投”。这与 CE `Architect.c:1691–1699` 清号后允许普通物品、以及 CE8 空 interior 的 origin 只是锚点的语义冲突。用户要求冲突以CE为准，故修改测试合同，不恢复旧错误排除、不修改生成实现。

43种子×26层（1118层、3628台机器）的 [来源记录](u-04c-evidence/center-provenance.json) 显示：原报错17件全部是实际普通人口产物，所在格机器号为0，其中2件符合历史“宝藏类型”分类；另有1件按区域feature声明正常接纳。所有center几何和物品可通行检查均无违例。逐件列表保留，不把17改为新的容许常数。

测试通过 `spawnPopulateItem` 的实际返回对象追踪一般人口物品；金币必须有刚消费的真实热图落点才记为人口产物；之后要求该格无机器号且每个对象仅消费一次。非声明/非人口来源的直投仍失败。新增f用例独立钉住“伪造新对象不能冒充人口产物、编号格不能豁免、重复对象不能重复解释”。[测试差异](u-04c-evidence/center-contract.patch)。

[反事实入口](../../scripts/u04c-center-counterfactual.mjs) 在临时副本用旧Game跑原c/e，两条通过；在最终生成器中注入一条“向已清号center额外直接投放附魔卷轴”的变体，新c/e两条必须失败。证据见 [center-counterfactual.json](u-04c-evidence/center-counterfactual.json)。测试修正及守卫加固不改变生成结果，基线不再重采。最终从build和完整R∪S重新运行，不拿定向绿灯替代完整门禁。

## 6. 最终复跑声明

所有实现、测试、扫描器与验证脚本编辑完成后，执行 [u04c-final-check.mjs](../../scripts/u04c-final-check.mjs)，入口退出码 **0**。本节只取第三批完整冻结运行，不拼接前面的定向结果或第二批失败结果。

| 最终门禁 | 结果 | 证据 |
|---|---|---|
| R∪S反查 | 扫描160测试文件；R=153，R∪S=154；未解析导入/非字面量动态导入均0 | [closure.json](u-04c-evidence/closure.json)、[语义清单](u-04c-evidence/semantic-hits.txt) |
| 修改边界 | 156份受保护既有测试不变；仅3份旧测试按本报告合同调整；P1-37全文件SHA256不变 | [boundary.json](u-04c-evidence/boundary.json) |
| `npm run build` | 退出0；vue-tsc + Vite通过，仅保留既有大于500kB chunk提示 | [final-build.txt](u-04c-evidence/final-build.txt) |
| 常规R∪S | **153文件全部通过；3180通过、0失败、8 skip、1 todo** | [final-tests.json](u-04c-evidence/final-tests.json)、[完整日志](u-04c-evidence/final-regression.txt) |
| `npm run test:drift` | 退出0；1文件、1项通过 | [final-drift.json](u-04c-evidence/final-drift.json)、[日志](u-04c-evidence/final-drift.txt) |
| R∪S合并覆盖 | **154/154文件；3181通过、0失败、8 skip、1 todo**；缺漏/额外/重复均0 | [result-coverage.json](u-04c-evidence/result-coverage.json)、[逐文件表](u-04c-evidence/final-files.md) |
| 核心专项 | P1-37 9/9（含原AD3a）；U03 14/14；U04c 7/7；blueprint_center 6/6 | 同一最终批次逐文件表 |

**最终复跑声明：** 冻结 **518** 个输入，运行前后逐文件SHA256完全一致，`changed=[]`；见 [运行前](u-04c-evidence/final-input-before.json)、[运行后](u-04c-evidence/final-input-after.json)。输入清单SHA256为 `c44a620c89ed5d497f39787223b8e098e8486a1a1471e128d1623855ab621416`；最终生成基线SHA256为 `572230efb156eb658bec5f5a7668bd3c847a6fafc167e7540cc94c87fb8e64e7`，与重采后记录一致。全部命令、退出码与用时见 [final-check.json](u-04c-evidence/final-check.json)、[运行日志](u-04c-evidence/final-run.txt)。最终门禁之后仅回填本报告及派生证据，未再修改生产代码、测试、基线、脚本或progress。既有skip/todo和反向变体未选中项均不计作通过。

