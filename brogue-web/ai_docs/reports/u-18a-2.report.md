# U18a-2：生成侧地形判据 1–4

2026-09-25；输入提交 `d8b61a4b9e84d927bd5fe57bc9bdb8674b2a9be1`。按 [任务书](../tasks/u-18a-2.prompt.md) 和 [U18a §6](u-18a.report.md) 实施。生产修改仅 `Core/Game.ts`、`Map/Pathfinding.ts`、`Items/ItemSpawnHeatMap.ts`。第 5–7 步、A*、物品抽取分布、机器物品资格/Q 族、U05a 所有权协议均未改。

## 1. 实现与 CE 回源

| 步骤 | 最终行为 | CE 依据与边界 |
|---|---|---|
| 1 入场终点 | `entryQualifiesForPlacement` 查全层 PB；保留怪物、机器、楼梯排除，楼梯检查 layers，覆盖物不能掩盖它 | RogueMain.c:839–869、Rogue.h:1948；不读有效 terrain、isPassable 或旧燃烧投影 |
| 2 入场路径 | 全层 DL→F=-1；D 单独覆盖为 O=-2；源点距离/成本强制 1；先验合格源点立即返回；最小正距离按 x/y 次序取并列 | Grid.c:287–360；DL 不含火和硫矿，PB 终点仍排除二者 |
| 2 末回退 | 无路径才走切比雪夫环，半径从 0 开始，允许地图边界，`randRange(1,count)-1`，空集返回 null | Monsters.c:3960–4028；单候选会调用退化 randRange，但 Random 不消耗 RNG；并列候选恰一次非退化抽取 |
| 3 扫描器 | 两个共同邻格都识别 -2 与旧正 30000 的 O；F=-1/29999 禁入但不挡斜角；正阻挡哨兵也不能作为传播种子 | Dijkstra.c:48–87、117–168；保留旧正哨兵接口，不改 A* 的对角代价 |
| 4a 普通热图 | secretPass 委托已有真实地形查询，使用秘密层直接发现后继；暗杆仍挡 P | Monsters.c:1259–1311、3672–3675；无玩家记忆查询、无 DF 执行；门热度、递归顺序、降温/抽取、机器归零不变 |
| 4b 护符旁路 | 在 D26 的原 floorTiles 顺序上仅为护符筛 `ITEMS\|PB`，随机选中后从原牌堆删除该位置 | 只补落物地形资格，保留护符唯一性和原选点方式 |
| 4c 钥匙旁路 | 沿原牌堆逆序找首个 `!(ITEMS\|PB)` 格，保留锁位/机器号绑定，不新增选点 RNG | 不在共享 floorTiles 上全局过滤，以免同时改变楼梯或 horde；不改机器 feature 物品资格 |

**任务书与 CE 的边界冲突**：任务验收的“终点合格且可达”适用于有路径分支。CE 明确保留 pathing-agnostic 末回退，不能据此承诺每种人工封闭地图都可达。本轮按 CE 实现并单测该分支；自然生成连通、落位、机器可解守卫仍原样回归。CE 的楼梯占用来自保存坐标；web 仍以 layers 中的楼梯代替，未提前实施 U04 的独立坐标合同。

## 2. 分阶段漂移与反事实

选择“四步完成后统一重捕获”。施工前保存 [旧基线](u-18a-2-evidence/baseline-before.json)；每阶段独立保存三份生产输入、104 层生成记录、原始 `test:drift` 结果。种子为当前基线的 `424242 / 777 / 20260913 / 31337`，每个 D1–D26。

采样由 [u18a2-generation.ts](../../scripts/u18a2-generation.ts) 实施：原基线四字段、完整 Cell 哈希、物品/怪物状态哈希、玩家位置、双流 RNG、入场候选变化及选择前后 RNG、每次 batchScan 的输入/输出哈希；入场路径额外保留完整成本/距离网格、资格访问顺序和全部抽取参数。构造器的临时随机局在正式 startNewGame 边界清除，避免混入无关观测。`terrain` 扩展字段是完整 Cell 哈希，不是只含地形的指纹；`heat` 含构建前后 RNG，不能把该字段变化直接当成热度变化。

| 阶段 | 原基线 fp/n/species/items 变化层/字段 | 扩展观测变化 | 归因 |
|---|---:|---|---|
| s1：PB 终点 | 0 / 0 | 0 层；相对旧资格排除候选累计 756 格 | 本批选中的终点均仍合格，不伪造落位/RNG 漂移 |
| s2：DL、D/O、首格/末回退 | 0 / 0 | 1 层：入场与 scan trace | seed20260913/D6 成本网格变化；实际结果和 RNG 不变。首格/末回退独立合成场景验证，未宣称本批自然生成触发它们 |
| s3：共享扫描器 | 0 / 0 | 104 层 scan；21 层 RNG/entry；17 层完整 Cell；20 层 heat 构建前后 RNG | 修正对角捷径；D6 的并列候选改变引发一次额外 RNG，后续环境预热沿该流变化 |
| s4a：普通热图 | 0 / 0 | 0 层 | 自然样本未触发暗杆热度差；密门/暗杆对抗场景区分 3005 与 0 |
| s4b：护符 | 0 / 0 | 0 层 | 本批最终选择和观测未变；对抗牌堆证明硫矿被跳过 |
| s4c：钥匙 | 0 / 0 | 0 层 | 样本原钥匙位置仍合格；对抗牌堆证明末尾硫矿被跳过且绑定/唯一性保留 |

完整字段与每层候选明细：[attribution.json](u-18a-2-evidence/attribution.json)。每阶段原始 drift 均退出 0；“没有四字段漂移”不等于完整世界或 RNG 不变。

另以 s1 源码的实际 `entryQualifiesForPlacement` 逐格代替采样器独立的资格公式，104 层全部记录逐值一致，排除 756 格，确认本批候选记录未漏楼梯覆层或占用差异，见 [candidate-audit.json](u-18a-2-evidence/candidate-audit.json)。

关键因果链：seed20260913/D6，源点 `(10,21)`。s1/s2 最近距离 2，仅 `(9,20)`；s3 最近距离 3，并列按次序为 `(8,20),(9,20)`，`randRange(0,1)=1`，实际玩家仍落 `(9,20)`，实质抽取数多 1。之后 D6–D26 共 21 层 RNG 状态不同；地形、物种与数量四字段仍相同。逐格补证发现 D7–D26 的 17 层发生 1197 个 volume 字段变化，16 层共 167 个 **GAS 层**变化；4 格的 rememberedLayers/rememberedTerrainFlags/rememberedTMFlags 随之变化，DUNGEON/LIQUID/SURFACE 无变化。见 [cell-diff.json](u-18a-2-evidence/cell-diff.json)，重新采样同时断言其余观测与原 s2/s3 逐值相同。

[反事实脚本](../../scripts/u18a2-counterfactual.mjs) 对每阶段实际生成 unified patch，再仅反向应用该阶段补丁到临时副本：恢复字节等于上一阶段；独立进程重生成的所有记录逐值等于上一阶段。继而用上一阶段源码运行对应的新守卫，要求真实断言失败，不以进程崩溃充当检出。各阶段记录见 [counterfactual.json](u-18a-2-evidence/counterfactual.json)。没有在最终源码中保留测试开关或运行期 trace。

## 3. 共享扫描器所有读方

源码反查见 [batch-readers.txt](u-18a-2-evidence/batch-readers.txt)；未把“四向扫描不受对角变化影响”替代实际回归。

| 读方 | 方向/输入 | 本轮证据 |
|---|---|---|
| Game 入场 | 8 向，DL F、D O、源点 1 | 1 次自然调用，完整网格及并列轨迹；新增路径/占用守卫、p1_31_35 |
| Waypoint.refreshWaypoint | 8 向，generic 负成本 | 3654 次相同输入，3585 次输出距离图改变、69 次相同；p4_10、u_18a |
| LoopMap.loopDoorSiteScan | 4 向，工作网格 O/F/1 | 3950 次输入和输出全部相同；c_0_add_loops |
| Blueprint.fillAreaInterior | 4 向，负成本及强制源点 | 396 次输入和输出全部相同；v_2b_9e_area_machine |
| Blueprint.fillVestibuleInterior | 4 向，旧前厅 mask | 31 次输入和输出全部相同；V/c_8；未顺手修前厅 PB |
| Architect.redesignInterior | 4 向，外部 -2、内部 1、多源 0 | 本批自然生成未观察到调用；现有 c_8、V 施工/连通守卫以及多源定值专项回归 |
| SafetyMap 两遍 | 4 向，玩家/怪物双 cost，距离可负 | p4_9 的数值/快照/梯度及 u_18a 全层状态表原样回归 |
| MonsterBlink.scanBlinkMap | 四向复用 batchScan；八向仍为 U07 独立实现 | U07 守卫原样回归；不借本轮合并扫描实现 |
| 旧 calculateMap | 8 向、正哨兵 | 未找到生产调用；旧接口保留。专项覆盖正/负 O/F、两侧斜角、阻挡种子、零成本通道、多源负距离及边框 |

上述自然调用统计来自 s2/s3 每次扫描的输入/输出比较；没有观察到的消费者明确以定向守卫为证据，不宣称自然样本覆盖。

## 4. 守卫与施工自检

新增 [u_18a_2_generation_terrain.test.ts](../../src/test/u_18a_2_generation_terrain.test.ts) 共 32 项：全层终点真值（火盖水、暗杆、压板、硫矿、透明阻障、楼梯覆网）、怪/机器占用与四邻序；DL/火路/硫矿路、D/O、早退/无路/边界/并列序；负与正扫描器输入；暗杆与密门热度、机器及物品禁区；护符/钥匙实际 populateLevel 旁路与唯一性。钥匙选择前 RNG 等于牌堆处理前状态，绑定字段不变。对抗测试中的抽取控制只作用于一次护符选择，几何分布循环仍走真实 RNG。

R∪S 由 [u18a2-audit.mjs](../../scripts/u18a2-audit.mjs) 构建：四层、地形规则、扫描器、热图、Game 等 R 的反向字符串引用闭包（包含读源码路径），与 src/scripts 的 S 合并（仅排除本轮 u18a2 观测/反事实脚本，避免自引用；原有脚本全部参与）；额外明确包含 p1_30、U24、U01/U03，readFile/readFileSync 命中也收入。44 份读源码测试均在清单内。显式清单共 158 份，见 [tests.txt](u-18a-2-evidence/tests.txt) 和 [search.json](u-18a-2-evidence/search.json)。原有测试、扫描器期望及源字段合同不改；生成基线仅在归因和守卫完成后按授权统一重捕。没有运行无参数全套测试来替代反查清单。

保留的关键守卫包括 p1_26/29/30/31_35/37、c_0…c_8、b_4b、p1_20、全部选中的 V/blueprint/U05a、p4_9/10、U07、U01/U03/U24 和旧字段/DF 读者白名单。楼梯、horde、机器 feature 的物品资格和产物所有权未改；不把热图 secretPass 用作物理位移许可。

施工中遇到的问题均未通过放宽原守卫消除：依赖根链接使 Vite 临时文件写到沙箱外，改为逐包只读链接、缓存留本树；首次候选采样混入构造临时局，修观察边界后重采 s0/s1；新夹具错误写只读 isBurning、全局固定最低 RNG 导致几何循环不退出、抽离静态 spawnKey 丢失 this，均修正夹具。早期混合批次和崩溃输出不是最终通过证据；最终测试用 afterEach 恢复观察 spy，避免反事实失败污染后续用例。

浏览器使用 develop-web-game 技能客户端及 [本地全页检查](../../scripts/u18a2-browser.mjs)：seed20260913 的 D1/D6/D26 真实渲染、玩家 PB 合格、D26 一件护符、键盘等待、1280×900 与 700×900；console/page errors 均为空。技能客户端 canvas 导出仍为黑图，已经实际打开，未计作视觉通过；有头全页截图可正常看到地图/玩家/侧栏，已逐图查看：[D6](u-18a-2-evidence/browser-d6.png)、[D26](u-18a-2-evidence/browser-d26.png)、[窄屏](u-18a-2-evidence/browser-narrow.png)。这是生成/渲染 smoke，不冒充交互式 CE 全流程差分。

## 5. 最终门禁、基线与哈希

入口：[u18a2-final-check.mjs](../../scripts/u18a2-final-check.mjs)。重捕获前的完整 158 文件批次已通过（3243 passed、0 failed），然后仅统一重捕一次，再执行以下冻结批次；没有拼接早期批次作为最终通过数字。

| 检查 | 最终结果 | 证据 |
|---|---|---|
| R∪S 显式回归 | 158/158 文件通过；3243 passed、8 skipped、1 todo、0 failed；1257.94 秒 | [原始 JSON](u-18a-2-evidence/regression-final.json)、[日志](u-18a-2-evidence/regression-final.txt)、[逐文件结果](u-18a-2-evidence/final-results.md) |
| 清单覆盖 | 缺漏、额外、重复均 0 | [coverage-final.json](u-18a-2-evidence/coverage-final.json) |
| 新增专项 | 32/32，通过且计入上述总数 | 同一最终 JSON 中的 u_18a_2_generation_terrain 文件 |
| build | vue-tsc 与 Vite 退出 0；仅既有大 chunk 提示 | [build-final.txt](u-18a-2-evidence/build-final.txt) |
| test:drift | 1/1，通过；4 seeds × D1–D26；20.17 秒 | [drift-final.txt](u-18a-2-evidence/drift-final.txt) |
| 输入冻结与原守卫 | 511 个输入前后 SHA256 相同，changed=[]；171 份既有测试/fixture 原字节不变（另列的滚动基线除外） | [输入哈希](u-18a-2-evidence/final-input-sha256.json)、[final-audit.json](u-18a-2-evidence/final-audit.json) |
| 补丁格式 | git diff --check 退出 0 | 同上；报告回填后再次检查 |

**最终复跑声明**：以上为统一重捕获之后、最终生产源码与测试上的完整批次。原有 skip/todo 不计作通过；加入独立 drift 后合计 3244 项通过。入口在 Node 中加载后曾短暂追加自身的覆盖清单检查，随即恢复为冻结字节，未改变正在执行的代码、命令、产品源码或测试；最终 511 项输入字节核验仍全同。覆盖清单由结束后的只读检查单独验证。此后仅回填报告、progress 与派生证据。

重捕获 **只有 note 一行变化**，原四字段的 104 层值逐值相同；第 3 步的 RNG/GAS 漂移由扩展观测单独保存，未扩展或放宽原基线字段。见 [recapture.json](u-18a-2-evidence/recapture.json)。

- 基线前 SHA256：`acf81e28f2b87d65c74c0e181c16287446dcfcc3966120f96dcaa933acaa9fe2`
- 基线后 SHA256：`1ee4216e44e04b04f477149d29f5f1493b7d8a0ff4cfbcef32cdf038db80a28e`

依赖复用本机主检出的逐包链接，缓存和 dist 留在本工作树；两处 package-lock SHA256 均为 `3d9027a9ff655ec834674ebbf4b8ff0c00257b8906a375270163ecba5f53e253`。未安装或改动依赖，不宣称全新安装验证。没有改 CE、提交或暂存文件；本轮也不声称完成后续第 5–7 步或 CE 二进制全游戏差分。
