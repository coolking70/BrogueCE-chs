# U17c：触发、陷门与暗杆载体

任务书：`ai_docs/tasks/u-17c.prompt.md`。基准 HEAD：`78c6f2298e54e3307b35d7a5919d742663eba304`。本轮未提交、未暂存。

## 交付范围与 CE 裁决

仅闭合并移除缺口登记 **154、17、152、95、144**。新增五个独立 TerrainType，旧枚举值保持不变；新增必要后继 DF96，不冒用现有测试重置板或普通洞口。其余20条登记保持原顺序：

`179,180,182,183,185,85,140,141,143,155,187,174,175,14,19,87,88,145,148,191`。

本地 CE 是裁决依据。任务书的“CE24 暗杆机器”与本地 `GlobalsBrogue.c` 不符：暗杆前厅是 **CE18 / vestibule_secret_lever**，CE24 是 beckoning。未修改 BlueprintEngine 的资格过滤；暗杆前厅、蠕虫隧道和退役炮塔机器均未回池。

另一项必须区分的事实：**DF152 是 CE22/F0、CE28/F1 的生成期中等坑洞，不是踩压力板后才挖洞**。压力板通过机器编号接线开闸/收笼；本轮删除了旧的“半径3触发任意普通陷阱并变地板”的自创路径。

## CE 数据、三链与消费者

独立脚本 `scripts/u17c-ce-golden.mjs` 直接解析 CE Globals.c / Rogue.h，生成5条 tile、7条 DF 黄金记录；不导入 web 目录。外观生成器扩展至149条，旧144条值完全相同。来源哈希及旧行比较见 [protected-and-ce-inputs.json](u-17c-evidence/protected-and-ce-inputs.json)、[appearance-extension.json](u-17c-evidence/appearance-extension.json)。

| DF | CE 数据与 tile | 上游及完整落点 |
|---|---|---|
| 154 | Globals.c:815；MACHINE_PRESSURE_PLATE_USED，DUNGEON，0/0，flags=0，无后继 | 生物踩板或物品落板 → 既有 Promotion wired 事务 → 同编号消费者晋升 → 留下不可重复触发的已用板。不同编号与断路器负例保留。 |
| 17 | :628；TRAP_DOOR，LIQUID，0/0，CLEAR_OTHER_TERRAIN → DF16 | 搜索 TRAP_DOOR_HIDDEN → 清密格所在层 → U17a DF事务铺洞、清其他层、生成 halo/闪光 → 既有玩家确认/坠层、怪物坠层与落物队列。未发现陷门直接触发坠落；悬浮豁免。 |
| 152 | :813；TRAP_DOOR，LIQUID，225/100，CLEAR_OTHER_TERRAIN\|SUBSEQ_EVERYWHERE → DF16 | 恢复 CE22/F0、CE28/F1 的 featureDF；施工先铺坑，每个传播格执行 halo。真实 CE22 蓝图生成15格洞后，玩家从安全地面投掷长剑压板，出口开启。 |
| 95 | :732；WALL_LEVER，DUNGEON，0/0，flags=0，有发现描述及 GENERIC_FLASH_LIGHT | 隐藏墙杆（包括 DF_CREATE_LEVER 生成的隐藏态）→ 搜索显形 → 玩家撞杆、不移动且耗时 → DF96 铺 WALL_LEVER_PULLED → 同编号闸门开启。再次撞击不重复发送电力。 |
| 144 | :799；MACHINE_TRIGGER_FLOOR_REPEATING，LIQUID，300/100，SUPERPRIORITY，传播地形 CARPET | 恢复 CE7/F2；无字形、无前景/背景/发光，保留 wired 与 PLAYER_ENTRY，不消失；玩家进入和等待可重试。空复活祭坛不消耗机会，后来出现可复活盟友时，同一地板成功复活。怪物/物品不代替玩家触发此旗标。 |

Tile 原列：USED :403，TRAP_DOOR :380，WALL_LEVER :348，PULLED :349，REPEATING :540。TRAP_DOOR 带 T_AUTO_DESCENT；两种显形墙杆阻挡旗标仍按墙体；重复地板无 VANISHES。五种均 NO_LIGHT。DF16 本来已有，保留其 halo 与闪光；新增 DF96 (:733) 是墙杆必要终点。

所有铺装/后继/效果通过现有 `spawnDungeonFeature` 事务。搜索先预检链上缺失载体，再清密格层；后续未闭合族不会被清成空地。新入口放在 Promotion 模块，既有源码读者白名单没有放宽。Game 中原先仅限藤蔓的 PLAYER_ENTRY 处理扩至旗标消费者；目标时间步也覆盖该旗标。DF_CREATE_LEVER 产生新密格会使“本层无密格”缓存失效。

本族 DF 没有 EVACUATE_CREATURES，不能自造疏散。怪物原地被标记坠落，普通物品排入既有跨层队列，药水沿既有坠落销毁规则处理；存读档、下层只接收一次的行为由独立守卫验证。未更改存档格式或复制一套落物系统。

`AutoGenerator` 第25行原 web 标签误写为 TRAP_DOOR 且未接线；CE variants/GlobalsBrogue.c:143 实为 **TRAP_DOOR_HIDDEN**。恢复该入口，保留原数值列：D9..39、频率/深度参数20/100/0、次数上限2。

仍登记的边界：其他毒气/沼气/火嘴显形、祭坛惰性态、棺木/蠕虫等20条缺口未接；搜索只新增本族的隐藏陷门与墙杆。复活最终消费复用已实现的 U16 行为，其 RESURRECTION_ALTAR_INERT / DF143 仍在缺口登记，本报告不将其算作恢复。独立构造的暗杆交互不等于暗杆整台机器恢复资格。

## 用户动作与独立验收

新增 `src/test/u_17c_triggers.test.ts`：17条，覆盖 CE 字面数据、准确缺口集合、外观、真实数据起点、不同编号/断路器、踩踏/怪物/投掷、搜索遮挡、显形清层/闪光、撞杆耗时与存档、创建密杆后的缓存、重复地板及复活重试、坑洞落物/怪物/持久化、完整 CE22 可解性。

10种隔离副本负向破坏全部翻红：[negative-summary.json](u-17c-evidence/negative-summary.json)。分别拿掉四类载体、撞杆入口、陷门/墙杆搜索、恢复旧半径板、拿掉物品晋升、拿掉等待触发、把两个中等洞数据起点降为单洞。失败数依次为7/6/5/4/2/3/2/2/2/2。

按 develop-web-game 技能调用了指定 Playwright 客户端；其无头截图为黑画布，保留在 `skill-client/`，未作为视觉通过证据。随后使用有头 Chromium 跑真实页面输入并逐图检查，页面错误数组为空：[browser-results.json](u-17c-evidence/browser-results.json)。场景状态通过测试布景注入，操作使用真实键盘/鼠标和游戏入口；CE22 使用生产 BlueprintEngine 构造完整蓝图。

- [踩板后的已用板与开闸](u-17c-evidence/browser-pressure-used.png)
- [搜索后撞杆](u-17c-evidence/browser-lever-pulled.png)
- [搜出陷门](u-17c-evidence/browser-trapdoor-found.png)、[玩家坠入D2](u-17c-evidence/browser-trapdoor-d2.png)
- [重复触发成功复活盟友](u-17c-evidence/browser-repeating-raised.png)
- [CE22 投掷开门](u-17c-evidence/browser-medium-hole-solved.png)、[窄窗口](u-17c-evidence/browser-narrow.png)

首个浏览器尝试把投掷目标设为相邻格，页面将相邻点击解释为移动；证据失败保留。验收布景改为距离3的合法投掷位置后通过，未改动界面输入规则。

## 生成流归因与唯一重捕获

初始 `npm run test:drift` 通过，激活后的旧基线失败留证。归因使用四个滚动基线种子 × D1..26 = 104层，同时记录四个基线字段、四层地形、实体/物品/玩家状态、RNG及载体数。阶段以 esbuild 内存覆盖构造，源码与基线不在阶段之间来回改写。完整记录见 [generation-stages.json](u-17c-evidence/generation-stages.json) 与各 `inputs-*.json`、`generation-*.json.gz`。

| 对照 | 单一杠杆 | 改变的记录字段数 | 四个基线字段变化 |
|---|---|---:|---|
| HEAD→s0 | 仅增加目录/枚举，五DF仍空载体、旧入口 | 0 | 0 |
| s0→s1 | DF154 | 0 | 0 |
| s1→s2 | DF17 | 0 | 0 |
| s2→s3 | DF152及其CE22/28数据起点 | 91 | fp4、species1、items1 |
| s3→s4 | DF95 | 0 | 0 |
| s4→s5 | DF144及CE7数据起点 | 23 | fp2、n2、species2、items2 |
| s5→s6 | 板的运行期触发 | 0 | 0 |
| s6→s7 | 陷门搜索、密门DF事务 | 0 | 0 |
| s7→s8 | 墙杆搜索/撞击/缓存 | 0 | 0 |
| s8→s9 | 重复地板玩家进入/等待及显示文案 | 0 | 0 |
| s9→final | 隐藏陷门autoGen第25行 | 737 | fp72、n67、species70、items60 |

字段数不是格数；各阶段是相对上一阶段，不能把跨阶段次数当作最终唯一变化量。生成期主要变化明确来自152、144与autoGen，运行期入口本身在这104层观测上无变化。

独立守卫与归因完成后，仅执行一次 `scripts/u17c-recapture.mjs`。脚本先校验原哈希、拒绝第二次写入，并校验最终观测的源文件哈希。实际改变74层、272个四字段值：fp74 / n67 / species71 / items60；只另更新说明note。记录：[recapture.json](u-17c-evidence/recapture.json)。

- 滚动基线前：`56622a15c6fb41099a2a6a2161b3e27df228b55391adbacec768d4265f4d655c`
- 滚动基线后：`35e673280602484d81c787209cb0c8a35e8081a766c0c183feeb6397bbcfc2f3`
- p2 / p2_2 / p2_3历史基线保持原哈希，完整列表见 `baseline-before.json` / `baseline-after.json`。

## 旧守卫反事实与前提修正（交验收方裁决）

发现轮169文件：3332通过、36失败、8既有skip、1既有todo。未将该施工期混合输入运行宣称为最终通过。原始 HEAD 隔离副本对全部36条原失败断言复跑，**36通过、0失败**：[head-premises-late.json](u-17c-evidence/head-premises-late.json)。较早的19条原断言也单独全部通过。

修正只涉及已证实的前提；生产行为错误先改代码。最初把 bump 直接调用 promoteTile 及在 Game 读取原始 promote/mech 字段触发源码白名单；已移入 Promotion，白名单原文保留。P1-30首次施工期还发现新增文案键尚未补齐，只补中文资源键，P1-30与U24守卫不变。

| 原前提 | 证据与修正 |
|---|---|
| 144个tile、25条缺口、旧DF总数 | 五个新tile、指定五缺口移除、必要DF96；扩展逐字段/逐集合断言或保留历史投影，未以宽松下限代替精确数量。涉及 C4a/0/b、C7、R1、G2、V3/4/5、U08、U17a/b。 |
| autoGen25无载体 | CE原行为隐藏陷门；C6精确检查其激活身份、旗标与原数值，其余未接入口仍逐条断言。 |
| CE7/22/28 featureDF必须缺省 | 替换为精确数据起点、tile及catalog转换正向断言；CE6/F2 MAGIC_PIPING仍明确缺省、仍抛缺载体错误。 |
| C4b F3仅旧的层组合 | 原断言副本收集拒绝格，并追踪每格真实写者：[layer-writer-trace.json](u-17c-evidence/layer-writer-trace.json)。CE纯层写入验证后仅补七种完整四元组；三层上限、GAS恒空、机器归属原守卫保留，MUD_WALL也要求归属。 |
| P1-42 A6 的r8布景之外没有可搜索对象 | seed42511在(17,11)/(19,11)/(20,10)/(20,11)/(21,11)有可见自然陷门，[search-probe.json](u-17c-evidence/search-probe.json)。仅该用例清理至完整FOV半径10；15种子、5连搜、零骰与隔墙密门断言全部不变。 |
| W11压力板会遥控相邻普通传送陷阱 | CE机器板没有该半径行为；布景改为同格普通陷阱+LIQUID压力板。实际入场者、两次环境处理、目的地一次晋升等原断言全部保留。 |
| C2在autoGen之后仍能用现存硫矿验证原始镶边 | seed424242/D18 (21..23,23)的硫矿被后铺DF_CRYSTAL_WALL清除，[lake-probe.json](u-17c-evidence/lake-probe.json)。将同一逐格半径2断言观察点放到真实fillLakes返回处，原函数照常执行；之后autoGen、清理、连通性、幂等、深度门槛与统计继续全跑。 |
| B2的CE71雕像仅可能被CE70藏怪墙替换 | seed22/D15 (59,19)被楼梯准备步骤改为TORCH_WALL，哨兵仍存活且home=3；[stair-writer-trace.json](u-17c-evidence/stair-writer-trace.json)记录实际调用栈，CE Architect.c:3663–3668原样如此。守卫仅接受真实prepareStairLoc调用中从STATUE_INERT改为TORCH_WALL的记录，并要求machineNumber=0、IMPREGNABLE；原CE70分支、三哨兵、不同落点、实际存活者、home、完整34种子连通性全部保留。 |

前提修正后的完整B2复跑：21通过，34种子×26层全样本，见 [b2-updated.json](u-17c-evidence/b2-updated.json)。其他定向复跑保留在 `premises-updated.json`、`target-last.txt`（98通过）、`closure-extra.txt`（35通过）；V3第一次定向复跑还暴露了内层旧缺口列表中的144，已按同一HEAD反事实修正为精确非缺口断言，随后全文件通过。

F3新增四元组（DUNGEON,LIQUID,GAS,SURFACE）：MUD_FLOOR/WATER_SHALLOW/NOTHING/{NOTHING,GRASS,BONES}，MUD_WALL/WATER_SHALLOW/NOTHING/NOTHING，FLOOR_FLOODABLE/WATER_SHALLOW/NOTHING/NOTHING，FLOOR_FLOODABLE/NOTHING/NOTHING/FOLIAGE，NOTHING/WATER_SHALLOW/NOTHING/ASH。依据 CE Goblin warren features、CE34 FLOOR_FLOODABLE、DF_ASH与Architect.c:1454纯层写入；不是任意地形笛卡尔积。

## R∪S、最终复跑与交付状态

最终闭包为 **171个测试文件**。审计脚本 `scripts/u17c-audit.mjs` 从生产文件及变动JSON数据起点计算反向导入闭包，并合并DF/搜索/地形/效果/源码读取文本命中。源码扫描守卫通过字符串路径也纳入；包括项目要求的p1_30、U24、U01/U03、C4a/b/c、C7、R1、F/G、V、W13/14、U17a/b，以及src/test以外的消费者测试。清单、依赖图、源命中与旧缓存读写分别落在 `tests.txt`、`search.json`、`legacy-accesses.json`。

最终使用 `scripts/u17c-final-check.mjs` 在冻结输入上顺序运行审计、build、显式R∪S、test:drift；同时记录src/public/scripts及配置的前后SHA256，不以分批局部绿代替最终完整复跑。现有8个skip和1个todo保持原样，未新增跳过、缩小样本、提高超时或放宽源码白名单。

环境记录：复用本机已有依赖，未改package清单/lockfile；Vite缓存目录保留在当前工作树。最初默认沙箱无法绑定端口/启动浏览器，使用获准的本地开发服务和有头浏览器完成验证。build保留既有大chunk提示，未产生编译错误。

| 门禁 | 结果 | 开始 / 结束（北京时间） |
|---|---|---|
| R∪S审计（171文件） | PASS，exit 0 | 2026-09-25 23:22:51 / 2026-09-25 23:22:52 |
| npm run build | PASS，exit 0 | 2026-09-25 23:22:52 / 2026-09-25 23:23:09 |
| 显式R∪S（--maxWorkers=6） | PASS，exit 0 | 2026-09-25 23:23:09 / 2026-09-25 23:58:38 |
| npm run test:drift | PASS，exit 0 | 2026-09-25 23:58:38 / 2026-09-25 23:59:00 |

最终回归：**171文件通过；3403通过、8既有skip、1既有todo，共3412项；无失败**。完整结果：[regression-final.json](u-17c-evidence/regression-final.json)、[final-gates.json](u-17c-evidence/final-gates.json)。drift为1文件/1项通过；build通过，既有chunk提示不影响编译。

**最终复跑声明：**上述门禁在唯一重捕获后、同一组冻结输入上从头顺序执行。冻结前后580个输入文件SHA256全等，差异列表为空：[frozen-input-differences.json](u-17c-evidence/frozen-input-differences.json)。最终基线哈希与唯一重捕获记录一致，三份历史基线不变。冻结后仅更新报告、progress与证据汇总，没有改动代码、测试、脚本、资源、配置或基线。

`git diff --check`通过；HEAD仍为任务开始值；暂存区为空。全部交付留在工作树，未提交。
