# U19f 执行报告

**完成状态：CE52、CE55 与剩余 25 条 autoGen 已闭包回池；一次基线重捕获完成，冻结门禁全部通过。旧守卫前提修订作为 §5 提案交验收方裁决。**

本轮按本地 `BrogueCE-master`、X-0 §4.3 U19 / §3.1 K28、K32 和 `u-19f.prompt.md` 执行。起点 HEAD：`e6247714a04358be3cde9bd3329c82e091730cc4`。没有提交、没有暂存。

## 1. 实现与 CE 对照

CE52、CE55 已解除 `blueprintQualifies` 中的显式退池。全部 CE 蓝图均通过自身深度/旗标的资格校验，D2 的 `RETIRED_INVENTED_BLUEPRINT_IDS` 保留。`RETIRED_AUTOGENERATOR_MACHINES` 仍为空。

49 条 autoGen 的 terrain、layer、DF、machine、两个 foundation、深度、frequency、intercept、slope、maxNumber 由只读取 CE C/H 的 `scripts/u19f-ce-catalog.mjs` 独立抽取，并逐列比较。恢复 25 条原被 carrier 阻挡的行；48 条可执行行现在全部 wired。index 0 仍不执行：CE Architect.c 的循环从 AG=1 起，这不是 web 自行退池。完整逐行账本见 [49 行矩阵](u-19f-evidence/autogen-49-matrix.md)。

补齐 21 个地形和 24 个 DF，追加枚举到 169–189，既有 0–168 不变。逐字段含 flags、mechFlags、三条晋升链、概率、光源、DF 后继/传播/闪光/描述、优先级及外观。中文名称与 DF 消息随载体补齐。独立 CE 源哈希保存在 `u19f-ce-catalog.json`；外观黄金表用既有 CE 提取器扩至 190 项。

CE 撞杆入口已由 U19d 的通用 `promoteOnPlayerBump` 实现，符合 Movement.c:1152–1160，本轮保留并在整机反事实中覆盖。U19e 的闭笼领养物品修复与 U17f 的 DF191/192 也直接复用。

实际交互发现并修复三处缺口：

- 搜索从硬编码旧隐藏地形清单改为跨层 `TM_IS_SECRET`，使新陷阱进入同一搜索/发现链。
- 储罐 DF 的 CE 闪光色新增到 Game 的真实消费表，避免撞碎时抛缺色异常。
- `finishWalls` 原 `setTerrain` 会清空其他层，抹去暴露边缘的休眠虫道标记，剩余埋墙标记无法开始扩展。按 CE Architect.c:2496/2517 只写 DUNGEON，保留 LIQUID/SURFACE/GAS。没有改动墙暴露判据。

`FUNGUS_FOREST` 的旧 FOLIAGE 别名也恢复为独立菌林，保留 CE 的菌光、踏倒、再生和燃烧链。此项单独归因，没有混进 autoGen row 9。

## 2. 两台自然机器的完整交互

自然样本扫描 24 seeds × D1–26，共 624 层，只登记最终 `populateLevel` 接纳的机器，排除生成重试中丢弃的提交。CE52 / CE55 各找到 1 台，CE65 / CE66 各 18 台；原始索引和快照见 `natural-index.json` / `natural-*.json.gz`。

**CE52：seed 17，D19，machine 3。** 玩家位于真实入口，82 个正常命令。撞原有 turret levers，原生 4 台炮塔从 dormant 醒来；按普通通行路线站到电晶后方，让炮塔实际 SPARK 射击，点亮全部 4 枚电晶；笼子收起，取得原领养奖励 ID 1451，再返回入口。没有提供闪电杖、直接晋升、传送炮塔或替换奖励。自然再生成测试与浏览器逐键回放均验证同一链。

**CE55：seed 13，D16，machine 4。** 入口 (41,9)，394 个正常命令。搜索显露墙杆，撞杆后 73 个 dormant 标记激活，隧道自然扩展；第 59 个命令时已开路。保留 4 条原生 Underworm 的正常 AI/战斗，途中击败它们，取得原钥匙 ID 341，返回入口。旧墙层实现的同一机器在 350 次等待后仍无路；撤销墙层修复的反事实再次失败。

共同观测边界：只在开始时将玩家移至实际入口、提高 HP 至 10000、移除无关怪物；保持本机居民、正常行动、战斗及 RNG，不宣称普通血量通关难度或所有种子都已穷举。奖励唯一性及存取档往返另行断言。

## 3. 每条恢复行的独立闭包与漂移

先运行旧 `test:drift`，1/1 通过，原始基线未变。固定四 seeds × 26 层共 104 层，逐阶段启用：旧 HEAD → 新载体支持但旧选择 → CE52 → CE55 → 菌林别名恢复 → 每一条 autoGen → 墙层保留。加载器变体不来回写工作树。每阶段记录地形指纹、实体/物品、休眠怪物、RNG、机器产物和视图哈希，详情见 `attribution.json` 及 `generation-*.json.gz`。

支持层本身变化 0 层；CE52 变化 71 层，CE55 变化 33 层，菌林别名变化 3 层（仅 fp/terrain）。以下“变化层数”是相邻阶段任一记录字段有差异的层数，包括 RNG 的后续传播，不表示机器在这些层都出现。

**autoGen 2（CE GlobalsBrogue.c:116）**：0 / DF_LUMINESCENT_FUNGUS。生成发光菌，玩家踏入并验证光照，1 个命令。该阶段变化 80/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 4（CE GlobalsBrogue.c:118）**：0 / DF_DEAD_GRASS。生成枯草并踏入，1 个命令。该阶段变化 92/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 5（CE GlobalsBrogue.c:119）**：0 / DF_DEAD_GRASS。按第二个深度窗口生成枯草并踏入，1 个命令。该阶段变化 72/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 6（CE GlobalsBrogue.c:120）**：0 / DF_BONES。生成骨堆并踏入，1 个命令。该阶段变化 60/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 9（CE GlobalsBrogue.c:123）**：0 / DF_FUNGUS_FOREST。踏倒菌林、离开后自然再生；另以玩家火杖验证焚烧，362 个命令。该阶段变化 56/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 10（CE GlobalsBrogue.c:124）**：0 / DF_BUILD_ALGAE_WELL。生成藻井、等待蓝/绿藻扩展、玩家进入深水，13 个命令。该阶段变化 68/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 11（CE GlobalsBrogue.c:125）**：STATUE_INERT / 0。生成地面雕像，玩家撞击保持阻挡，1 个命令。该阶段变化 84/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 12（CE GlobalsBrogue.c:126）**：STATUE_INERT / 0。按另一深度窗口生成雕像并撞击，1 个命令。该阶段变化 68/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 13（CE GlobalsBrogue.c:127）**：TORCH_WALL / 0。墙面生成火把，验证发光和阻挡，1 个命令。该阶段变化 84/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 15（CE GlobalsBrogue.c:131）**：NET_TRAP / 0。踏中罗网、缠绕、挣脱返回，4 个命令。该阶段变化 100/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 17（CE GlobalsBrogue.c:133）**：ALARM_TRAP / 0。踏警报使原睡眠见证怪进入追踪，1 个命令。该阶段变化 92/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 18（CE GlobalsBrogue.c:134）**：GAS_TRAP_CONFUSION / 0。踏迷惑气陷阱并受迷惑，1 个命令。该阶段变化 100/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 20（CE GlobalsBrogue.c:136）**：FLOOD_TRAP / 0。踏涨水陷阱、观测真实洪水，1 个命令。该阶段变化 68/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 22（CE GlobalsBrogue.c:140）**：NET_TRAP_HIDDEN / 0。搜索显形，再踏中罗网并挣脱，10 个命令。该阶段变化 84/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 24（CE GlobalsBrogue.c:142）**：ALARM_TRAP_HIDDEN / 0。搜索显形，踏警报唤醒见证怪，2 个命令。该阶段变化 76/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 26（CE GlobalsBrogue.c:144）**：GAS_TRAP_CONFUSION_HIDDEN / 0。搜索显形，踏陷阱释放迷惑气并受状态影响，6 个命令。该阶段变化 64/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 28（CE GlobalsBrogue.c:146）**：FLOOD_TRAP_HIDDEN / 0。搜索显形，踏陷阱并涨水，6 个命令。该阶段变化 48/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 30（CE GlobalsBrogue.c:148）**：0 / DF_SUNLIGHT。生成日光池、验证照明并踏入，1 个命令。该阶段变化 104/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 31（CE GlobalsBrogue.c:149）**：0 / DF_DARKNESS。生成黑暗区域并踏入，1 个命令。该阶段变化 104/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 32（CE GlobalsBrogue.c:150）**：STEAM_VENT / 0。等待蒸汽自然喷发、踏入并受到伤害，26 个命令。该阶段变化 44/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 34（CE GlobalsBrogue.c:154）**：DEWAR_CAUSTIC_GAS / DF_CARPET_AREA。撞碎腐蚀气储罐，释放毒气并留下碎玻璃，1 个命令。该阶段变化 76/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 35（CE GlobalsBrogue.c:155）**：DEWAR_CONFUSION_GAS / DF_CARPET_AREA。撞碎迷惑气储罐、释放迷惑气并留下碎玻璃，1 个命令。该阶段变化 76/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 36（CE GlobalsBrogue.c:156）**：DEWAR_PARALYSIS_GAS / DF_CARPET_AREA。撞碎麻痹气储罐、释放麻痹气并留下碎玻璃，1 个命令。该阶段变化 76/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 37（CE GlobalsBrogue.c:157）**：DEWAR_METHANE_GAS / DF_CARPET_AREA。撞碎甲烷储罐、释放甲烷并留下碎玻璃；另用玩家火杖引燃至爆轰，1 个命令。该阶段变化 76/104 层；单独撤掉该行 carrier 后，其行为测试失败。

**autoGen 38（CE GlobalsBrogue.c:160）**：0 / DF_LUMINESCENT_FUNGUS。D40 生成发光菌、验证光照并踏入，1 个命令。该阶段变化 0/104 层；单独撤掉该行 carrier 后，其行为测试失败。

row 38 的固定样本变化为 0，因为它的执行深度为 D40，另有 D40 实际生成与交互覆盖。墙层修复在固定 104 层中变化为 0；补充只改变 `WallDoorFinish.ts` 的 seed13/D1–16 对照，只有 D16 的 terrain 状态哈希变化，其他记录列不变，见 `wall-case-attribution.json`。相同 seed13/D16 的整机行为由失败变为成功。

火/桥补充验证：autoGen 菌林真实火杖焚烧，甲烷储罐气云真实引燃并传播至爆轰；自然 CE65 seed4/D8 的石桥可踏入/返回且受火后保留。CE65 的 `DF_CATWALK_BRIDGE` 生成的是 STONE_BRIDGE，不能将其当成可燃绳桥；原 U17b 绳桥焚毁/坠落链保持在回归范围内。

## 4. 独立守卫、浏览器与反事实

新增 `u_19f_autogen`、`u_19f_machines`、`u_19f_fire` 共 34 条行为/目录断言；数据金标准只读取 CE，不从生产表推导预期。33 个独立撤销变体全部被行为断言捕获：25 行各一个、两台重新退池、删除 bump、禁止 SPARK 充电、去掉活跃虫道载体、恢复旧墙层、恢复旧搜索、删除储罐闪光色。详见 `negative-summary.json`，每个变体均有完整失败日志，不把转换错误当有效反事实。

浏览器使用本地 Vite 和 Playwright 的实际按键，共 27 个场景（CE52、CE55、25 行）。每个阶段核对位置/HP/物品/目标地形，记录 `render_game_to_text`，页面和控制台错误为 0。实际查看了电晶开笼、虫道打开、菌林再生、藻光、网/涨水陷阱、蒸汽和储罐截图。标准 develop-web-game 客户端已运行；其 canvas 截图为黑，故视觉验收使用 headed Chromium 的全页面截图，未将黑图当作通过证据。脚本 `u19f-browser.mjs`、快照及 `browser.json` 可重放。

## 5. 旧守卫前提修订（交验收方裁决）

这里的修改是前提修订提案，不是删除行为门禁。原断言先保存红灯，再放到隔离的精确 HEAD 源码/测试/黄金表中反事实运行。不是只换导入模块：源码读取守卫也读取隔离树的 HEAD 文件。

已保存 `static-original-final`、`static-extra-original`、`alias-count-original`、`discovery-remaining` 的原失败，以及对应 `head-premises-*` 结果；四组精确 HEAD 对照分别为 15/15、11/11、4/4、39/39 通过。修订保持历史投影数量和原字段比较，新增独立 CE 字段/真实动作守卫后，仅修目录总数、新成员所属层/优先级、深水/阻挡成员、光源载体清单、已闭包条目的 wired 前提及两台机器的资格前提。

多层生成守卫仍拒绝未知组合，仍要求 GAS 空及至多三层；新增的是逐写入追踪并回查 CE 的完整四层元组，详见 `layer-writer-trace.json`。成本测量原来只把显示层当成全部通行语义，新增 STEAM_VENT 可盖住 SURFACE 的 BLOODFLOWER_STALK；seed31337/D17(11,1) 的完整状态见 `cost-probe.json`。新分类只接纳这个确证组合，未知差异继续报错。

此前 U19d/e 的固定自然种子因生成流变化需重新定位；保留相同机器族、命令驱动和原结果断言，候选动作和拒绝原因完整留存在 `prior-natural-retarget.json`。补充严格取样见 `prior-natural-strict.json` / `prior-natural-resurrection.json`，被拒绝的候选也保留；没有改弱原来的类别、机关状态、复活满血或存取档断言。

以下旧前提亦有 HEAD 反事实和具体来源核对，作为本次提案提交验收：

- C1 的房间物理地板计数改读 DUNGEON 基础层，避免新日光/黑暗液体层的显示优先级掩盖地板；房间数、均值及面积下限不变。
- 饥饿精确阈值测试换到 seed7：原 seed1 现于 turn209 被鳗鱼击败；seed7 的昏厥/饥饿/饿死链完整出现，原阈值不变。`hunger-probe.json` 保存两条真实轨迹。
- 保护武器单次命中样本固定为已有三例采用的笼中目标（防御 0）；生成 RNG 变化使原一击抽到 miss，与腐蚀行为无关。
- T9 航点读档检查保留 sentinel 覆盖和原坐标；显式重建使用原构建时网格及 RNG。CE 在 50 次环境预热前建航点，seed42 预热会踩倒两格树丛，见 `waypoint-probe.json`。
- 物品合法性承认有实际领养对象来源的闭笼奖励，但必须用完整命令取回同一实例并返回；所有普通物品仍要求零非法落点。CE47 回放路线支持正常八向移动和暗门搜索，不攻击标记祭品，从门外进入，并避开会将玩家送走的传送陷阱；seed42/D11 的偏离命令记录在 `altar-l1-probe.json`。原 p1_20 取物/唯一性/返回断言未改。
- center 的 seed25/D26 唯一新样本是正常 D26 护符人口投放，旧记录器漏掉了这条已有路径。新增对实际 `spawnAmulet` 实例的单次追踪，仍要求已清机器编号、可通行，额外直投及重复实例继续失败。
- V-2b-7 的旧 14 种子没有抽到 CE52，旧“仍退池=0”断言会偶然绿；保留它们并追加自然种子 17，改为必须实际出现，与 U19f 整机及重新退池反事实配套。

## 6. 一次基线重捕获与最终门禁

已完成且只执行一次 `u19f-recapture.mjs`。104 层完整状态与最后归因阶段逐字一致，被观察源码 SHA256 也一致，随后才写基线；脚本拒绝第二次写入。详见 [recapture.json](u-19f-evidence/recapture.json)。

- 前：`a98e48574125886d4dea353aa151c299381055a3ebeccc5e3175ec42daf4850d`
- 后：`884425ec51c04dd496b77db2c9cbae61820cf885c8ddd72708a16ebc9a32cffa`

最终复跑完成于 2026-09-26 20:27:32–21:02:50（Asia/Shanghai）。执行时固定源码、测试、数据、脚本与配置；**763 个输入文件的前后哈希完全一致，变化为 0**。最终全量结果如下：

| 门禁 | 结果 |
|---|---|
| `npm run build` | 通过，退出码 0；仅有 Vite 既有大块体积提示 |
| 独立撤销反事实 | 33/33 被行为守卫捕获 |
| 全量回归 | 195/195 文件通过；3743 passed，0 failed |
| 既有留痕项 | 8 skipped、5 todo；未新增、未用其替代行为验收 |
| `npm run test:drift` | 1/1 通过，22.68 秒 |
| `git diff --check` | 通过 |
| R∪S 覆盖 | 必跑 189 文件，实际 195 文件，缺失 0 |
| 任务书点名守卫 | p1_30、U24、U01/U03、V 全系、b2、blueprint_center、c_8、U05a、U17a–f、U25 均有执行记录，缺失 0 |

[最终机器可读汇总](u-19f-evidence/final-summary.json)、[命名守卫覆盖](u-19f-evidence/named-guard-coverage.json)、[冻结前输入](u-19f-evidence/frozen-inputs-before.json)、[冻结后输入](u-19f-evidence/frozen-inputs-after.json)、[零输入差异](u-19f-evidence/frozen-input-differences.json)、[既有跳过/TODO 明细](u-19f-evidence/existing-deferred-tests.json)。第一次发现性全测曾报错，后续只在证实旧前提后修改；它未被当作本节的最终复跑。最终复跑期间没有修改任何受冻结文件。

HEAD 仍为 `e6247714a04358be3cde9bd3329c82e091730cc4`，暂存区为空，没有提交。报告及证据文件保留在工作区。


## 7. 复现入口与证据索引

从仓库根进入 `brogue-web/`，使用现有 `node_modules` 与本地 `../BrogueCE-master`：

```sh
node scripts/u19f-ce-catalog.mjs --check
node scripts/u21c-ce-appearance.mjs --check
node scripts/u19f-audit.mjs
node scripts/u19f-final-check.mjs
```

最终检查脚本先核对唯一重捕获的基线，再冻结 `src`、`public`、`scripts` 和配置文件。顺序执行 build、33 个反事实、全测试（排除单独运行的 drift）、drift 和 diff-check；结束再次逐文件核对哈希。不要再次运行基线重捕获。

- [完整状态单变量归因](u-19f-evidence/attribution.json)、[墙层补充对照](u-19f-evidence/wall-case-attribution.json)
- [原始机器/自动生成器动作证据](u-19f-evidence/actions.json)、[反事实结果](u-19f-evidence/negative-summary.json)
- [R∪S 反查明细](u-19f-evidence/search.json)、[必跑文件清单](u-19f-evidence/tests.txt)
- [CE52 最终画面](u-19f-evidence/browser-ce52-final.png)、[CE55 最终画面](u-19f-evidence/browser-ce55-final.png)
- [冻结门禁逐项结果](u-19f-evidence/final-gates.json)、[最终逐文件结果表](u-19f-evidence/final-results.md)

浏览器复现需先启动 `npm run dev -- --host 127.0.0.1 --port 5199`，再运行 `node scripts/u19f-browser.mjs`；它从证据目录快照回放实际按键，并核对每个阶段状态。

P2 基线均保持入口哈希：

| 文件 | SHA256 |
|---|---|
| p2_2_baseline.json | `ba5cfdab37e2bdf633714ac9da1b54c5db71318967c54cffdb47da7519f51726` |
| p2_baseline.json | `f1690f3748c3e49c0df692cbee283fb962719d9b290131b9507282a6b504fc4b` |
| p2_3_baseline.json | `9b3bb0c149e968f6eccae04752e13390beaf4602c6e8216aa65426432f5ecf2e` |
