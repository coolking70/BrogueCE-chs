**U17f 执行报告 — DF 最后六项载体与 RUBBLE 自动生成**

任务书：`ai_docs/tasks/u-17f.prompt.md`；规格：X-0 §3.3、§4.3 U17、K19/K28。基点为 `254455e79bb4dc79aff171716000c02c586f65b1`。工作目录为仓库根，修改限于 `brogue-web/`。未提交、未暂存。最终门禁结果见本文末尾；当前正在等待冻结后的全量回归完成。

本轮恢复全部六项 DF，追加五种地形（原 ID 0–163 保持，新增 164–168），复用已有 `FLOOR_FLOODABLE`。补入虫道必需后继 `DF_GRANITE_CRUMBLES=192`，DF 目录共 148 项，`DF_MISSING_TILES` 清空但守卫保留。按 CE 接回自动生成表 index 7 的 `DF_RUBBLE`。另修复实际交互暴露的两个末端缺口：可通行钥匙祭坛的接触用钥匙入口、传奇盟友 horde 的结盟标志消费。

**CE 核验与实现范围。** 使用仓库内 `BrogueCE-master/src/brogue/Globals.c`、`Rogue.h`、`Time.c`、`Movement.c`、`Monsters.c`、`Architect.c` 与 `src/variants/GlobalsBrogue.c`，未以旧注释代替规格。[CE 独立解析器](../../scripts/u17f-ce-golden.mjs) 不导入 web 实现；其 [黄金表](../../src/test/fixtures/u17f-ce-catalog.json) 固定 CE 源码哈希、6 行 tile、11 行 DF 及相关蓝图起点。外观生成器扩为 169 行，原 164 行值不变；两种开裂石材的深度动态颜色也进入断言。

| DF / CE Globals.c | 载体、层 | 三链与最终消费者 |
|---|---|---|
| 155 WALL_CRACK / 818 | RAT_TRAP_WALL_CRACKING，DUNGEON；tile 行 560 | 取祭坛物品→机器广播→裂墙；立即后继 RUBBLE 不唤醒；500 概率晋升 WALL_SHATTER 后唤醒原鼠群、通路开放、落物可取 |
| 187 CRACKING_STATUE / 872 | STATUE_CRACKING，DUNGEON；tile 行 353 | 入口触发板/祭坛→开裂；3500 概率晋升 STATUE_SHATTER→RUBBLE；唤醒原休眠实体，雕像格可通行 |
| 148 COFFIN_BURSTS / 807 | COFFIN_OPEN，DUNGEON；tile 行 373 | CE11 的 TRIGGER_AREA→机器触发→开棺并唤醒吸血鬼；保留原携带钥匙，无重复实体；darkGray 闪光半径 3 |
| 191 WORM_TUNNEL_MARKER_ACTIVE / 880 | 同名地形，LIQUID；tile 行 569 | 拉杆→标记激活；按开放邻格计算 -2000 概率→GRANITE_CRUMBLES→FLOOR + TUNNELIZE/RUBBLE；复用唤醒事务 |
| 87 ALTAR_RETRACT / 724 | 已有 FLOOR_FLOODABLE，DUNGEON；tile 行 324 | 拾取祭坛物品→沉入地面、GENERIC_FLASH_LIGHT；该地板可被已有洪水扩散消费者淹没 |
| 88 PORTAL_ACTIVATE / 725 | PORTAL_LIGHT，SURFACE；tile 行 488 | 匹配钥匙接触祭坛→机器广播→先疏散占格生物、唤醒传奇盟友；真实 PORTAL_ACTIVATE_LIGHT 光源；10000 概率晋升清除光层，底层 PORTAL 保留 |

六条 DF 的起始概率/递减均为 0/0。新增 192 对照 Globals.c:881，为 `FLOOR/DUNGEON`，`SUPERPRIORITY | ACTIVATE_DORMANT_MONSTER`，后继 `DF_TUNNELIZE`。全部铺装、疏散、刷新、唤醒、消息、闪光与后继均经过现有 U17a 事务；未另写鼠群、虫群或休眠系统。地形属性、层、绘制优先级、光源、三链、中文名称与外观一并接上。

`Game.useContactKeyAt` 对照 Time.c:543–546、Movement.c:616–671，复用坐标/机器号/深度匹配及 `promoteLayersWithMechFlag`。背包、地面、占格怪物携带的匹配钥匙均可驱动链路；一次性钥匙从原所有者移除，非一次性钥匙保留。原入口只处理撞锁门/笼子，无法消费可通行的 `ALTAR_KEYHOLE`。传奇盟友入口对照 Monsters.c:879、753，领袖和成员复用 `becomeAllyWith`；自然 CE12 回放曾实证“已唤醒但敌对”，修复后同一真实 horde 的实体成为盟友。

**任务文字与 CE 的边界。** CE42 下沉祭坛本身没有洪水触发；没有增加自创淹水事件。测试先经玩家拾取恢复 `FLOOR_FLOODABLE`，再让独立、已实现的洪水机器传播到该格。CE55 的普通 underworm feature 未带 `MF_MONSTERS_DORMANT`；构造虫道中的休眠虫仅用于验证 DF192 明确声明的唤醒副作用，不声称 CE55 原生虫群休眠。CE55 的自然生成资格仍退池，因此虫道证据为构造载体上的真实玩家输入链，未伪造自然生成证据。

CE69 的既有自动生成强制建造入口不等于加权回池；本轮只补其裂雕像 payoff，原 frequency/资格不动。[范围哈希](u-17f-evidence/scope-hashes.json) 证明 BlueprintEngine、Architect、blueprints.json、hordes.json 与 HEAD 相同。AutoGenerator 的唯一数据改动为 index 7：CE GlobalsBrogue.c:121，深度 0–39、frequency 30、最大 4、FLOOR/NOTHING 基座，原数量参数均保持。U19 的整机资格工作未提前执行。

**用户触发与独立行为证据。** [u_17f_carriers.test.ts](../../src/test/u_17f_carriers.test.ts) 共 18 项，包含六条玩家动作链、原实体身份/钥匙/落物、不同机器隔离、疏散、存读档、概率严格边界、虫道封闭时零 RNG、错误钥匙/断路器，以及真实 CE69 建造→人口落位→玩家触发→唤醒。传送门测试直接消费 CE12 的真实 horde flags，没有手设目标的盟友身份。

自然回放保留生成地形、机器编号、驻留实体和物品绑定；只将玩家放到触发点邻格、提高生命并移除无关活动敌人。传送门将该层真实生成的匹配钥匙转入背包，作为触发前提；未声称完成整层寻钥匙路线。[生成起点](u-17f-evidence/natural-starts.json)、[回放状态](u-17f-evidence/natural-replay.json)、[回放日志](u-17f-evidence/natural-replay.txt) 与压缩存档可复核。

| 自然场景 | seed / 深度 | 回放结果 |
|---|---|---|
| 鼠墙 | 424242 / D7 | 17 个原居民全部退出休眠，目标墙成为 FLOOR + RUBBLE |
| 雕像 | 777 / D11 | 原休眠居民唤醒，雕像消失，地面可通行 |
| 棺木 | 777 / D23 | 原吸血鬼现身，最终 COFFIN_OPEN |
| 传奇盟友传送门 | 777 / D10 | 原传奇实体以盟友身份醒来，临时光消退，PORTAL 留存 |
| 下沉祭坛 | 424242 / D3 | 拾取后成为 FLOOR_FLOODABLE |

使用 develop-web-game 工作流完成标准浏览器客户端运行；该客户端的 headless 截图为黑画面，未将其计作视觉通过。随后使用可见 Chromium、真实方向键/拾取/等待，验证六场景的前态、激活态、末态及窄屏，共 19 张截图；浏览器 pageerror/console error 均为 0。人工查看裂墙、裂雕像、空棺、地板、虫道、真实传奇盟友光照与窄屏截图。为拍到短暂中间态，首次动作暂时固定晋升骰为上界，随后恢复 RNG 并实际等待到终态；没有手工覆盖目标终态。[浏览器记录](u-17f-evidence/browser-results.json)、[传奇盟友截图](u-17f-evidence/browser-portal-active.png)、[虫道截图](u-17f-evidence/browser-worms-after.png)、[窄屏截图](u-17f-evidence/browser-narrow.png)。窄屏沿用现有布局，地图缩得较小，本轮未改 UI 布局。

**真实缺陷注入。** [隔离脚本](../../scripts/u17f-negative.mjs) 每次只破坏一个实现点，在临时副本运行行为用例，工作树不来回切换。最终 [12 项结果](u-17f-evidence/negative-summary.json) 全部 exit 1，均由行为断言捕获：六条 DF 各自置空；DF192 置空；删除接触用钥匙入口；删除 horde 结盟消费；断开 RUBBLE autogen；删除事务唤醒回调；删除事务疏散。捕获失败数依次为 1、2、1、1、1、2、1、1、2、1、7、1（顺序以 JSON 的 name 为准）。初版疏散反向测试曾存活：占格怪物自行行动掩盖缺陷。已固定它的行动计时与速度，再注入相同缺陷即翻红；最终全套重跑全部捕获。此处没有以目录计数失败代替行为证明。

**生成流与唯一重捕获。** 修改前 `npm run test:drift` 为 1/1 通过，见 [入口日志](u-17f-evidence/drift-entry.txt)。接入后、重捕获前的旧基线确实失败，见 [旧基线漂移](u-17f-evidence/drift-before-recapture.txt)。隔离构建共 12 个累计阶段，每阶段均跑基线四个 seed × D1–D26（104 层），记录四个基线字段及分层地形、活动/休眠实体、实体图、物品、玩家和 RNG。使用最终观察器补齐休眠实体字段后重新跑完整阶段；不是只靠 `fp/n/species/items` 推断全状态不变。

- HEAD→支撑代码：0 个观察字段差异；支撑阶段仍禁用六载体、RUBBLE、接触钥匙与结盟新入口。
- 支撑代码→结盟入口：仅 3 个休眠实体哈希改变（424242/D23、777/D25、31337/D25）；四基线字段和 RNG 不变。
- 结盟→接触钥匙：生成观察值差异 0；玩家接触路径由独立行为测试覆盖。
- DF155：只接 WALL_CRACK，104 层观察值差异 0；玩家取物→裂墙→鼠群链由独立测试及自然 D7 回放证明。
- DF187：只接 CRACKING_STATUE，差异 0；独立触发及 CE69 完整建造证明后继和唤醒。
- DF148：只接 COFFIN_BURSTS，差异 0；独立 TRIGGER_AREA 与自然 CE11 回放证明原吸血鬼现身。
- DF191：只接 WORM_TUNNEL_MARKER_ACTIVE，差异 0；构造拉杆链验证负概率边界与 192→TUNNELIZE，CE55 上游资格保持关闭。
- DF87：只接 ALTAR_RETRACT，差异 0；拾取→可淹地板与洪水消费者分别有实行为证据。
- DF88：只接 PORTAL_ACTIVATE，差异 0；真实钥匙/horde 的玩家入口、疏散、盟友唤醒、光消退均有独立证据。
- 六条→RUBBLE index7：1106 个扩展观察字段差异；其中基线字段 388 个：fp 104、n 93、species 100、items 91。104 层 RNG 状态均变化。
- RUBBLE→最终实现：差异 0。

零差异只适用于这 104 层的生成观察，不代表交互不会消耗随机数。[阶段明细](u-17f-evidence/generation-stages.json)、[阶段日志](u-17f-evidence/attribution-final.txt) 与各阶段 `inputs-*.json`/`generation-*.json.gz` 保存完整输入及差异。

[重捕获脚本](../../scripts/u17f-recapture.mjs) 验证原基线哈希、104 层完整性及捕获所用源码哈希，并通过 `recapture.json` 拒绝第二次写入。实际只写一次 generation_baseline，修改上述 388 字段与 note；三个历史 P2 基线不重捕获。[写入记录](u-17f-evidence/recapture.json)。

| 基线 | SHA-256 修改前 | SHA-256 修改后 |
|---|---|---|
| generation_baseline.json | `66dbe4353f22e033152d0166d88e5024fc1ccd0605e2afe3e134e0a6a860412a` | `9bb429cbf6164c69a23cb1463bde563c520be74d2a78c98385afa1c2dd322cbf` |
| p2_baseline.json | `f1690f3748c3e49c0df692cbee283fb962719d9b290131b9507282a6b504fc4b` | 同左 |
| p2_2_baseline.json | `ba5cfdab37e2bdf633714ac9da1b54c5db71318967c54cffdb47da7519f51726` | 同左 |
| p2_3_baseline.json | `9b3bb0c149e968f6eccae04752e13390beaf4602c6e8216aa65426432f5ecf2e` | 同左 |

**旧守卫的前提修正，交验收方复核。** 未删除行为守卫、跳过失败用例或扩大数值容差。对原断言先保存当前失败，再在隔离目录恢复精确 HEAD 的全部已改源码与旧测试，证明原断言通过，然后修改其过期前提。[逐断言清单](u-17f-evidence/premise-review.json) 共 36 项；对应原始失败及 `head-premises-*.json` 可追溯。旧版全量探索是 182 文件、3565 通过、9 失败、8 skipped、5 todo；这是定位记录，不作最终门禁。

| 前提 | 处理与保留的约束 |
|---|---|
| 164 地形、旧层/优先级/光源全表 | 按 CE 追加 5 行到 169；新光源精确加入，历史投影不变 |
| 6 个缺 tile、旧 135/139/140 DF 投影 | 缺口精确断言为空；历史目录投影只排除新增 192，闭包全等守卫仍在 |
| 老硬编码可走集合 | 仅将新增两种阻挡地形移出历史论域；新增 CE 真值与玩家行为独立覆盖 |
| autogen wired 集/运行统计、负晋升概率集 | 精确加入 RUBBLE index7、活虫道 -2000，其他成员保持 |
| U17e 自然图书馆 seed777 D2 | 原自然样本移至 D1；借出、离开关笼、归还再开等原行为断言完整保留 |
| B1 伤害端点依赖生成 RNG 位置 | 仅在那条突进用例的建场后以原 5 seed 重播攻击 RNG；自动命中、9/12/15 值域、9 与 15 两端点仍检查 |
| B1a 随机剑/新局鉴定集合 | 显式构造无符文的 +2 剑；新局重置用例使用无自然图书馆的 test 模式，身份隔离与精确集合断言不变 |
| P2-4 空房节奏用例 | test 模式排除自然有线图书馆。原场景即使清掉地面物品，机号仍在，首步会关笼并中止自动路径；两步、同步完成、无暂停/输入锁断言保留 |
| blueprint_center 空锚点与样本覆盖 | 增加 seed35 D6 的 CE39 声明 origin 样本（[真实区域/feature/钥匙绑定三项核验](u-17f-evidence/true-origin-proof.json)）。另核实 seed1 D12 的 CE8 空锚点恰好与子机合法领养护甲落点重合；只在原实例 ID、子机成功 adoption feature、地面产物三证相符时认可。伪造 ID、错误 feature、非领养产物均有负例；按生成计划 instanceId 去重，缺失 ID 不获认可；CE8 重合不计入真实 BUILD_AT_ORIGIN 非空转样本，同一声明只消费一次，重复/额外直投和不可通行仍失败 |

反事实批次 `static-discovery / extra-static / static-more-exact / static-more / late-catalog / late-natural / late-terrain / late-gameplay / late-rubble-integration` 分别通过 16/8/5/1/1/1/2/63/1 项，批次间有重复，63 项是相关三个完整文件的原始顺序证明。中间 `family-recheck` 曾混入新 U17f 用例，不作为旧前提证明；有效凭据已在逐断言清单中指定。新增测试最初错误地将 legacy KEY 分类表示与 CE itemFlags 原始列逐项等同，已去除该无关表示层假设；实际钥匙行为独立覆盖。

**最终门禁与冻结声明。** [反查脚本](../../scripts/u17f-audit.mjs) 计算源码反向依赖 R 与符号/读源码扫描 S，闭包为 179 个测试文件，包含任务书点名的 p1_30、U24、U01/U03、c_4a/b/c、c_7、r_1、f/g、V 全系、W13/14、U17a–f、U16 及源码守卫。最终采用无文件筛选的并行全量 `npm test`，覆盖闭包后另跑 drift；未使用 `--fileParallelism=false`。明确清单见 [tests.txt](u-17f-evidence/tests.txt)。

第一次冻结尝试发现 C6 AD-8 的独立运行统计白名单仍少 index7，已中止并保留 [该次日志](u-17f-evidence/first-freeze-attempt/status.json)，补做 HEAD 证明和 C6 全 17 项正向验证后重新冻结；基线没有第二次写入。其后自审将 CE8 合法重合与真实区域 origin 的非空转计数严格分开，并对实例 ID 去重、拒绝缺失 ID；为保证原守卫强度，又中止两个未完成尝试（[记录二](u-17f-evidence/second-freeze-attempt/status.json)、[记录三](u-17f-evidence/third-freeze-attempt/status.json)），完成构建与负例后重新冻结。以上中断均未重捕获基线。最终完整命令为 `node scripts/u17f-final-check.mjs`，内部顺序：反查 → `npm run build` → `npm test -- --maxWorkers=6 --reporter=default --reporter=json --outputFile.json=ai_docs/reports/u-17f-evidence/regression-final.json` → `npm run test:drift`。脚本比较 src/public/scripts/config 全部输入哈希，并核对 R∪S 每个文件实际执行。

## 验收方最终门禁（执行方最终回归因 codex 认证 401 中断）

执行方冻结后的最终检查只完成反查与 build（exit 0），全量回归运行中 codex 会话因 401 Unauthorized 终止，未产出 regression-final.json。验收方将工作树三方合并至 main（含 U25/U26b/U27 等后续轮），在 Mac 本地完整复跑：185 文件中 184 通过、1 失败，3594 通过；build、test:drift 通过。

唯一失败为 `v_2b_5_dormant` D1 "休眠怪一律 TRACKING_SCENT"：777/D10 的传奇盟友经本轮 horde 结盟消费以盟友身份休眠。CE Architect.c:1657 明确仅对 `creatureState != MONSTER_ALLY` 置 TRACKING_SCENT，守卫前提漏此例外；验收方按 CE 仅对盟友跳过状态断言（isDormant/machineHome 断言保留），复跑 17/17 通过。另核 AutoGenerator index 7 = CE variants/GlobalsBrogue.c:121（DF_RUBBLE，0–39，freq 30，max 4）。

