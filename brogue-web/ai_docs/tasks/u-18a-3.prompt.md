# U18a-3 + U04 + U04b：怪物群落位、楼梯最终落位与 CE66、密门映射（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线。**

## 0. 规格

- **U18a 报告 §6** 施工清单第 5–7 步（`ai_docs/reports/u-18a.report.md`）：
  5. horde leader/follower/周期/召唤分入口接永久物种 forbiddenFlags 与层匹配（不接运行期 monsterAvoids，不提前合并 U19c 实化顺序）
  6. **U04 楼梯**：先落实最终落位合同，再墙龛候选/回退/清层；每个杠杆单独 trace；上下梯互达、D26 边界；**CE66 成功与失败种子**——
     CE66 回池**单独一个阶段**
  7. 前厅 PB、区域密门后继、机器 view 的 P|V/PB 各自独立
- **X-0 §4.2 U04/U04b 行、§3.1 K27/K29**：K27 楼梯从含 FLOOR 格抽取后才过滤、D26 不造下梯、CE66 退池；
  K29 SECRET_DOOR 不在 BlueprintEngine 地形映射，CE17/F0、27/F1 密门 feature 被跳过
- **U02b 报告 §5**：层种子表已预抽楼梯坐标，但 web 仍用地板牌堆放梯、未接 CE `placeStairs` 最多 50 次 dig 重试——本轮一并对齐
- **U23**：密门发现已有 CE `discover()` helper `discoverSecretsAt`（DungeonFeature.ts）——密门落地后发现链复用它

📌 用户裁决：生成变化不是卡点；**每个杠杆单变量归因**，全部完成后一次重捕获（或分阶段重捕获，二选一）。

## 1. 验收

- 每阶段变化层数/字段与反事实（只回退该阶段复现上一阶段）
- 楼梯：上下梯互达、不在机器内、D26/深层边界、`placeStairs` 失败重试与 CE 同构程度
- CE66 回池：成功种子与失败种子各一，退池留痕守卫按"回池"翻正（给依据）
- 密门：CE17/F0、27/F1 实际落地、发现前后外观（U21c 目录）、寻路可解
- 连通/落位/机器可解/产物唯一（U05a）守卫不回退

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**反查闭包含 p1_30、U24、U01/U03 合同守卫与所有读源码守卫（含 C-4 白名单）**。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-18a-3.report.md`。
