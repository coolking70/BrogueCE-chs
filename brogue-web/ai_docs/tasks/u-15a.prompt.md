# U15a：碎墙卷轴不碎不可破坏格

> **本地执行（`codex exec --worktree`）。X-0 路线第一批玩法修复之一。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.3 U15a 行、§3.1 K20 是权威规格**（`153dbb5`）：

> **U15a 不可碎墙**；K20｜前置 无｜生成流：间；在场：否｜R: G、Grid、BlueprintEngine；
> S: crystalizeFromPlayer、impregnableCells、IMPREGNABLE；scroll_effects、b_3、V 相关｜
> 原"所有墙"期望可能红；**不可破格保留全部层/标记，普通格仍碎，集合往返后同效**

K20：`crystalizeFromPlayer` 没有 IMPREGNABLE 门，直接把阻挡层变 FORCEFIELD（Game 约 :5571–5586）；
CE `Items.c` 约 :4904 `crystalize()` 在半径判定的同一条件里就排除 `IMPREGNABLE`。

## 1. 线索（**全部请自行核实**，读到执行路径）

- CE `crystalize` 整个函数：半径判定、IMPREGNABLE 门、哪一层被改（`layers[DUNGEON]`）、判据是
  `T_OBSTRUCTS_PASSABILITY | T_OBSTRUCTS_VISION`、DF_SHATTERING_SPELL、格内 `MONST_ATTACKABLE_THRU_WALLS`
  的怪被致死、以及函数末尾（请读完）的其他动作（视野/寻路刷新等）
- W-13 已建 `Grid.impregnableCells` / `isImpregnable`（`59f462f`），W-13/W-14 报告登记了"碎墙卷轴 IMPREGNABLE
  接线欠账"——**前轮留痕正是为本轮预约的**
- web 现有 crystalize 与 CE 的**其他差异**（若有），在本轮顺手对齐还是登记，请按"属于同一函数的执行路径"判断，
  超出该函数的登记即可

## 2. 验收标准

- 不可破格（机关墙、地图边界等——**列出 web 里哪些格会被标 IMPREGNABLE，及其来源**）读卷轴后：
  全部层、标记、外观**完全不变**
- 普通阻挡格照常变 FORCEFIELD，DF 与致死行为不变
- impregnable 集合**存读档往返**后同效（U01 合同下验证）
- 真实入口：从背包读卷轴（`readItem` 类入口），不只调内部函数

## 3. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- ❌ 不改其他卷轴、不改 IMPREGNABLE 的赋值来源
- 📌 用户裁决：不做旧存档兼容

## 4. 教训（已连续三十八轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**注释写的是意图，执行路径里可能有例外**；**前轮的留痕登记可能正是为你预约的**；
**守卫靠旧 bug 才绿时**，先证明它依赖的是 bug，再**只扩观测、不动阈值**，并交验收方裁决（U05a T11 先例）。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证。）

## 7. 报告

写到 `ai_docs/reports/u-15a.report.md`：CE 执行路径全文核实；IMPREGNABLE 来源清单；实现；
真实入口验证与存读档；§1 线索结论；反查与两份清单；撞红处置；§3 自检 + 最终复跑声明；清单外改动与新发现登记。
