# C-0 补做：会话异常中断，代码在但报告与实测缺失

**上一轮你的会话异常结束了**（zcode 退出码 1，JSON 输出为空），但**代码已经落盘且质量看起来没问题**：`Architect.ts`/`Game.ts` 已改，新增 `Map/LoopMap.ts` 与 `test/c_0_add_loops.test.ts`，类型检查通过，全量 528 passed。

缺的是三样：**报告、任务书第 4 条要求的三项旁证实测、以及两条需要更新的断言**。本轮只补这些，**不要重写已有实现**（除非你自己复核后发现确有错误，那就如实报告）。

## 一、验收方已经替你测了一部分，先看数据

我在真实生成的关卡上测了三项，5 种子 × D1-D26：

| 指标 | C-0 前 | C-0 后 |
|---|---|---|
| 割点比例（4 连通抽样） | 6.3% | 5.5% |
| 深水每层均 | 9.5 格 | **12.7 格（+34%）** |
| `IN_LOOP` 点亮比例 | 无此概念 | 5.9% / 0% / 17.1% / 12.3% / 30.5% |

**深水回升 34% 是环路真实生效的强信号**（湖泊闸门能接受更多落点了）。
但有两处我判断不了，需要你补：

1. **割点只降了 13%，偏少。** 而且 C-0 前后的地图完全不同（`shuffleList` 移动了 RNG），我是在比较两批不同的地图，噪声很大。**你能从内部做同图对照**：同一张 grid，跑与不跑 `addLoops`，量同一指标。请这么测。
2. **seed 777 的 `IN_LOOP` 是 0%。** 一整层没有任何环上格子，可能合理（那层确实没成环），也可能是 `checkLoopiness` 剥离过度。请查明并说明。

## 二、必须补的实测（报告里逐项给数）

1. **每层实际开了几扇环路门**（`addLoops` 把多少个格子从 0 改成 2）。这是最直接的证据，我从外部测不到。给出 5 种子 × D1-D26 的分布（均值/最小/最大）。
2. **同图对照**：同一张 grid 跑与不跑 `addLoops`，量割点比例的变化。这才是干净的对照。
3. **`IN_LOOP` 点亮比例**，并解释 seed 777 的 0%。
4. **深水回升**：确认我测的 9.5 → 12.7（你自己复跑一遍，口径以你为准）。
5. **P1-33 的坏层**：`p1_29_lake_connectivity.test.ts` 的坏层集合从
   `777/D15、999/D12` 变成了 `20260916/D23、1/D12`——**仍是 2 层但换了层**。
   这符合"RNG 流移动导致所有地图改变"的预期，不是回归。请复核并说明。

## 三、必须更新的两条断言

1. `src/test/p1_29_lake_connectivity.test.ts` 的 `KNOWN_MACHINE_STAGE_BAD_LEVELS`
   → 更新为实测的新坏层集合，并在注释里写明"因 C-0 移动 RNG 流而换层，非回归"。
2. `src/test/p1_26_invariants.test.ts` 的 `KNOWN_UNREACHABLE_STAIRS_LEVELS`
   → 按你的实测更新（若 5 种子集合内的坏层数变了）。

**`generation_baseline.json` 仍然不许刷新**——那条红是预期内的，由验收方授权重捕获。

## 四、报告

写入 `ai_docs/c_0_add_loops_report.md`：改动清单、CE 行号对照、`checkLoopiness` 的实现说明、**上面第二节五项实测数据**、对抗性测试与反向验证（若上一轮已写在测试文件里，把真实失败输出补进报告即可；若没做，本轮补做）、`generation_baseline` 变红情况。

## 边界

与原任务书相同。另加：**不要重写 `LoopMap.ts` / `Architect.ts` 的既有实现**，除非复核发现真错误——本轮目的是补齐证据链，不是重做。

门禁：`npm test` 除 `generation_baseline` 外全绿、`npm run build` 绿，真实输出尾部贴进报告。
