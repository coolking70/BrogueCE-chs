# U22：发现屏、物品详情口径与帮助

> **本地执行（Windows 轨）。X-0 路线。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.4 U22 行、§3.1 K12/K26 是权威规格**（`153dbb5`）：

> **U22 发现屏/详情/帮助**；K12/K26｜前置 U15 各物品真实模型，W 后缀可先接｜生成流：无｜
> R: Input、InventoryOverlay/DetailPanel/新屏、DetailGenerator、G；S: discovered、magicCharDiscoverySuffix、
> maxChargesKnown、runicKnown、actualEnchantment｜w_2/7/24/25/26、DetailGenerator、键绑定；
> **未知身份不泄漏，极性/绰号/资源口径一处定义；D 键与帮助真实入口**；浏览器验收

K12：完整发现屏和 D 入口不存在；只有 `magicCharDiscoverySuffix` 与背包极性（Input.ts 约 :27–126，对 CE IO.c 约 :4372–4393）。

## 1. 范围

1. **发现屏**：CE `printDiscoveriesScreen`（Items.c）——按类别（食物/药水/卷轴/法杖/魔杖/戒指/护符/武器/护甲……按 CE 实际列）
   列出全部种类；已鉴定显示真名与频率/描述、未鉴定显示外观名、绰号（called）显示、**极性后缀**
   （`magicCharDiscoverySuffix`，W-2/W-24/W-25 已接）；已知/未知计数。**CE 的排序、分组、颜色规则按原代码**
2. **入口**：CE 的 D 键（及帮助里列出的键）；web 键位表（p1_46 守卫）只回归、新增 D 键按 CE
3. **帮助屏**：CE 的命令帮助（按键列表）——web 已有的键位逐项对照，缺的键**只显示 web 实际支持的**，不要列出没实现的命令
4. **物品详情口径统一**（K26 局部）：发现屏与背包详情对"未知/已知/极性已揭示/容量已知"用**同一套判断**，
   不泄漏未鉴定信息
5. ❌ 不改物品模型/鉴定规则；U15 各物品的效果文案若与实际不符，**登记**（U24 处理），不在本轮改文案语义

## 2. 验收

- 新局 / 鉴定若干后 / 检测魔法后 / 起绰号后，发现屏内容与 CE 规则一致；未知项不显示真名或资源数值
- D 键与帮助屏的真实入口（浏览器）
- 中英文；窄屏布局

## 3. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- ❌ 不改鉴定/极性/外观逻辑（只读）
- 📌 用户裁决：不做旧存档兼容；保留现状不是卡点

## 4. 教训

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
📌 **UI 不能比游戏知道得更多——未鉴定的东西不能从任何界面泄漏**。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明「这是最终状态下的运行结果，不是中途快照」。

## 7. 报告

写到 `ai_docs/reports/u-22.report.md`：CE 发现屏/帮助的执行路径（分组、排序、颜色、计数）；web 实现；
信息不泄漏证明；入口与键位；浏览器截图；反查与两份清单；撞红处置；§3 自检 + 最终复跑声明；登记给 U24 的文案问题。
