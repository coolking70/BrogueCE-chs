# U19e：置换/复活/献祭/闭笼祭坛机器族——回池与整机可解（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U19 第五个子轮（U19a–d 已合并；U17e 已闭合这些族的 DF 载体与消费者）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U19 行与其后子轮表（约 :197、:203）、§3.1 K28/K32 是权威规格**（`153dbb5`）：

> **U19 蓝图剩余算法/机器玩法**；K28/K32｜前置 U05/U17（已合并）、视图需 U18a（已合并）｜生成流：**直**｜
> R: BlueprintEngine/Architect/AutoGenerator、G、数据；S: IN_VIEW_OF_ORIGIN、pendingItems、pendingMonsters…｜
> **按子轮分别验，先闭包后解退池；每台必须有"用户触发→结果"的证据，不能只点亮身份覆盖**
> **U19e** 置换/复活/献祭/闭笼祭坛（每类独立，复活走 U16）

## 1. 范围（**全部请自行核实 CE**；四类逐类独立归因）

- **置换**（CE6 等）、**复活**（CE7，复用 U16）、**献祭**（CE47 系/献祭笼）、**闭笼祭坛**（CE47 领养资格排除）四类机器：
  逐台核对 CE 选池方式（frequency/资格/领养/autoGen）与 web 一致；有退池或资格排除且理由已被 U17e 闭合的 → 按 CE 恢复
- **领养落点检查（web 自创）**：CE `Architect.c:1495–1533` 直接 `placeItemAt`，没有"领养落点被阻挡即拒绝"的检查；web 的该检查拦下了 CE28/47/52，
  U19d 仅对 CE28 加了窄例外 `isThrowingTutorialReward`。本轮核实这个检查的来历与作用：若它只是替 CE 未实现的机关兜底，则在机关已闭合后**按 CE 移除或收敛为通用规则**，
  并删去 CE28 特例（不再逐台加例外）；若确有 CE 依据则写明
- 每台回池机器：自然生成出现、**玩家从入口到奖励的完整操作链**（置换两件物品、复活盟友、诱标记怪上祭坛、闭笼取物归还）证据
- **不在本轮**：CE52 电晶、CE55 虫道（U19f）

## 2. 生成流

先 drift 记录 → 单变量归因（每族回池/修正各一段）→ 独立守卫（每台整机可解；去掉修复即红）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**反查闭包含 p1_30、U24、U01/U03、V 全系、b2_transcription、blueprint_center、c_8、U05a、U17a–f、U25 观测守卫 与所有读源码守卫**；
可用 U25 的 `scripts/u25-machine-inventory.mjs` 追踪机器产物。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-19e.report.md`。
