# V-2b-7 DF 特征系统轮

## 0. 先读：勘察报告对本轮的描述**已经过期**

V-2b-0 §0 把本轮定为「DF 特征系统」，理由是「web `FeatureDef` 连 df 列都没有，
约 24 条蓝图连逐字表达都做不到」。**验收方实测，这条已经不成立**：

| 勘察报告说 | 实际（验收方查证） |
|---|---|
| `FeatureDef` 没有 df 列 | **已有 `featureDF?: string`**，且引擎在 `BlueprintEngine.ts:1265` 消费（CE `:1434-1440` 直译）——**V-2b-6 顺带做的**，数据里已有 2 个载体 |
| DF 系统缺失 | DF 目录 **57 条**、闭包完整（`c_4b` E2/E3 钉着），`spawnDungeonFeature` 早在 C-4b 就有 |

**所以本轮不用建机制，是扩数据 + 补一件欠账。** 别去重写 `featureDF` 分支。

---

## 1. 四件事

### 1.1 DF 目录扩充（8 条）

13 条目标蓝图实际引用、web 目录里没有的：

`DF_GLYPH_CIRCLE`(3 条用) · `DF_TRIGGER_AREA`(2) · `DF_DEAD_FOLIAGE` ·
`DF_SMALL_DEAD_GRASS` · `DF_SURROUND_WOODEN_BARRICADE` · `DF_SWAMP` ·
`DF_VOMIT` · `DF_WORM_TUNNEL_MARKER_DORMANT`

⚠️ **闭包**：`c_4b` E2/E3 钉死「无悬空 `subsequentDF` 引用」。新条目若链到别的
DF，链上的也要补齐——**补出来的条数可能多于 8**，这是正常的，在报告里说明链展开。

### 1.2 地形载体（约 17 个）

`COFFIN_CLOSED`(2) · `ALTAR_KEYHOLE` · `ALTAR_SWITCH_RETRACTING` · `BRAZIER` ·
`DEAD_GRASS` · `DEMONIC_STATUE` · `FLAMETHROWER_HIDDEN` · `GAS_TRAP_POISON_HIDDEN` ·
`KEY_CAGE` · `KEY_PORTAL` · `MANACLE_L` · `MANACLE_T` · `PORTAL` ·
`SACRIFICE_ALTAR_DORMANT` · `SACRIFICE_CAGE_DORMANT` · `VOMIT` · `POTION_LICHEN`（物品种类，先核 web 有没有）

**`ALTAR_INERT` 不是缺口**：V-2b-6 已裁决它就是 web 的通用 `ALTAR`（CE `Globals.c:362`
是通用祭坛本体，没有「独立的普通祭坛 tile」）。本轮 6 处引用**一律写 `"ALTAR"`**，
不要新建枚举。

`KEY_CAGE` 若 V-2b-6 已落（10 号 Kennel 用过），核一下别重复。

### 1.3 十三条蓝图

9 · 11 · 12 · 30 · 33 · 42 · 45 · 46 · 47 · 49 · 53 · 55 · 57（CE 目录序）

### 1.4 补欠账：`MachineResult` 暴露 feature 落点

`MachineResult` 现在只有 `itemSpawns` / `monsterSpawns`，**没有地形类 feature 的落点**。
后果是 `p1_37` AD3 的一条逐格断言从 V-2b-3 起就**空转**（登记在案）：它想验证
「每个带 machineNumber 但不在 interior 里的格都对应一次 feature 落位」，但 CE
`Architect.c:1484-1486` 对**一切** feature 打标记，而地形类 feature 在 web 没有落点记录。

本轮把落点暴露出来，并**恢复那条断言的力量**（不是让它继续空转、也不是删掉）。

---

## 2. 两个高风险点

### 2.1 11 号 Vampire lair 会激活钥匙可解性的一整条路径

V-2b-6 把 `MF_MONSTER_TAKE_ITEM`（怪物携带物品）接线了，但**当时池里没有任何载体**，
所以它在可解性证明里被记为「结构性不可能死局」。**11 号是 CE 里唯一带这个旗标的蓝图**
——落了它，这条路径就活了：吸血鬼携带钥匙，若它死在玩家够不着的地方就是死局。

**本轮硬要求**：复跑 V-2b-6 的 **F 组可解性证明**（`v_2b_6_keys.test.ts`），
并**补上怪物携带形态那一半**：携钥匙的怪可达、且它的活动范围不会让钥匙落进
熔岩/深渊/封闭机器区。**若确实可能死局，如实报告并给复现 seed，不要硬把用例写绿。**

### 2.2 47 号 Sacrifice altar 依赖 web 没有的机制

CE 的献祭链用 `MB_MARKED_FOR_SACRIFICE`（V-2b-5 报告已登记「web 无该机制，跳过」）。
落 47 号时**不要造替身** ——把缺口登记清楚，数据该带的旗标照带（引擎零消费是可以的，
`MF_KEY_DISPOSABLE` 在 V-2a 有先例），但要在报告里写明「47 号当前不可通关/退化成什么」。

---

## 3. 基线重捕获（最后一步）

成因至少三股：①十三条蓝图入池；②新地形/DF 落地；③`MachineResult` 扩字段
（若它不改变生成流，应当为 0——**若不为 0，那是实现误伤，停下来查**）。
沿用受控单变量。**分不开就说分不开。**

---

## 4. 授权改动清单

**引擎与数据**：`src/engine/Generator/BlueprintEngine.ts`、`src/engine/Map/Grid.ts`、
`src/engine/Map/TerrainCatalog.ts`、`src/engine/Map/DungeonFeatureCatalog.ts`、
`src/engine/Core/Game.ts`、`src/data/blueprints.json`、`src/data/arcana.json`（若需新物品）

**测试与固件**：`src/test/fixtures/generation_baseline.json`（仅最后一步）、
新建 `src/test/v_2b_7_features.test.ts`、
**穷举表类**（加地形/DF 必撞）：`c_4a_terrain_catalog`、`c_4a_0_layer_model`、
`c_4b_dungeon_feature`、`c_7_lighting`、`r_1_appearance`、`g_2_gas_df_wiring`、
`p1_42_secret_door_search`、`invented_content_pool`、`p1_30_i18n_gate`（新物品名必须补键）、
**机器/蓝图相关**：`blueprint_center`、`p1_33_machine_chokepoint`、
**`p1_37_machine_flag_i18n`（§1.4 的欠账在这里）**、`p1_20_item_placement`、
`p1_31_35_placement_snapshot`、`b_4b_item_placement`、`v_1a_blueprint_items`、
`v_1b_alternative`、`v_1c_machine_structure`、`v_2a_vestibule_return`、
`v_2b_2a_placement_flags`、`v_2b_2b_blueprints`、`v_2b_3_wired`、`v_2b_4_altars`、
`v_2b_5_dormant`、**`v_2b_6_keys`（§2.1 的复跑在这里）**、`c_8_connectivity`、`p1_26_invariants`

清单外改动必须申报。`BrogueCE-master/` 只读。**守卫顺延不放宽**。
**行为断言**撞断 > 5 个停下来说明（穷举表连带不计入）。

---

## 5. 本轮不做

- `MB_MARKED_FOR_SACRIFICE` 的机制实现（登记即可，见 §2.2）
- 13 条之外的任何 CE 蓝图
- 重写 `featureDF` 分支（V-2b-6 已做）
- 把 `ALTAR_INERT` 独立成 TerrainType（V-2b-6 已裁决）

---

## 6. 授权反驳 + 对抗性要求

事实判断若与 CE 不符，**驳回并纠正**并给行号。前几轮你抓出验收方多处错误
（CE 护符房不是抽签建成、`ALTAR_INERT` 不该独立、`>= -10` 含等号），继续这样做。
**本任务书 §0 就是因为你们上几轮的工作而重写的** ——勘察报告的判断会过期，
发现哪条过期了就说。

**对抗性要求（沿用两条）**：
① 「疑似断言过期」的诊断必须回答「如果结论反过来是实现错，我会看到什么现象？
我确认过没看到吗？」
② 每条新断言回答「这条用例在什么实现缺陷下会翻红？」——**§1.4 恢复的那条断言
尤其要答**：它空转了四轮，恢复后必须能真正抓住「feature 落点没被标记进机器」。

---

## 7. 门禁与报告

```
npx vitest run
npm run build
```

并行跑，不加 `--fileParallelism=false`；失败清单完整输出，不要 `| tail`。
加完地形/DF 立刻 build。全量墙钟 9-26 分钟（视负载）。

报告写到 `ai_docs/reports/v-2b-7.report.md`，含：

1. DF 目录扩充的条数与**闭包链展开**说明
2. 地形载体逐个的落地/别名判定
3. 十三条蓝图的落地情况（含 47 号的退化说明）
4. **§1.4 那条空转断言恢复后的形态，以及它现在能抓什么**
5. **§2.1 可解性证明的复跑结果（含怪物携带形态那一半）**
6. §3 三股基线成因的分离
7. 撞断的守卫清单与处置
8. §6 两条对抗性要求的回答
9. 清单外改动申报
10. 门禁两条命令的完整输出结尾
