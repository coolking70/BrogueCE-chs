# U18a-2：生成侧地形判据（U18a 施工清单 1–4 步）

> **本地执行（Mac 轨）。X-0 路线。⚠️ 移动生成流。**

## 0. 规格

**U18a 报告 §6「U18a-2 精确施工清单」**（`ai_docs/reports/u-18a.report.md`）是本轮施工图，**逐步单变量**：

1. `entryQualifiesForPlacement` 由旧枚举改全层 PB（入场落点）
2. `findQualifyingPathLocNear` 路径改 DL、D/O 区分、CE 首格早退/末回退
3. Dijkstra `-2` 对角阻挡与正哨兵两种输入分别验证，追所有 batchScan 读方（U07 已登记旧共享扫描器的问题）
4. `ItemSpawnHeatMap` 的 secretPass 换发现后继；普通热图与钥匙/护符 floorTiles 旁路分批

第 5–7 步（horde forbiddenFlags、U04 楼梯、前厅/密门/机器 view）留给后续轮。

📌 用户裁决：生成变化不是卡点；**但每一步都要先 drift 记录 → 单变量归因 → 独立守卫，全部四步完成后再一次重捕获**
（或每步单独重捕获，二选一，报告说明）。

## 1. 验收

- 每步：变化层数/字段 + 为什么只来自这一步（反事实：只回退这一步即复现上一阶段）
- U18a 清单每步列出的"必须先记录/保留的守卫"全部回归
- 连通/落位/机器可解/产物唯一（U05a）守卫不回退

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**反查闭包含 p1_30、U24、U01/U03 字段合同守卫与所有读源码守卫**。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-18a-2.report.md`。
