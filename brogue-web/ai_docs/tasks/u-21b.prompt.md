# U21b：光照 CE 口径纠正（CE_DCOLS=79、矿灯、黑暗状态消费）

> **本地执行（Windows 轨）。X-0 路线。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U21b 行、§3.1 K40/K16 是权威规格**（`153dbb5`）：

> **U21b 光照 CE 口径纠正**；K40 与 K16 逻辑光｜U14 黑暗互相共定合同｜生成流：间（含生成后光刷新）｜
> R: LightCatalog/LightMap、G、TerrainCatalog；S: CE_DCOLS、minersLight、lightMultiplier、darkness、inWater、intrinsicLight、flare｜
> **c_7 黄金值预计红；先当前 CE 79 宽固定公式对照**，再状态/戒指/生物光

K40：`LightCatalog.ts:32` 写 `CE_DCOLS = 64`，矿灯基半径（:310）用它；当前 CE `Rogue.h:127/168/174` 得 79
（COLS 100 − STAT_BAR_WIDTH 20 − 1）。C-7 报告的"64 黄金值"与本树 CE 不符，**不能沿用旧黄金值**。

📌 用户裁决：生成/可见范围变化不是卡点。

## 1. 范围

1. `CE_DCOLS` 与所有依赖它的光半径（矿灯、爆炸、传送光……）按 79 重算；**编译 CE 原函数取新黄金值**
   （`updateMinersLightRadius` 等）替换 c_7 旧黄金值——这是本轮授权的改义，逐条给 CE 依据
2. 矿灯公式的其他参数：`lightMultiplier`、`STATUS_DARKNESS`（U14a 已建载体，其报告 §5 写了本轮要接的内容）、
   `inWater`、戒指光明（light ring，若 web 目录有）——按 CE `updateMinersLightRadius` 逐项
3. 黑暗状态的光照消费：施加/衰减/到期/消魔/治愈后矿灯与 FOV 立即刷新（CE 在哪些点调用 `updateMinersLightRadius`+`updateVision`）；
   潜行光照（`calculateStealthRange` 等读光的地方）
4. 生物固有光/flare（K16）：能忠实接的接，否则登记

## 2. 验收

- 新黄金值：CE 编译函数 vs web，多组参数（深度、黑暗值、在水中、戒指）
- 黑暗药水饮用 → 视野变小 → 到期恢复（真实入口，浏览器截图）
- 如影响交互 RNG/生成后光刷新导致 drift：先归因再更新基线

## 3. 硬约束与教训

- 📌 **任务书与 CE 冲突时以 CE 为准并写明**；**公式有定点/舍入细节时编译 CE 原函数取黄金值**；
  **撞上守卫时改代码，不改守卫、不绕扫描器**（c_7 黄金值是本轮明确授权改义的例外，须逐条 CE 依据）；
  **反查闭包含 p1_30、U24 硬串守卫与所有读源码守卫**；Windows 注意不产生 CRLF

## 4. 门禁与报告

R∪S 自查 + `npm run build` + `npm run test:drift`；最终复跑声明。报告写到 `ai_docs/reports/u-21b.report.md`：
CE 光照函数路径；新旧黄金值对照；黑暗消费链；登记项；反查与两份清单；撞红处置。
