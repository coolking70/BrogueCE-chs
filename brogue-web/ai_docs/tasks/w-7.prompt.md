# W-7：附魔法器入口

> **本地执行（`codex exec --worktree`）。W 链第七轮，机制前置的最后一轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-7 行是权威规格**（`823eb0e`）：

> **W-7 附魔法器入口**｜可选包内杖/魔杖；**STAFF 升 E/次数/充能进度，
> WAND 加 range 下限次数**；**本轮不重写武器/护甲附魔规则**｜
> 依赖 W-2、W-5、W-6｜生成流：**否**｜
> 改动面 G、I、`InventoryOverlay.vue`、所需文案 → R(实际文件)+S(U,Q,G) 并搜 enchant｜
> 撞红预期：`invented_content_pool` 的装备附魔送符文路径、B-1 识别/保存、
> 取消/物品引用稳定性；**附魔后公式读取留后续效果轮验证**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 前两轮的交接（本轮是 W-5 / W-6 的汇合点）

- **W-5**（`60a89ee`）：`Items/ArcanaInstance.ts` 把 **E 与当前次数分离**，
  并有容量字段。**本轮是第一个会改 E 的入口**——E 升了，容量与当前次数怎么跟，
  回 CE 查实（`Items.c` 的附魔分支），别凭直觉。
- **W-6**（`1219bad`）：`Items/ArcanaRecharge.ts` 的回电周期是 `5000 / E`，
  计时存在独立字段 `staffRechargeRemaining`。W-6 报告明确写了两件留给本轮的事：
  - "**W-7 的主动附魔入口/进度调整尚未实现**"
  - CE 的**过长倒计时归一化循环**"可在极高 E 或 **E 改变后**生效"
  ⇒ **E 一变，周期 D 就变**。附魔后的计时怎么处理（重置？按比例？保留？）
  **按 CE 原码**，并给出与 W-6 归一化循环的交互证据。

## 2. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ **不增种类、不改 frequency**（W-24~26）
- ❌ **不实现任何效果行为**（W-8~W-23）——"**附魔后公式读取留后续效果轮验证**"
- ❌ **不重写武器/护甲的附魔规则**（W-0 点名）——只加法器这一支
- ⚠️ `invented_content_pool` 里有**装备附魔 20% 送符文**那条路径，它测的是
  自创符文不出现——**本轮的附魔入口别意外改到那条路径的随机流**

## 3. 教训（已连续十四轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**。

## 4. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定，按 W-0 提示**搜 `enchant`**。
W-5 / W-6 留下的 `scripts/w5-test-scope.mjs` / `w6-test-scope.mjs` 可改造复用。
⚠️ 本轮改 `InventoryOverlay.vue`——**UI 层无 import 关系，R 抓不到**，用 S 补。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 5. 门禁

按 §4 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 6. 授权反驳

W-0~W-6 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 7. 报告

写到 `ai_docs/reports/w-7.report.md`，含：

1. STAFF 升 E/次数/充能进度、WAND 加次数的 CE 依据与落地
2. **E 改变后容量、当前次数、回电计时各自怎么变**（W-5/W-6 交接），附 CE 行号
3. **与 W-6 归一化循环的交互证据**
4. **§2 边界证明**：未改武器/护甲附魔规则、未动 `invented_content_pool` 那条送符文路径的随机流
5. 选择/取消/物品引用稳定性的处理
6. §4 反查过程与实际跑的文件（含 UI 层怎么补的）
7. 撞红清单与处置
8. §2 自检 + **§5 最终复跑声明**
9. 清单外改动申报
