# U07：怪物专用瞬移（BLINKING）调度

> **本地执行（`codex exec --worktree`）。X-0 路线；W-22 学习的前置之一。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.3 U07 行、§3.1 K06 是权威规格**（`153dbb5`）：

> **U07 怪物 blink 专调度**（W-22 前置一）；K06｜前置 **U18a 的怪物位置合同**；不依赖学习｜生成流：间｜
> R: M、G、Bolt、SafetyMap/Pathfinding；S: blinkToPreferenceMap、specificallyValidBoltTarget、BLINK、40；p4_9、W-11/12｜
> **玩家 blink 守卫应保持，怪物旧拒绝路径翻正**；CE 偏好图/周界目标/收益门/障碍/盟友敌人场景，
> 失败不耗行动/资源的具体规则回源确认

K06：怪物 blink 被 `specificallyValidBoltTarget` 约 :117 拒绝，没有偏好图 + 40 周界瞄准调度；
CE `Monsters.c` 约 :2299–2370 `monsterBlinkToPreferenceMap`。**玩家 blink 与安置原语不能替代。**

## 1. 可用的前轮成果（请核实后复用）

- **U18a**（本轮前刚合并）：`terrainBlocksMovement`、`genericPathCost`、`safetyTerrainCosts` 等判据接口；
  其报告 §6 明确写了给 U07 的接口与"调用者必须另做"的清单——**按那张表做**
- **W-12**：玩家 blink 的轨迹/落点/距离公式；**W-11**：`CreaturePlacement` 安置原语；**W-3/W-4**：弹道与反射
- W-22 报告 §1、§4.2 描述了 CE blink 的完整调用点（逃险、逃敌、追敌、追领袖）——**线索，自行核实**

## 2. 线索（**全部请自行核实**，读到执行路径）

- `monsterBlinkToPreferenceMap(monst, preferenceMap, blinkUphill)`：四邻格比较、**40 个周界瞄准点**、冲击位置、
  收益门（跳过去是否真的更好）、实际 `zap`；失败时是否消耗行动
- 所有调用点：`monstersTurn`/`moveAlly`/逃跑/追击等——**每个调用点用的是哪张偏好图**（安全图、敌人距离图、气味图……）、
  `blinkUphill` 取值、前置条件（`MA_` 能力、`BOLT_BLINKING` 在 bolts 里、施法冷却）
- `monsterBlinkToSafety` 若存在，与上者的关系
- 施法者动作耗时（`attackSpeed`、`MONST_CAST_SPELLS_SLOWLY`）
- `p4_1b` 里"通用施法跳过 BLINKING"的断言：本轮**翻正**为"通用施法仍跳过，由专调度处理"——注意别让通用施法路径也开始 blink

## 3. 验收标准

- 场景：怪物逃跑时 blink 远离、追敌时 blink 靠近、盟友追领袖、障碍阻挡、落点被占、目标不可见
- 40 周界点的遍历与选择（含平局与 RNG 消耗），与 CE 同构
- 失败（无更优落点）时的行动/资源消耗按 CE
- 玩家 blink（W-12）所有守卫**只回归**

## 4. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- ❌ 不做学习（U11）、不做网/藤（U08）
- 📌 用户裁决：不做旧存档兼容

## 5. 教训（已连续四十三轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**注释写的是意图，执行路径里可能有例外**；**前轮的留痕登记可能正是为你预约的**（U18a §6、W-22 §1）；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
📌 **同一个效果对玩家和怪物是两条完全不同的决策路径**——复用执行原语，不复用决策。

## 6. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单，每条改义给 CE 依据。

## 7. 门禁

§6 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证。）

## 8. 报告

写到 `ai_docs/reports/u-07.report.md`：CE 调度全路径与每个调用点的偏好图；40 周界点与收益门；
失败消耗规则；场景验证；p4_1b 翻正依据；§2 线索结论；反查与两份清单；撞红处置；§4 自检 + 最终复跑声明；
清单外改动与新发现登记（尤其 W-22 学习恢复还差什么）。
