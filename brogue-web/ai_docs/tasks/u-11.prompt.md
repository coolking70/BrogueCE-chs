# U11：盟友尸体学习状态机（W-22 收口）

> **本地执行（`codex exec --worktree`）。X-0 路线；W-22 学习恢复的最后一步。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U11 行、§3.1 K05 是权威规格**（`153dbb5`）：

> **U11 完整盟友尸体学习状态机**；K05｜前置 **U07/U08/U09/U10 齐**｜生成流：间｜
> R: M、G、Combat、尸体/死亡载体；S: newPowerCount、absorb、removeDeadMonsters、corpse、moveAlly；W-17/19/20/21/23｜
> **资格→选能力→走尸体→倒数→安装→扣额度；被打断/尸体消失/离层/存读/消魔/复制逐项**，不再只验数值强化

**W-22 报告（`59e172c`）§2–§5 是 CE 执行路径的详细审计**，本轮以它为起点（仍须自行核实）。
四个前置均已合并：U07 `8b13602`（怪物 blink）、U08 `27203b9`（网/藤）、U09 `3d668fe`（消费者矩阵，
`syncFlagDerivedStatuses(true)` 接口）、U10（吸收字段与存档，其报告 §6 是**可直接使用的接口**）。

## 1. 核心要求

1. **触发**：CE `killCreature` 里的 `anyoneWantABite(decedent)`（Combat.c 约 :2038）——在 web 的**实际死亡链**里
   保证**每次死亡只分配一次**（W-22 §5：`deathEffectTriggered` 只覆盖 DF_ON_DEATH，不能直接当守卫；
   两个回合清扫入口不能各抽一次；区分行政删除/休眠/携带怪释放）
2. **资格与选择**：`canAbsorb` + `anyoneWantABite` 两级抽签。⚠️ **W-22 §3 用 CE 原函数探针证实：第二遍复用的是
   第一遍最后一名盟友的距离图**（CE 缺陷）——**必须忠实复刻**并用回归锁住，不能"修正"成候选数组均匀抽样；
   `ourBolts` 何时被填充也按 W-22 §3 的结论
3. **走向与吸收**：`moveAlly` 的尸体支线（前置：非中毒、非燃烧或火免）、到达后 counter=20 + 进行态、
   `updateMonsterCorpseAbsorption`（原地吸收倒数 / 离开尸体 ≤15 放弃 / 到期失效），完成时按 bolt 优先安装、
   `newPowerCount--`、调用 U09 的永久同步、完成消息（中英文，按 CE 文案）
4. **中断**：受伤（U10 已接）、换层（U10 已接只清位置）、消魔（CE 不清吸收字段，但返还额度）、复制/变形（按 CE）
5. **详情页**：U10 的进度条随状态机真实推进显示；"准备学习 N 项"文字（W-21 已接）随额度消费变化

## 2. 验收标准

- 端到端：强化盟友 → 击杀可学怪物 → 盟友走向尸体 → 吸收 20 次行动 → 学会能力 → **能力真的能用**
  （用 U07/U08/U09 的出口：学到 BLINKING 会瞬移、学到 VINES 会放藤蔓、学到 FLIES 会飞过深水等）
- 每种放弃/中断路径（中毒/燃烧不启动、被打、离开尸体 16→15、超期、换层、存读中途）
- "第二遍用最后一名盟友距离图"的 CE 缺陷：用受控夹具复现 W-22 §3 表中四种情形
- RNG：抽签只在死亡路径上发生，说明对交互 RNG 的影响
- 旧 W-21/W-22 "学习未完成"的留痕守卫按 CE 翻正（逐条给依据）

## 3. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- ❌ 不重写一般盟友 AI（U12）——只接 CE `moveAlly` 里的尸体支线及其在优先级链中的位置
- 📌 用户裁决：不做旧存档兼容

## 4. 教训（已连续四十七轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**CE 里"看起来不一致"的先查是否承重，可能就是 CE 的 bug——要忠实复刻并登记**；
**前轮的留痕登记可能正是为你预约的**（W-22 全篇、U07–U10 的"给 U11"章节）；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
📌 **"学会了"要用"能用出来"来证明**。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单，每条改义给 CE 依据。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证。）

## 7. 报告

写到 `ai_docs/reports/u-11.report.md`：CE 全路径（触发→资格→选择→走向→吸收→完成/放弃）与 web 实现；
一次分配守卫；CE 距离图缺陷复刻；端到端"学会并能用"证据（每类能力至少一例）；中断矩阵；RNG 影响；
翻正的留痕；反查与两份清单；撞红处置；§3 自检 + 最终复跑声明；
**empowerment 最终口径**（"数值与学习均对齐"或具体剩余限制）。
