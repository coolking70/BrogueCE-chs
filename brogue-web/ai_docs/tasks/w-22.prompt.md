# W-22：强化后的学习

> **本地执行（`codex exec --worktree`）。W 链效果收口第十五轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-22 行是权威规格**（`823eb0e`）：

> **W-22 强化后的学习**｜消费待学次数、选择可学尸体能力、排 BF_NOT_LEARNABLE、吸收动作与
> 实际施法/状态通路；同一 empowerment 魔杖收口｜依赖 W-21、W-1 元数据、已完成效果的 caster
> 通用入口；**外部依赖见下**｜生成流：**否**｜改动面 M、G、学习 helper、必要的尸体/保存字段 →
> R(实际文件)+S(M,C,B) 并搜 corpse/absorb｜撞红预期：P4-1b/2/3/4、P1-24 死亡、
> **尸体被消费一次/读档中断**；**新学 bolt 必须有可用执行出口，不得只展示学会**

**同时必读 §2.3 表后两段**（W-22 外部依赖与"暂缓时的标注口径"）。本任务书不复述；冲突时以报告为准。

## 1. 外部依赖——本轮的边界

W-0 已登记：
- 现有 `MONSTER_BOLT_TABLE` 只登记初始怪物所用种类；**学到的其他可学 bolt 需要新的执行映射**
- 可学的 **BLINKING** 依赖怪物专用 blink 调度（W-12 的玩家 blink **不**自动完成它）
- 可学的 **ANCIENT_SPIRIT_VINES** 依赖网/藤 DF 旁支

⇒ 本轮验收**可执行能力子集**。对未就绪的能力：
- ❌ **不得从候选里删掉 CE 合法候选**来伪造完整覆盖（会改变 CE 的抽签分母）
- ❌ **不得"学会但无效"**——只在说明页显示、实际不施放
- ✅ 请**自己按 CE 判断**对这类候选的正确处理，并明列取舍及其对抽签/行为的影响。
  如果结论是"本轮做不到忠实"，就**停下写清楚**，不要硬做

每一种 CE 可学 bolt 列一张表：web 是否有执行出口、本轮是否接通、未接通时的处理。

## 2. 线索（**全部请自行核实**，读到执行代码）

- 触发点：`Combat.c` 约 :2038 的 `anyoneWantABite(decedent)`，以及它前面的调用条件
- 候选资格：`canAbsorb`（约 :1632）——距离图、`targetCorpseLoc` 已占用、无生命/不动、`monsterAvoids`
- 选择：先 abilities/behaviors 标志，**没有时才**选 bolt；两级 `rand_range`
- ⚠️ `anyoneWantABite` 里**两次遍历**盟友——**请核对两次用的距离图是不是同一张**、
  以及第二次遍历能否找不到第一次数出来的第 N 个。**先读准 CE 的真实行为**再决定怎么移植
  （教训："看起来不一致"的先查是否承重；也可能就是 CE 的 bug，要忠实复刻并登记）
- `ourBolts` 何时被填充——注释说了一句，**请以执行路径为准**
- 吸收过程：`Monsters.c` 约 :3217（走向尸体、开始吸收的前置条件：中毒/燃烧）、
  约 :3259 `updateMonsterCorpseAbsorption`（20 回合计数、离开尸体的 15 回合阈值、完成时
  `newPowerCount--`、完成后按新旗标补永久状态）
- 尸体位置清除点：`Time.c` 约 :1567、:2119（换层/坠落）
- 说明页：`IO.c` 约 :4827 进度条、`Monsters.c` 约 :4555 "准备学习"文字

## 3. 与 W-21/W-23 的边界

- W-21 已写对 `newPowerCount/totalPowerCount`；本轮**消费** `newPowerCount`
- CE `Items.c` 约 :4544（消魔后 `newPowerCount = totalPowerCount`）与 `wasNegated` 读方属于 **W-23**，
  本轮不做，但**字段与存档要留好接口**
- 学到的 behavior 若是 FLIES/INVISIBLE/IMMUNE_TO_FIRE，CE 会补 1000 回合永久状态——这与
  已有的永久状态推导（p1_28）要一致

## 4. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ 不改 frequency、不增种类
- ⚠️ **尸体只能被消费一次**；读档中断（吸收中存档再读）要按 CE 字段恢复
- ⚠️ 新增 RNG 抽签只在**击杀路径**上；说清楚对交互 RNG 的影响

## 5. 教训（已连续二十九轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**；
**复用前轮原语发现不够通用就如实登记前轮缺口**；
**前轮的留痕登记可能正是为你预约的**，动手前 grep 相关注释；
**照搬上一轮的做法前，先确认 CE 在这里是不是也这么做**；
**公式有定点/舍入细节时，编译 CE 原函数取黄金值**；
**自己 W-0 的规格也可能写得不准**；**注释写的是意图，执行路径里可能有例外**；
**"看起来不重要"的 CE 判定位置可能是关键**；**C 的按值复制在 JS 里是浅拷贝**；
**"解状态"不是一个动作，是逐项规则**；
📌 **做不到忠实时，缩小范围并明列，不要删候选伪造完整**。

## 6. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定，额外搜 `corpse` / `absorb`。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 7. 门禁

按 §6 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 8. 授权反驳

W-0~W-21 都是你自己写的，实现时发现判断有误照样驳回并说明。
**若判断本轮应拆分或暂缓**（W-0 已预留这一口径），直接写报告说明，不必硬做完。

## 9. 报告

写到 `ai_docs/reports/w-22.report.md`，含：

1. CE 触发 → 资格 → 选择 → 走向 → 吸收 → 完成/放弃 的执行路径
2. §2 中"两次遍历"与 `ourBolts` 的核实结论（CE 真实行为、是否 bug、web 如何复刻）
3. **可学能力逐项表**（abilities / behaviors / 每种可学 bolt）：web 执行出口、本轮接通与否、未接通的处理
4. 尸体一次消费、读档中断、换层/坠落清除
5. 完成后的永久状态补齐与 p1_28 的一致性
6. 与 W-21 计数、W-23 消魔的接口
7. RNG 影响
8. §2 线索的核实结果
9. §6 反查过程与实际跑的文件
10. 撞红清单与处置
11. §4 自检 + **§7 最终复跑声明**
12. 清单外改动申报
13. **empowerment 的完成状态口径**（"数值与学习均对齐"或"学习部分对齐，限制如下"）
