# W-12：blink 与反向 beckoning

> **本地执行（`codex exec --worktree`）。W 链效果收口第五轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-12 行是权威规格**（`823eb0e`）：

> **W-12 blink 与反向 beckoning**｜BLINKING **移 caster**、E 距离、遇障前停；
> BECKONING **复用反向 blink** 到 caster 邻近，释放/等待；
> blinking 杖待入池、beckoning 魔杖已在池｜
> 依赖 W-3、W-4、W-5、**W-11 的安置原语**｜生成流：**否**｜
> 改动面 B、G、移动 helper；**怪物 beckoning 若迁移显列 M** → R(实际文件)+S(B,M,D,C)｜
> 撞红预期：**P4-1b 中 BLINKING 仍不经 tryUseBolt，另测明确的玩家移动**；
> 9c 阻挡、C-5/陷阱、P2 时序、**近邻不越位**、**目的地拾取**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 本轮是 W-11 安置原语的第一次复用

W-11（`f141c64`）新建了 `Movement/CreaturePlacement.ts` 的 `placeCreature`，
并**专门为 W-12 设计成通用的**（W-11 用危险格、玩家拾取、嵌套位移测试证明没写死
随机传送策略）。

⇒ **本轮就是检验那个设计的时刻。** 如果复用时发现 `placeCreature` 不够通用、
需要改它的接口，**如实报告**——那说明 W-11 的通用性判断有缺口，不是本轮的失败。
**不要为了复用而在 W-12 里另写一份安置逻辑。**

## 2. 线索（**全部请自行核实**，验收方的 CE 事实可能是错的）

- `PowerTables.c` 附近可能有 `staffBlinkDistance`，**公式请自行核**
- W-0 说 BECKONING "**复用反向 blink**"——即把目标拉到施法者邻近，
  与 blink 共享距离/遇障逻辑的方向相反版本。**这个"复用"在 CE 里是否真是同一段代码，请核实**
- W-3 已做"遇障前停"的轨迹；W-4 已做反射。**blink 的"遇障前停"与普通射线的是否同一语义**，请核

## 3. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ **不增种类、不改 frequency**——blinking 杖"待入池"是 W-25 的事，本轮只做效果
- ❌ **怪物自主 blink 不做**（W-0 登记为旁支）：P4-1b 里 BLINKING **仍不经 `tryUseBolt`**，
  这条**要保持**。本轮测的是**明确的玩家移动**
- ⚠️ **怪物 beckoning 若迁移到新实现，必须明列**（照 W-9 的方式申报怪物行为变化）
- ⚠️ **近邻不越位**（W-0 点名）：施法者已贴身时 beckoning 不能把目标拉过头/拉进施法者格
- ⚠️ **目的地拾取**（W-0 点名）：blink 落到有物品的格，拾取行为按 CE

## 4. 教训（已连续十九轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
📌 **撞上守卫时改代码，不改守卫、不绕扫描器**（W-11）。

## 5. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定。`scripts/w*-test-scope.mjs` 可复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 6. 门禁

按 §5 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 7. 授权反驳

W-0~W-11 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 8. 报告

写到 `ai_docs/reports/w-12.report.md`，含：

1. BLINKING 的距离公式、遇障前停、移动施法者的 CE 依据与落地
2. BECKONING 的 CE 依据，以及"复用反向 blink"是否属实
3. **对 W-11 `placeCreature` 的复用情况**：直接可用？需要改接口？（如需改，说明 W-11 的缺口）
4. **近邻不越位、目的地拾取**的处理
5. P4-1b 的 BLINKING 仍不经 `tryUseBolt` 的证明
6. 若迁移怪物 beckoning，**明列变化**
7. §2 线索的核实结果
8. §5 反查过程与实际跑的文件
9. 撞红清单与处置
10. §3 自检 + **§6 最终复跑声明**
11. 清单外改动申报
