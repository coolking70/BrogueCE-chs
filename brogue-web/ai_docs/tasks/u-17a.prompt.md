# U17a：DF 副作用事务——疏散/落物/刷新/聚怪统一出口（⚠️ 可能移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U17 第一轮（其后按 §3.3 五族独立施工）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U17 行、§3.1 K19、§2 D02 是权威规格**（`153dbb5`）：

> **U17 DF 副作用与机器后继载体**；K19/K28｜前置 U18a｜**先副作用事务，再按 §3.3 五族独立施工**；
> 每族铺装→实体疏散/落物→晋升→后继→可解/显示；**禁止一次删完 30 条缺口登记**

K19：DF 返回 evacuationRequired/aggravateRadius 等并不等于副作用落实；spawn 递归未形成统一生物/物品 refresh 事务
（DF.ts 约 :774–776、:839–859，对 CE Architect.c:3210–3285、3359–3495）。W obstruction 局部补丁只证明那条路径。

## 1. 范围（**全部请自行核实 CE**）

- CE `spawnDungeonFeature` 的全部副作用，按 CE 顺序：`DFF_EVACUATE_CREATURES_FIRST`（evacuateCreatures）、
  `DFF_BLOCKED_BY_OTHER_LAYERS`、`DFF_SUBSEQ_EVERYWHERE`、`DFF_AGGRAVATES_MONSTERS`（aggravateMonsters 半径/来源）、
  `DFF_CLEAR_OTHER_TERRAIN`、`DFF_PERMIT_BLOCKING`/blockingMap 回退、`refreshDungeonCell`/`applyInstantTileEffectsToCreature`
  与物品落入（`T_OBSTRUCTS_ITEMS`/`T_AUTO_DESCENT`/`swapItemToEnchantLevel` 等 CE 实际存在的调用）、`subsequentDF` 递归、`flavorMessage`/`description`
- 建一个**统一的 DF 副作用出口**：生成期（Architect/BlueprintEngine/AutoGenerator）与运行期（晋升、机关、法术、陷阱）共用；
  把现有分散的局部补丁（含 W obstruction）迁到这一出口，**不另留平行实现**
- 生成期与运行期的差别（如生成期无消息、`refreshSideEffects` 条件）按 CE 实际分支
- **不在本轮**：§3.3 的 30 条 tile=null 缺口、五族载体激活、autoGen 回接 —— 保持登记不动

## 2. 生成流

若生成期副作用落实改变生成：先 drift 记录 → 单变量归因（疏散、阻挡回退、subsequent、聚怪各自一段）→ 独立守卫 → 一次重捕获。
📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**返回值存在不等于副作用落实**——每种副作用要有"触发→实体/格子最终状态"证据；
**反查闭包含 p1_30、U24、U01/U03 合同守卫、c_4b/c_4c、f/g、V、W-13/14 与所有读源码守卫**。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-17a.report.md`。
