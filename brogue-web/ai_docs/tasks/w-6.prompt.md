# W-6：充能生命周期

> **本地执行（`codex exec --worktree`）。W 链第六轮，机制前置。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-6 行是权威规格**（`823eb0e`）：

> **W-6 充能生命周期**｜**WAND 停自然回电**；STAFF 依 E/智慧计时、随机间隔，
> blink/obstruction 特殊周期预留；充能卷轴的 STAFF/CHARM 范围单独接清楚｜
> 依赖 W-5｜生成流：**否（初始值留 W-5）；交互 RNG 会变**｜
> 改动面 G、I、资源 helper；必要时 L → R(实际文件)+S(Q,C,G)
> 并搜 recharging/ringWisdom｜
> 撞红预期：P2 客观时间、B-1/快照、scroll_effects/耗电极性；
> **不可按玩家动作数回电，也不可把护符完整重做混入**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. W-5 的交接（本轮是它的另一侧）

W-5（`60a89ee`）新建了 `Items/ArcanaInstance.ts`，把 **E（附魔等级）与当前次数分离**：
E3 初始 3 次 → 提交后当前次数 2、**E 仍 3、容量仍 3**。

⇒ **本轮的回电读 E 与容量，写当前次数。** 不要把回电写进 E，也不要让回电改容量。
W-5 的旧档迁移是确定性、零 RNG 的——**本轮的回电计时若要存档，同样要有旧档兜底**。

## 2. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
  （W-0 标注"交互 RNG 会变"——那是交互期，不是生成期）
- ❌ **不改初始值**（那是 W-5 刚做完的）
- ❌ **不增种类、不改 frequency**（W-24~26）
- ❌ **不实现任何效果行为**（W-8~W-23）
- ⚠️ **不可按玩家动作数回电**（W-0 点名）——CE 按客观时间，
  web 有 P2 的客观时间体系
- ⚠️ **不可把护符完整重做混入**（W-0 点名）——充能卷轴对 CHARM 的范围要
  "单独接清楚"，但**护符自己的充能模型不在本轮**
- ⚠️ **blink / obstruction 的特殊周期只预留，不实现**（它们的效果在 W-12 / W-14）

## 3. 教训（已连续十三轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**。

## 4. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定，并按 W-0 的提示
**搜 `recharging` / `ringWisdom`**（智慧戒指影响回电速度）。
W-5 留下的 `scripts/w5-test-scope.mjs` 可以改造复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 5. 门禁

按 §4 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 6. 授权反驳

W-0~W-5 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 7. 报告

写到 `ai_docs/reports/w-6.report.md`，含：

1. WAND 停回电、STAFF 按 E/智慧计时与随机间隔的 CE 依据与落地
2. **与 W-5 `ArcanaInstance` 的接口**：回电读什么、写什么，证明未改 E 与容量
3. **"不按玩家动作数回电"的证明**（客观时间怎么接的）
4. 充能卷轴对 STAFF / CHARM 范围的处理，**以及为何没有重做护符**
5. blink / obstruction 特殊周期的预留方式
6. 回电计时的存档与旧档兜底
7. §4 反查过程与实际跑的文件
8. 撞红清单与处置
9. §2 自检 + **§5 最终复跑声明**
10. 清单外改动申报
