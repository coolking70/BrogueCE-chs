# U19a：蓝图 IN_VIEW_OF_ORIGIN 视图约束（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U19 第一个子轮（U17 全部载体已闭合）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U19 行与其后子轮表（约 :197、:203）、§3.1 K28/K32 是权威规格**（`153dbb5`）：

> **U19 蓝图剩余算法/机器玩法**；K28/K32｜前置 U05/U17（已合并）、视图需 U18a（已合并）｜生成流：**直**｜
> R: BlueprintEngine/Architect/AutoGenerator、G、数据；S: IN_VIEW_OF_ORIGIN、pendingItems、pendingMonsters…｜
> **按子轮分别验，先闭包后解退池；每台必须有"用户触发→结果"的证据，不能只点亮身份覆盖**
> **U19a** IN_VIEW_OF_ORIGIN 视图约束

## 1. 范围（**全部请自行核实 CE**）

- CE `buildAMachine` 中 `MF_IN_VIEW_OF_ORIGIN` / `MF_IN_PASSABLE_VIEW_OF_ORIGIN` 的实现（Architect.c：从 origin 计算 FOV/可通行视野 `viewMap`，
  候选格须在视野内；视野计算所用的阻挡规则、半径、是否穿门/密门、何时计算——按 CE）
- web 当前对这两个标志的处理（忽略/近似/错误）逐一对照；接到 BlueprintEngine 的候选格过滤
- 受影响蓝图清单（哪些 feature 带这两个标志），每个受影响机器至少一个"生成→玩家从 origin 能看到/走到该 feature"的证据
- 如有因视图缺失而资格退池的机器，本轮只修约束，回池另轮（登记）

## 2. 生成流

先 drift 记录 → 单变量归因（IN_VIEW 与 IN_PASSABLE_VIEW 各一段）→ 独立守卫（候选格必在视野内；反例：去掉约束即红）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**反查闭包含 p1_30、U24、U01/U03、V 全系、b2_transcription、blueprint_center、c_8、U05a、U17a–f、U25 观测守卫 与所有读源码守卫**；
可用 U25 的 `scripts/u25-machine-inventory.mjs` 追踪机器产物。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-19a.report.md`。
