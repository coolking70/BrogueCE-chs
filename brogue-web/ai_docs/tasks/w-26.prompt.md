# W-26：地形/辅助杖目录与总收口

> **本地执行（`codex exec --worktree`）。W 链最后一轮。⚠️ 本轮移动生成流。**
> 用户已在场授权（2026-09-24）。

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.5 的 W-26 行、表前表后段落、§2.6 是权威规格**（`823eb0e`）：

> **W-26 地形/辅助杖目录与总收口**｜增 `staff_of_obstruction`(10)、`staff_of_discord`(10)、
> `staff_of_protection`(5)；**逐项审计全部 21 种正常施法入口**｜依赖 W-9、W-14/15、W-25；
> 其余效果轮完成｜生成流：**是**｜改动面同上 → R(实际文件)+S(G,Q,B,U,D,C)｜
> 撞红预期：生成/机器、B-1、c_4b/c_4c、9c｜**genStaffs=12，频率110；最终定义共24
> （21 CE+3退池），生成种类21**；旧档、新局和多 seed 获取均覆盖

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 前两轮已经为本轮铺好的东西（**请核实，不要假设**）

- **W-25**（`2472cfe`）§2：杖数组按 CE 表序**跳过未入池行**；本轮在 entrancement 后插 obstruction/discord、
  haste 后 light 前插 protection；`flavorIndex` 分别是 7/8/12；**原九行相对顺序不再调整**
- **W-25** §3：obstruction 的资源特例（初始 1000、10000/E 慢回电、卷轴 10000/E）已随 blink 一起激活，
  但 obstruction 的 JSON 身份/配置/入口**尚不存在**——本轮补齐后要**实测**这些特例对 obstruction 生效
- **W-25** 核实：CE obstruction 的物品详情 `buf2` 为空，没有独立数值文案——**不要臆造**
- W-24/W-25 的方法：CE 原表解析、票枚举、多 seed 分布守卫、**先单变量归因再决定是否重捕获**、
  `staffFlavors` 旧档兼容。本轮沿用
- 注意：W-25 零漂移是因为"所有杖同一条 E 抽签"——本轮是否仍成立**请自己验证**，不要预设结论

## 2. 总收口：逐项审计全部 21 种正常施法入口

W-0 原话"逐项审计全部 21 种正常施法入口"。21 = CE 9 种魔杖 + 12 种杖。**每一种**给一行：

| 种类 | CE 表行 | JSON 身份 | bolt 配置 | 正常 useArcana 入口 | 瞄准/自动选目标 | 取消不耗电 | 实际效果（前轮编号） | 鉴定/autoID | 充能/回电 | 已知限制 |

- 效果列**只引用前轮验收结论与本轮实测**，不重新实现
- 发现任何一种入口不通/效果与前轮结论不符 → **停下写清楚**，按 W-0"退回所属效果轮"，不在本轮修
- 同时汇总 W 链全部**已登记未闭合项**（W-22 学习暂缓、怪物 blink/藤蔓、机器不配发法器、
  独立发现屏不存在、NAUSEOUS/DARKNESS/MAGICAL_FEAR 未建模、怪物 bolt 伤害公式旧债、flare 动画等），
  标明来源轮次——这份清单会用于 W 链之后的全项目勘察

## 3. 线索（**全部请自行核实**）

- CE 杖表 `src/brogue/Globals.c` 约 :1648/:1649/:1653 三行
- protection 的 bolt 是 `BOLT_SHIELDING`，极性 −1（辅助类）；discord 极性 +1——逐列核对
- discord 杖与怪物 discord 共用效果（W-9），入口自动选目标的敌友方向
- obstruction 瞄准（W-14 效果：落点动态复制 DF_FORCEFIELD）；是否有 CE 使用前确认类特例

## 4. 生成流与基线

同 W-24/W-25 §3：先 drift 记录 → 单变量归因 → 分布守卫（目标 12 种、合计 110）→ 再决定是否重捕获。

## 5. 硬约束

- ✅ 允许重捕获 `generation_baseline`（仅在 §4 完成后）
- ❌ 不实现/修改效果（发现缺陷退回所属效果轮）；❌ 不动魔杖目录；❌ 不删退池定义
- ❌ 不调整原九种杖的相对顺序
- ⚠️ 怪物 BLINKING/藤蔓仍是 W-22 外部依赖，`p4_1b` 相关断言不动

## 6. 教训（已连续三十三轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**；
**前轮的留痕登记可能正是为你预约的**，动手前 grep 相关注释；
**照搬上一轮的做法前，先确认 CE 在这里是不是也这么做**；
**自己 W-0 的规格也可能写得不准**；**注释写的是意图，执行路径里可能有例外**；
**基线变绿 ≠ 生成正确**；**重捕获前先归因**；**表的行顺序也是数据**；
**前轮建原语时该物品还没入池——它留的是接口还是遗漏，要读代码确认**；
📌 **上一轮零漂移的理由，这一轮要重新验证，不能继承结论**。

## 7. 授权测试清单：**自行反查**

按 W-0 §2.2/§2.6 的 R(files)+S(pattern) 自行确定（本轮 G,Q,B,U,D,C），给出
"预计改义断言"与"只回归不可放宽断言"两份清单，每条改义给 CE 依据。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 8. 门禁

按 §7 自查清单 + `npm run build` + `npm run test:drift`（重捕获后必须绿）。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证；基线文件前后哈希单列。）

## 9. 授权反驳

W-0~W-25 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 10. 报告

写到 `ai_docs/reports/w-26.report.md`，含：

1. CE 杖表三行逐列对照与 web 最终 12 种目录
2. obstruction 资源特例的实测
3. 三种杖入口核实
4. **漂移记录与归因**、基线决定
5. 分布守卫（12 种、合计 110）
6. 旧档兜底（含 flavor）
7. **21 种正常施法入口总审计表**（§2 格式）
8. **W 链已登记未闭合项汇总**（带来源轮次）
9. 发现的效果缺陷（若有）及退回建议
10. §1/§3 线索的核实结果
11. §7 反查过程、两份清单与实际跑的文件
12. 撞红清单与处置
13. §5 自检 + **§8 最终复跑声明**
14. 清单外改动申报
