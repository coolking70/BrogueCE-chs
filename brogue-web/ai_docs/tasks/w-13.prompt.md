# W-13：掘地效果

> **本地执行（`codex exec --worktree`）。W 链效果收口第六轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-13 行是权威规格**（`823eb0e`）：

> **W-13 掘地效果**｜TUNNELING：**按地形逐层开通、E 预算、不可穿墙/反射**、
> 路径与地图缓存；tunneling 杖待入池｜依赖 W-3、W-4、W-5｜生成流：**否**｜
> 改动面 G、B、掘地 helper；**Map 只改必要接口** → R(实际文件)+S(B,D)｜
> 撞红预期：c_4a 层模型、c_3 门墙、c_8 连通、p4_10_waypoint、scroll_effects 碎墙；
> **E=2 连墙预算、不可穿墙保护不能删**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 前几轮留给本轮的已知缺口（**线索，请自行核实**）

- W-2 登记："不改**墙在旧轨迹中进不了 path** 的问题"——即普通射线轨迹遇墙就停，
  而掘地射线要**穿进墙里**。W-3 做轨迹时也明说"**tunneling 不启用特殊取线奖励/挖墙预算**"
  ⇒ 本轮要给掘地射线一条**不同于普通射线**的轨迹规则，但**不能改坏 W-3 的普通轨迹**
- W-0 点名"**不可穿墙保护不能删**"——即有些墙（地图边界、不可破坏的墙）掘地也打不穿。
  这条保护是什么、在哪，请回 CE 查
- "**E=2 连墙预算**"——E 决定能挖穿几格墙。公式请自行核

## 2. ⚠️ 地形层写入有守卫——**W-11 撞过**

`c_4a_0_layer_model.test.ts` 钉着 **`setTerrainLayer` 调用点白名单**。
W-11 第一版在 Game.ts 直接写地形层，撞上了它。W-11 的正确处置是：
**把地形写入归入已有地形模块（`Map/Promotion.ts` 等）做成语义入口，Game 只调入口**
——"不改白名单、不用别名隐藏直接写层"。

⇒ 本轮掘地**一定会写地形**，**照 W-11 的做法**，别在 Game.ts 里直接写层。
W-0 原话"**Map 只改必要接口**"也是这个意思。

## 3. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**（掘地是交互期改图）
- ❌ **不增种类、不改 frequency**——tunneling 杖"待入池"是 W-25 的事
- ❌ **不改 W-3 的普通射线轨迹**——掘地的穿墙规则只对掘地生效
- ❌ **不删"不可穿墙"保护**（W-0 点名）
- ⚠️ 掘地改图后**连通性与寻路缓存要更新**（c_8 连通、p4_10 waypoint 点名）
- ⚠️ `scroll_effects` 里的**碎墙卷轴**（shattering）也改墙——**别改到它**

## 4. 教训（已连续二十轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**（W-11）；
📌 **复用前轮原语时，发现它不够通用就如实登记前轮的缺口**（W-12 对 W-11 做得很好）。

## 5. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定。`scripts/w*-test-scope.mjs` 可复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 6. 门禁

按 §5 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

⚠️ **近两轮机器变慢约 3 倍**（W-12 你自己的并行运行就撞了 4 项超时）。
若再遇超时，照 W-12 的做法：**串行重跑超时文件、如实区分"逐文件通过"与"一次完整
并行命令退出 0"、不把资源竞争推测写成根因、不改超时上限**。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 7. 授权反驳

W-0~W-12 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 8. 报告

写到 `ai_docs/reports/w-13.report.md`，含：

1. 逐层开通的规则与 CE 依据；**E 预算公式**
2. **掘地射线的穿墙轨迹规则**，以及怎么保证**没改坏 W-3 的普通轨迹**
3. **"不可穿墙"保护**是什么、在哪、怎么保留的
4. 反射的处理（掘地射线能否被反射？按 CE）
5. **地形写入走的语义入口**（证明没在 Game.ts 直接写层）
6. 连通性与寻路缓存的更新
7. 与碎墙卷轴的边界
8. §1 三条线索的核实结果
9. §5 反查过程与实际跑的文件
10. 撞红清单与处置
11. §3 自检 + **§6 最终复跑声明**
12. 清单外改动申报
