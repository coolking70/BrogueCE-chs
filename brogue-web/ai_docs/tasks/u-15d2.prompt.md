# U15d-2：护甲符文效果 CE 化（不移动生成流）

> **本地执行（Windows 轨）。X-0 路线；U15d 第二轮（U15d-1 武器符文已合并）。生成映射另交 Mac 轨。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U15d 行、§3.1 K24 是权威规格**（`153dbb5`）：

> **U15d 武器护甲符文**；K24｜**逐符文小轮：目标物种/几率/附魔、谱影和消魔、负面符文生成、持久化**
> K24：缺 A_MULTIPLICITY/BURDEN/VULNERABILITY/IMMOLATION 等；**A_MULTIPLICITY 消魔识别不等于动态生成完成**

## 1. 范围（**全部请自行核实 CE**）

- CE 全部护甲符文（A_MULTIPLICITY、A_MUTUALITY、A_ABSORPTION、A_REPRISAL、A_IMMUNITY、A_REFLECTION、A_RESPIRATION、A_DAMPENING、
  A_BURDEN、A_VULNERABILITY、A_IMMOLATION）按 CE `applyArmorRunicEffect`（Combat.c:896 起）及其各消费者逐个对齐：
  - 触发条件/几率（`armorRunicChance` 等，含近战 vs 投掷两处调用 Combat.c:1272、Items.c:6822 的差别）
  - 效果：multiplicity 谱影（复用 U16/U15d-1 谱影生命周期）、mutuality 分摊、absorption 减伤、reprisal 反伤、immunity 物种（`vorpalEnemy`/U15d-1 MonsterClass）、
    reflection（弹道反射，复用既有 reflectionChance）、respiration（气体免疫）、dampening（爆炸免疫）、burden（力量需求递增）、vulnerability（受伤翻倍/背刺）、immolation（点燃）
  - 符文揭示/鉴定消息与时机
- **不在本轮**：生成映射（缺失符文入池移动生成流）
- 字段复用 U01 既有 `runicType/runicKnown/vorpalEnemy`；新增字段纳入 U01/U03 合同；不做旧存档迁移

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
每个符文要有"受击→触发/效果终态"黄金值（多个附魔，含 RNG 消费次数）；**不得改变 test:drift**；
**反查闭包含 p1_30、U24、U01/U03、armor_runic*、armor_model_effect、b_4a、W-20/23、U16、U15d 与所有读源码守卫**；不产生 CRLF。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明。报告 `ai_docs/reports/u-15d2.report.md`。
