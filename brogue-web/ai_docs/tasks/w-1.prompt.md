# W-1：身份与配置契约

> **本地执行（`codex exec --worktree`）。W 链第一轮，机制前置。**

## 0. 本轮规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` 是本轮的权威规格**（已合入 `823eb0e`）。
验收方在这个子系统上估错过四次（CE 表位置、效果数 28 vs 22、缺口 13 vs 6、
测试反查命令漏 `src/engine/**`），所以**本任务书不复述技术细节，按报告执行**。

报告 §2.3 对 W-1 的定义：

> **W-1 身份与配置契约**｜建 CE bolt 类型/效果/旗标/magnitude 语义映射、
> 施法者/命中者/落点/autoID 结果契约；补 `POLYMORPH`/`PLENTY` 类型。
> **保留现有路由，不新增 JSON 物品**｜依赖 W-0｜生成流：**否**｜
> 改动面 B、G、独立映射 helper → R(B,G)+S(B,Q)｜
> 撞红预期：Bolt 配置构造的 TS 编译、`monsterBolts`、P4-1b 已知缺口表；
> **不要偷偷启用怪物 BLINKING/网/藤**

报告 §2.1 的切分原则同样适用：机制改动、效果改动、入池改动**分别验收**。

## 1. 硬约束

- ❌ **不移动生成流**。`npm run test:drift` 必须绿**且不许重捕获基线**。
  这是本轮成败判据之一。
- ❌ **不新增 `arcana.json` 的任何物品**（留 W-24~26）
- ❌ **不实现任何效果行为**（留 W-8~W-23）——本轮只建契约与映射
- ❌ **不要偷偷启用怪物 `BLINKING` / 蛛网 / 藤射线**（报告点名的陷阱）
- ✅ 补 `POLYMORPH` / `PLENTY` 两个**类型**（不是行为）

## 2. 教训（已连续八轮验证，继续照做）

- **0.1 闭包**：填活 DF tile 沿四条边递归走完
- **0.2 退池留形 = 加引擎过滤，不是改数据**
- **0.3 覆盖门没观测对象时换样本，不改期望**；不钉精确台数
- **0.4** **「建成 0 台」≠「RNG 足迹为 0」**——看是否参与抽签
- **0.5** 驳回时给**登记位置**，区分"仍有效"与"已过期"的登记
- **0.6** **代码事实为真 ≠ 它是本次失败的成因**——诊断要实测

## 3. 授权测试清单：**用报告 §2.2 的方法自己反查**

⚠️ 验收方连续两轮栽在手写清单上（9e-1 漏三个、9e-2 漏一个），
且给的反查命令还漏了 `src/engine/**`。

⇒ **本轮不给固定清单。** 按报告 §2.2 的两层方法自行确定：
1. **R(files)**：对最终改动的生产文件建反向 import/re-export 闭包
2. **S(pattern)**：在闭包与全体测试中搜公开 API、私有桥接、数据字段、旧行为断言

⚠️ 报告自己的限定要遵守：**闭包是"可能受影响的结构集合"，
既不是预计必红数，也不是修改断言的授权。**

**在报告里列出你实际跑了哪些文件、以及反查是怎么做的。**

## 4. 门禁

按 §3 自查得出的清单跑，外加 `npm run build`、`npm run test:drift`。

⚠️ **不要跑不带参数的全量 `npx vitest run`**——会和验收方抢核。

**最终复跑（硬要求）**：所有编辑完成后重跑，给逐文件结果并声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 5. 授权反驳

W-0 报告是你自己写的，但**实现时若发现报告的判断有误，照样驳回并说明**。
验收方这份任务书若与报告冲突，**以报告为准**。

## 6. 报告

写到 `ai_docs/reports/w-1.report.md`，含：

1. 契约与映射的落地（CE 依据行号）
2. `POLYMORPH` / `PLENTY` 类型补齐的位置与"仅类型不含行为"的证据
3. **§3 的反查过程与实际跑的文件清单**
4. 撞红的守卫清单与处置（特别是 P4-1b 已知缺口表怎么处理的）
5. §1 自检：`test:drift` 绿否、确未重捕获、`arcana.json` 是否零改动
6. **§4 的最终复跑声明**
7. 清单外改动申报
