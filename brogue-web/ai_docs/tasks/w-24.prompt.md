# W-24：魔杖目录闭环

> **本地执行（`codex exec --worktree`）。W 链物品入池第一轮。⚠️ 本轮移动生成流。**
> 用户已在场授权开始（2026-09-24）。

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.5 的 W-24 行、表前表后段落、§2.6 是权威规格**（`823eb0e`）：

> **W-24 魔杖目录闭环**｜增 `wand_of_polymorphism`(3)、`wand_of_negation`(3)、`wand_of_domination`(1)、
> `wand_of_plenty`(2)；empowerment frequency **3→1**；按 CE range/极性接线｜
> 依赖 W-1～7、W-11/12、W-17/19/20、W-23｜生成流：**是**｜
> 改动面 arcana.json、B、L、文案/必要 UI → R(实际文件)+S(G,Q,B,U)｜
> 撞红预期：b_4a/b_4b、invented_content_pool、B-1 三项、itemFlavors、blueprint_center/v_2b_6_keys、i18n；
> **目标 genWands=9、频率合计22，定义11（含两退池）**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 本轮只做目录/入口/生成，不做效果

W-0 原话：四条魔杖"都已经在前轮独立验过，届时只做目录/入口/生成，**不再顺手实现效果**；
若入池前还发现效果缺陷，应**退回所属效果轮修复**"。

⇒ 发现效果缺陷时：**停下、写清楚、不修**（除非是一行接线错误且明显属于本轮"入口"范畴，需申报）。

前轮状态交接：
- **W-17 支配、W-19 变形、W-20 复制、W-23 消魔**：效果已验收
- **W-21 强化**：数值已对齐；**W-22 学习暂缓**（`59e172c`）——empowerment 频率改 1 不等于学习完成，
  说明文字与发现屏不得宣称学习已可用以外的内容（W-21 改过的说明保留）
- 两条自创退池项（`wand_of_fire`、`wand_of_lightning`）**继续退池、不删定义**

## 2. 线索（**全部请自行核实**）

- CE 魔杖表 `GlobalsBrogue.c` 约 :700–:710：**行顺序**、frequency、marketValue、charges range、
  **魔法极性列**、flavor（`itemMetals[i]`）下标。
  ⚠️ **行顺序会影响按频率抽种类的遍历顺序与 flavor 分配**——web 的 kind 列表顺序要对齐 CE 表序，
  不能把新魔杖追加在末尾了事。请读 web 实际抽签代码确认它按什么顺序遍历
- 极性影响哪些地方（发现屏的善/恶标记、检测魔法、商店/祭坛等）——**逐个读方确认**
- 发现屏/鉴定、直接构造、正常 `useArcana` 入口、**机器配发**（V 链登记"机器不能配发 staff/wand"
  这条债项与本轮的关系，请核实后说明，不扩轮）
- `ArcanaInstance.ts` 的 charges 表是否已含四条新魔杖（W-5 建的）

## 3. 生成流与基线（本轮核心风险）

§2.6：`generation_baseline` **不记录**每种法器频率/充能分布——它变绿不代表生成正确。
⇒ 必须：
1. **先**跑 `npm run test:drift`，记录漂移范围与原因（哪些层、哪些字段），**再**重捕获基线
2. 新增**种类/频率/充能分布**的独立守卫（多 seed 统计 + 随机调用计数），证明 9 种魔杖、合计 22
3. 说明漂移为什么只可能来自本轮改动（归因）；若发现与本轮无关的漂移，**停下报告**
4. 重捕获基线前后的差异摘要放进证据目录

## 4. 硬约束

- ✅ 允许重捕获 `generation_baseline`（仅在第 3 节步骤完成后）
- ❌ 不动杖（staff）目录——那是 W-25/26
- ❌ 不实现/修改效果逻辑（见 §1）
- ❌ 不删退池定义、不把退池项塞回生成池
- ⚠️ 旧档：旧存档里的物品身份表/flavor 映射在新增种类后如何兜底，要有测试

## 5. 教训（已连续三十一轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**；
**复用前轮原语发现不够通用就如实登记前轮缺口**；
**前轮的留痕登记可能正是为你预约的**，动手前 grep 相关注释；
**照搬上一轮的做法前，先确认 CE 在这里是不是也这么做**；
**自己 W-0 的规格也可能写得不准**；**注释写的是意图，执行路径里可能有例外**；
**做不到忠实时，缩小范围并明列**；
📌 **基线变绿 ≠ 生成正确**；**重捕获前先归因**；**表的行顺序也是数据**。

## 6. 授权测试清单：**自行反查**

按 W-0 §2.2/§2.6 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。** 每条改义断言给 CE 依据。

## 7. 门禁

按 §6 自查清单 + `npm run build` + `npm run test:drift`（重捕获后必须绿）。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证；基线文件前后哈希单列。）

## 8. 授权反驳

W-0~W-23 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 9. 报告

写到 `ai_docs/reports/w-24.report.md`，含：

1. CE 魔杖表逐行对照（顺序/频率/价值/充能/极性/flavor）与 web 最终目录
2. web 抽种类的遍历顺序与 CE 一致性
3. 极性的所有读方
4. 发现屏/鉴定/直接构造/useArcana/机器配发 逐入口核实
5. **漂移记录与归因**（重捕获前）、基线差异摘要
6. 种类/频率/充能分布守卫：方法、seed 数、结果
7. 旧档兜底
8. 发现的效果缺陷（若有）及退回建议
9. empowerment 口径（学习未完成）
10. §2 线索的核实结果
11. §6 反查过程、两份清单与实际跑的文件
12. 撞红清单与处置
13. §4 自检 + **§7 最终复跑声明**
14. 清单外改动申报
