# UR2：Game.ts 拆分第二步——物品使用/魔法效果协调（纯重构，不改行为）

> **本地执行（Windows 轨）。X-0 路线 UR 系列第二轮（UR1 编解码已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` UR 表的 UR2 行是权威规格**（`153dbb5`）：

> **UR2 物品使用/魔法效果协调**｜从 A6/A7/A8 抽窄服务：资源提交、目标上下文、共享效果调用；Game 保留 UI 门面；减少 U05/U06/U15 复用时的分叉｜
> **验收**：固定初态+命令日志，对每步资源/位置/伤害/状态/日志/行动 tick/两流计数做前后 trace；W-2…26 和 b/scroll/投掷合同不变；**取消/反射/失败入口也比较**｜
> **风险**：私有 applyBoltEffect、boltWorld 等先保留薄包装；**源码扫描迁到新实际实现而非删除**；调用边界和 effect 顺序为主

## 1. 范围

- 从 Game.ts 抽出物品使用/魔法效果的协调层（喝药水、读卷轴、法杖/魔杖/护符使用、投掷、附魔/鉴定选物的资源提交与效果分派、目标上下文），
  成为显式依赖注入的窄服务模块；Game 保留原 UI 门面方法（签名与 `this` 语义不变）
- **零行为变化**：效果顺序、RNG 调用次数与顺序、消息文本、行动 tick 一律不变
- 验收：用 U27 命令日志录一组固定序列（覆盖每类物品的成功、取消、失败、反射、确认拒绝），重构前后逐步 trace
  （资源、位置、伤害、状态、日志、tick、两流 RNG state/count）逐字段相等
- 源码读取守卫若指向旧位置：**迁到新实际实现，不删除、不放宽**

## 2. 约束

📌 **撞上守卫时改代码，不改守卫**；守卫前提若仅因"代码搬家"失效，先反事实证明再迁移前提、交验收方裁决；
**不得改变 test:drift**；**反查闭包含 p1_30、U24、U01/U03、U27、UR1、W-2…26、b_*、scroll_effects、投掷与所有读源码守卫**；不产生 CRLF。

## 3. 门禁与报告

R∪S + build + test:drift；**全量 `npm test` 完整跑完**（不得中止）；最终复跑声明。报告 `ai_docs/reports/u-r2.report.md`（含 Game.ts 行数前后对比）。
