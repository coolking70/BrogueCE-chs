# X-1：收尾勘察——U00–U27 与 UR 系列完成后的全项目 CE 对齐度复核（只读）

> **本地执行（Windows 轨，astra xhigh）。只读勘察：不改生产代码、测试、数据、基线；交付仅报告、`scripts/x1-*` 与 `ai_docs/reports/x-1-evidence/`。**
> 与 Mac 轨 U26a（D27–40 深层/宝石/超胜利地图）并行：**本轮不评 D27–40 深层生成**，只登记"待 U26a 合并后复核"。

## 0. 依据

- 上一轮全项目勘察 `ai_docs/reports/x-0-survey.report.md`（`153dbb5`）的 §2 域表 D01–D27、§3.1 K01–K41、§4.3 U00–U27、UR1–4
- 其后各轮报告 `ai_docs/reports/u-*.report.md`、`u-r*.report.md`（逐轮已声明完成的范围与**登记的保留边界**）
- 权威仍是本工作树 `BrogueCE-master`

## 1. 任务

1. **逐条结案核对 K01–K41**：每条给出"已闭合 / 部分 / 未闭合 / 已过期"，**以当前代码执行路径与现有守卫为证据**（入口→消费者），不以报告自述为准；
   对"已闭合"抽查至少一个真实守卫是否确实覆盖（读测试断言，不只看文件名）
2. **逐域复核 D01–D27**：沿 X-0 方法（缺/偏/创/已过期），重新盘点；重点找 **X-0 未列、或各轮报告"保留边界"中登记而无后续轮承接**的差异
3. **汇总各轮登记的保留边界/债务**（逐轮报告中"不在本轮""登记""保留"条目），判断哪些仍有效、哪些已被后续轮闭合
4. **自创内容（D2 退池、RETIRED_INVENTED_BLUEPRINT_IDS、invented_content_pool 等）**：确认仍不进入生成、不被玩家触达
5. **i18n/UI 最终一致性**：中文渲染、硬编码英文残留（K41 延续）、详情数值与实际效果一致性抽查
6. 产出**剩余工作清单**：按"影响玩家可见行为 / 仅内部 / 仅文档"分级，给出建议派发单元（若有）

## 2. 约束

- 只执行构建、只读探针和现有测试的定向运行；**不注入变体、不重捕获基线、不改任何现有文件**（进度日志除外）
- 结论须有文件:行号与执行路径证据；证据不足写"未核实"，不推测
- 不产生 CRLF

## 3. 交付

报告 `ai_docs/reports/x-1-survey.report.md`（结构对齐 X-0：方法、域表、K 结案表、保留边界汇总、剩余工作清单）。
