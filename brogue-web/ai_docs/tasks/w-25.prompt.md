# W-25：移动/控制杖目录

> **本地执行（`codex exec --worktree`）。W 链物品入池第二轮。⚠️ 本轮移动生成流。**
> 用户已在场授权（2026-09-24）。

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.5 的 W-25 行、表前表后段落、§2.6 是权威规格**（`823eb0e`）：

> **W-25 移动/控制杖目录**｜增 `staff_of_tunneling`(10)、`staff_of_blinking`(11)、`staff_of_entrancement`(6)，
> **特别核 blink 回电周期与瞄准**｜依赖 W-1～7、W-12/13/18、**W-24 基线归因完成**｜生成流：**是**｜
> 改动面同 W-24 → R(实际文件)+S(G,Q,B,U,D)｜撞红预期：同物品/生成组，另 P2/地形/目标 UI；
> **genStaffs 从 6 到 9，定义 10（含 light），频率 58→85**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 照 W-24 的做法，但先确认 CE 在杖这里是不是也这样

W-24（`5b8b31b`）已建立：CE 原表解析测试、按表序排 kind、票枚举、多 seed 分布守卫、
**先单变量归因再重捕获基线**、旧档 flavor 兼容快照。**本轮沿用这套方法**，但注意：

- CE 杖表在 **`src/brogue/Globals.c`**（约 :1641），**不在** variants 文件里——请自己确认 web
  现在对齐的是哪一张表
- ⚠️ **表序**：验收方粗看 CE 表是 lightning 在 firebolt **之前**，而 web `arcana.json` 是 fire 在前。
  **请自行核实**，若确实不一致，本轮一并按 CE 表序修正（与 W-24 修 beckoning 同理），
  并在漂移归因里把"表序修正"和"新增三种"**分开归因**
- 本轮只加三种；**obstruction/discord/protection 是 W-26**。W-26 未入池前，
  表序里它们的位置怎么处理（跳过？占位？）要让 W-26 只需"加行"而**不必再挪顺序**，请说明设计
- flavor（`itemWoods[i]`）下标、极性列、frequency、charges range，逐行核对
- W-24 的 `wandFlavors` 旧档兼容：杖是否需要同样的 `staffFlavors`？（W-24 改魔杖表序才需要；
  若本轮改杖表序，**同样的问题会出现**）
- `staff_of_light` 是退池自创项，**继续退池、不删定义**

## 2. W-0 点名："特别核 blink 回电周期与瞄准"

线索（**全部请自行核实**，读到执行路径）：
- CE 对 **STAFF_BLINKING / STAFF_OBSTRUCTION** 有多处特例：生成时初始 enchant2（`Items.c` 约 :338）、
  充能卷轴（约 :4730）、自然回电（`Time.c` 约 :2028）、描述文字（约 :3039/:3109）、
  以及约 :7358 的一处与 blink 相关的判断——**请逐个读，并确认 W-5/W-6/W-7 是否已处理**
  （它们建时 blink 杖还没入池，可能留了接口也可能漏了）
- W-7 的附魔写 `500/E`——**blink 在附魔路径上有没有特例**？请读 CE 附魔代码确认，不要假设
- 瞄准：W-12 已做玩家 blink 效果；本轮核入口（最大距离随 E、目标选择 UI、取消不耗电）
- entrancement 与 tunneling 的入口同理核对（W-18、W-13）

## 3. 生成流与基线

同 W-24 §3：先 drift 记录 → 单变量归因（**表序修正与新增种类分开**）→ 分布守卫 → 再重捕获。
`generation_baseline` 不记录法器分布，变绿不等于正确。

## 4. 硬约束

- ✅ 允许重捕获 `generation_baseline`（仅在 §3 完成后）
- ❌ 不加 W-26 的三种杖；不动魔杖目录
- ❌ 不实现/修改效果（W-0："入池前发现效果缺陷应退回所属效果轮"）——发现就停下写清楚
- ❌ 不删退池定义
- ⚠️ 怪物 BLINKING 仍是 W-22 登记的外部依赖；**玩家的 blink 杖入池不等于怪物会 blink**，
  `p4_1b` 对怪物跳过 BLINKING 的断言保持不动

## 5. 教训（已连续三十二轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**；
**前轮的留痕登记可能正是为你预约的**，动手前 grep 相关注释；
**照搬上一轮的做法前，先确认 CE 在这里是不是也这么做**；
**自己 W-0 的规格也可能写得不准**；**注释写的是意图，执行路径里可能有例外**；
**基线变绿 ≠ 生成正确**；**重捕获前先归因**；**表的行顺序也是数据**；
📌 **前轮建原语时该物品还没入池——它留的是接口还是遗漏，要读代码确认**。

## 6. 授权测试清单：**自行反查**

按 W-0 §2.2/§2.6 的 R(files)+S(pattern) 自行确定（本轮多一个 D），给出
"预计改义断言"与"只回归不可放宽断言"两份清单，每条改义给 CE 依据。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 7. 门禁

按 §6 自查清单 + `npm run build` + `npm run test:drift`（重捕获后必须绿）。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证；基线文件前后哈希单列。）

## 8. 授权反驳

W-0~W-24 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 9. 报告

写到 `ai_docs/reports/w-25.report.md`，含：

1. CE 杖表逐行对照（顺序/频率/价值/充能/极性/flavor）与 web 最终目录；表序核实结论
2. 为 W-26 预留的表序设计
3. **blink/obstruction 全部 CE 特例逐处**：W-5/6/7 是否已处理、本轮处理
4. 三种杖的入口核实（瞄准/距离/取消/鉴定/直接构造/机器配发）
5. **漂移记录与归因**（表序修正 vs 新增，分开）、基线差异摘要
6. 分布守卫：方法、seed 数、结果（目标 9 种、合计 85）
7. 旧档兜底（含 flavor）
8. 发现的效果缺陷（若有）及退回建议
9. §1/§2 线索的核实结果
10. §6 反查过程、两份清单与实际跑的文件
11. 撞红清单与处置
12. §4 自检 + **§7 最终复跑声明**
13. 清单外改动申报
