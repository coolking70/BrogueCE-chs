# W-21：强化数值与计数

> **本地执行（`codex exec --worktree`）。W 链效果收口第十四轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-21 行是权威规格**（`823eb0e`）：

> **W-21 强化数值与计数**｜EMPOWERMENT 的 +12/+10/伤害增量、全疗/解状态、重复强化、
> newPowerCount/totalPowerCount 保存；empowerment 魔杖｜依赖 W-4、W-9、W-20｜生成流：**否**｜
> 改动面 G、M、保存/说明 → R(实际文件)+S(M,C,Q) 并搜 empower/mutation｜
> 撞红预期：monster_stats_effect、**克隆/变形后属性**、快照；
> **去掉"只能强化一次"的错误说明**；**本轮明确尚未完成学习**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 现状（验收方粗看，**请自行核实**）

web `Game.ts` 约 :4894 的 EMPOWERMENT 分支是**发明逻辑**：盟友 `maxHp×1.5`、敌人 `maxHp×1.3`、回满血，
别的都不动。CE 的 `empowerMonster`（`Monsters.c` 约 :547）与 `BE_EMPOWERMENT`（`Items.c` 约 :5311）
看起来是另一回事——请**读执行路径**确认：
- 作用对象的门槛（玩家？无生命？无敌？盟友/敌人是否区分？）
- 各属性增量的**整数运算**（伤害上下界的增量公式有 `max(1, …/10)` 一类的细节）
- 回血调用的参数——**第三个参数是否意味着解状态**，解哪些、怎么解（逐项读 `heal()`，
  有的状态设为 1、有的清 0、有的连带清数量字段；**不要概括成"全清"**）
- 计数字段的递增，以及**别处谁读它们**（学习、说明文字、复活祭坛的优先级……），
  本轮只需保证**写对并存档**；读方若已移植就接上，未移植的登记
- 反射到玩家时的行为（CE 说明文字有一句，请以执行路径为准）
- 自动鉴定条件、闪光

## 2. 与前几轮的交接

- **W-20 克隆**：克隆整体复制后，计数字段与已强化的属性应随之复制——请用 W-20 的
  `cloneMonster` 验证，不要另写一套
- **W-19 变形**：变形时计数字段/强化属性是否被重置，**按 CE 变形代码逐项核实**，
  若 W-19 的重置表遗漏或多做了，如实登记/修正
- **W-9** 的 heal 语义（若已有 helper，确认 panacea 分支是否已移植；若只移植了百分比回血，
  本轮补齐或明确登记）

## 3. 说明文字

W-0 点名：`arcana.json` 里 empowerment 的说明含"**每只怪物只能被强化一次**"——CE 没有这个限制。
改成与 CE 说明文字语义一致的中文（CE 原文在 `GlobalsBrogue.c` 物品表里）。
**不要删掉"可学新能力"的表述**，但本轮学习未完成——报告里写清楚。

## 4. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ **不改 frequency**（empowerment 3→1 是 W-24 的事）、**不增种类**
- ❌ **不实现学习**（W-22）：`newPowerCount` 只增不消费；显示"准备学习"文字若 CE 在说明页有，
  可以接上但不得给出学习动作
- ⚠️ 旧存档没有计数字段 → 兜底为 0

## 5. 教训（已连续二十八轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**；
**复用前轮原语发现不够通用就如实登记前轮缺口**；
**前轮的留痕登记可能正是为你预约的**，动手前 grep 相关注释；
**照搬上一轮的做法前，先确认 CE 在这里是不是也这么做**；
**公式有定点/舍入细节时，编译 CE 原函数取黄金值**；
**自己 W-0 的规格也可能写得不准**；**注释写的是意图，执行路径里可能有例外**；
**"看起来不重要"的 CE 判定位置可能是关键**；
**C 的按值复制在 JS 里是浅拷贝**；
📌 **"解状态"不是一个动作，是逐项规则**——每个状态的处理方式可能不同。

## 6. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定，并额外搜 `empower` / `mutation`。
`scripts/w*-test-scope.mjs` 可复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 7. 门禁

按 §6 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。
若遇超时，照 W-12 的做法处理（串行重跑、如实区分、不改超时上限）。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 8. 授权反驳

W-0~W-20 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 9. 报告

写到 `ai_docs/reports/w-21.report.md`，含：

1. CE `BE_EMPOWERMENT` 门槛与 `empowerMonster` 执行路径
2. 属性增量的整数运算与**黄金值**（至少覆盖伤害下界 0/1/9/10/19 一类边界）
3. `heal(…, panacea)` 的逐状态表：web 的对应处理
4. 计数字段：写、存档、旧档兜底；**CE 中所有读方**及其 web 移植状态
5. 重复强化（同一只强化 N 次）的累积结果
6. 克隆（W-20）/变形（W-19）后的计数与属性
7. 反射到玩家、无生命/无敌目标
8. 说明文字修改前后
9. **学习未完成的明确声明**
10. §1 线索的核实结果
11. §6 反查过程与实际跑的文件
12. 撞红清单与处置
13. §4 自检 + **§7 最终复跑声明**
14. 清单外改动申报
