# U14b：状态缺口第二轮——缠绕计数、穿甲、狂怒、寿命

> **本地执行（`codex exec --worktree`）。X-0 路线。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U14 行、§3.1 K14 是权威规格**（`153dbb5`）：

> **U14 状态缺口**｜分 U14a 恶心/黑暗/恐惧/虚弱，**U14b 束缚/穿甲/狂怒/寿命**｜
> **每项施加、刷新、客观/主观计时、到期/消魔、保存/显示**；独立 SEARCHING/营养不重做

U14a（`f93e3ff`）已完成第一轮，其报告是本轮的格式模板。

## 1. 本轮四项（**全部请自行核实 CE 执行路径**）

- **STATUS_STUCK（缠绕）**：U08（`27203b9`）报告 §3 登记——当前是"50% 挣扎、挣扎时 20% 破网"的近似；
  CE Time.c 约 :319 施加 3–7 计数，Movement.c 约 :1402–1420 / Monsters.c 约 :3773–3784 尝试移动时递减，
  攻击占格目标时的挣脱顺序、随位移清理、`hitProbability` 对 STUCK 目标 100%（U13 已接判定入口，核对载体）
- **STATUS_DONNING（穿甲）**：换甲期间的防御/计时（CE 装备护甲路径）
- **STATUS_ENRAGED（狂怒）**：施加来源（CE `moralAttack` 等）与效果
- **STATUS_LIFESPAN_REMAINING（寿命）**：召唤物/谱影寿命——W-16 刀刃在 CE 里"1 HP 无寿命"（W-16 已核），
  请确认 CE 里**谁**真的有寿命，不要给刀刃加寿命
- 每项：若 CE 中无活跃来源（像 U14a 的魔法恐惧），**只建载体并如实登记**，不编造来源

## 2. 与既有代码的关系

- 网/藤的旧近似（Promotion.breakEntanglingTerrain 等）按 CE 替换，**U08 的地形行为测试只回归**
- 护甲装备现有流程（B 系列、armor_model）只回归；穿甲只加 CE 规定的状态与其消费者
- U14a 建的 `maxStatus` 子集按需扩展到本轮四项，**不推广到全状态表**

## 3. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- ❌ 不改 SEARCHING / 营养
- 📌 用户裁决：不做旧存档兼容

## 4. 教训（已连续四十八轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**"解状态"不是一个动作，是逐项规则**；**前轮的留痕登记可能正是为你预约的**（U08 §3、U13 §7、U07 §8）；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
📌 **CE 里没有活跃来源的状态，只建载体，不编造来源**（U14a 魔法恐惧先例）。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单，每条改义给 CE 依据。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证。）

## 7. 报告

写到 `ai_docs/reports/u-14b.report.md`：每个状态的全链表（格式同 U14a §2）；来源清单（含无活跃来源的登记）；
替换的旧近似与依据；反查与两份清单；撞红处置；§3 自检 + 最终复跑声明；清单外改动与新发现登记。
