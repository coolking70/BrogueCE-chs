# U03：整局与跨层存档

> **本地执行（Mac 轨）。X-0 路线。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.2 U03 行、§3.1 K01/K03 是权威规格**（`153dbb5`）：

> **U03 整局和跨层保存**：levels、计量、pending 坠层/跟随、层时间/气味、全局关系；K01/K03｜前置 U01/U02｜
> R: G、Grid/Scent、M、App.vue；S: levels、meteredItems、foodSpawned、goldGenerated、spawnFuse、pendingFallenByDepth、leaderId｜
> p1_31_35、b_4a/b、c_5、W 持久化/生成夹具；**D1→D2→存读→D1 与连续局等价**，当前层/缓存层/休眠…

K01：toSnapshot 只有当前层；meteredItems/foodSpawned/goldGenerated、spawnFuse 等未入档。
K03：普通换层缓存未保存 scent；重访没有从缓存恢复；普通盟友跨梯/离层时间推进未齐。

📌 用户裁决：**不做旧存档兼容**（schema 直接改）；保留现状不是卡点。

## 1. 前轮已完成的合同（请直接复用）

- **U01**：实例快照统一合同 `EntitySnapshot`（AST 字段覆盖守卫）
- **U02a**：两流 RNG 状态、waypoint 产物入档；**U02b**：层种子表、visited、`currentLevelDepth`（其报告 §7 是给 U03 的接口）
- **U10**：`clearCorpseTargetOnLevelChange()` 原语——U03 接普通 `monsterEntersLevel` 时调用（U10 报告 §2 登记）
- **U00**：run 级状态枚举表（哪些是 run 级、需要进整局存档）

## 2. 范围

1. **全部 levels 缓存进档**：每层 grid 四层地形/旗标/记忆、实体图（活动/休眠/携带）、地面物品、scent、waypoint、
   层离开时间（CE `levels[n].awaySince` 一类——请读 CE `startLevel` 的离层/回层时间补偿）
2. **run 级全局**：meteredItems、foodSpawned、goldGenerated、spawnFuse、奖励房计数、pendingFallenByDepth、鉴定/外观表、统计……
   以 U00 的枚举表为清单，逐项确认进档
3. **跨层关系**：leader/follower 跨层（CE 盟友跟随上下楼梯 `monstersFollow` 一类路径——读 CE 执行路径）；
   本轮只做到**存读档后关系与跟随行为等价**；若 CE 普通跨层跟随在 web 根本没实现，**实现它或明确登记**（按工作量判断，大就拆 U03b）
4. **回层时间补偿**：CE 回到旧层时按离开时长推进环境/怪物——U02b 登记为未实现（环境预热/补跑）；本轮决定做或登记

## 3. 验收（X-0 原话 + 补充）

- **D1→D2→存档→读档→回 D1** 与 **不存档连续玩** 的完整状态逐字段等价（含两流 RNG）
- 多层缓存、坠层待落怪、休眠怪、携带物、盟友跟随，存读后各自同效
- 存档体积与读写耗时记录（全 26 层）

## 4. 硬约束

- 若本轮实现回层补偿/跟随会改变生成或交互 RNG：先归因再重捕获（生成变化不是卡点）
- 📌 **任务书与 CE 冲突时以 CE 为准并在报告里写明**

## 5. 教训

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**"恢复得像"不等于"恢复得一样"——用逐字段比较证明**；**前轮的留痕登记可能正是为你预约的**（U00/U01/U02a/U02b/U10）；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；**反查闭包含 p1_30 与所有读源码守卫**。

## 6. 门禁与报告

R∪S 自查 + `npm run build` + `npm run test:drift`；最终复跑声明。报告写到 `ai_docs/reports/u-03.report.md`：
存档 schema 全字段表；run 级清单核对；跨层关系与跟随；回层补偿决定；等价性证明；体积/耗时；反查与两份清单；撞红处置；给 U27（录像）的接口。
