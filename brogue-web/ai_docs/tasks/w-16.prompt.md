# W-16：召唤刀刃

> **本地执行（`codex exec --worktree`）。W 链效果收口第九轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-16 行是权威规格**（`823eb0e`）：

> **W-16 召唤刀刃**｜CONJURATION：落点附近 `floor(3E/2)` 把 spectral_blade、
> 合格格、**绑定玩家/不追踪**、首行动等待与死亡清理；conjuration 杖｜
> 依赖 W-3、W-5｜生成流：**否；施法时会用 RNG**｜
> 改动面 G、M、召唤 helper；有新增关系字段则含快照 → R(实际文件)+S(M,B,C)｜
> 撞红预期：P4-2 horde 召唤、P4-4 分裂计数、P4-3 消魔死亡、V-2b-5、跨层/存档；
> **不凭一条 Blade 飘字验收**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. 这是 W 链里最典型的空壳——**验收标准是实体，不是文字**

W-0 登记 `staff_of_conjuration` 的分支"**仅文案/飘字**"。
W-2（`53e9a27`）做 autoID 时因此把它判成 **`false`**，原话：

> 现有 conjuration 只有文案/飘字，明确 false……**CE true 位于实际召唤后**（`:5494-5514`）

⇒ 本轮做完，**autoID 应该从 false 变成 true**——而且必须是因为**真的召唤出了刀刃**才变。
W-0 原话"**不凭一条 Blade 飘字验收**"：测试要断言**网格上真的多出了 spectral_blade 实体**、
数量对、位置合格、行为对，不能只断言消息日志里出现了"Blade"字样。

## 2. 线索（**全部请自行核实**，验收方的 CE 事实可能是错的）

- 刀刃数量 `floor(3E/2)`——`PowerTables.c` 附近可能有 `staffBladeCount`，请自行核
- "**绑定玩家 / 不追踪**"：刀刃是玩家的临时盟友，**跟随玩家但不主动追踪敌人**？具体语义请回 CE 查
- "**首行动等待**"：召唤出来的第一回合是否不行动，按 CE
- "**死亡清理**"：刀刃有寿命，到期消失——**消失不是"死亡"**，别触发死亡掉落/经验/DF
  （W-10 刚做好"死亡只结算一次"，这里要确认刀刃的消失走的是不是同一条路）

## 3. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
  （W-0 标注"施法时会用 RNG"——那是交互期，不是生成期）
- ❌ **不增种类、不改 frequency**
- ⚠️ **P4-2 已有 horde 召唤**（怪物召唤同伴）、**P4-4 有分裂计数**——召唤 helper 若共用，
  **明列怪物行为变化**
- ⚠️ **P4-3 消魔死亡**：刀刃是魔法生物，被消魔会怎样？按 CE
- ⚠️ **跨层与存档**：玩家下楼时刀刃跟不跟？存档里怎么存？按 CE，并给旧档兜底

## 4. 教训（已连续二十三轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**；
**复用前轮原语发现不够通用就如实登记前轮缺口**；
**前轮的留痕登记可能正是为你预约的**，动手前 grep 相关注释；
**照搬上一轮的做法前，先确认 CE 在这里是不是也这么做**；
📌 **公式有定点/舍入细节时，编译 CE 原函数取黄金值**（W-15 用 clang 发现浮点 182 ≠ CE 181）。

## 5. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定。`scripts/w*-test-scope.mjs` 可复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 6. 门禁

按 §5 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。
若遇超时，照 W-12 的做法处理（串行重跑、如实区分、不改超时上限）。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 7. 授权反驳

W-0~W-15 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 8. 报告

写到 `ai_docs/reports/w-16.report.md`，含：

1. 刀刃数量公式、落点、合格格的 CE 依据；`staffBladeCount` 核实
2. **"绑定玩家/不追踪"、"首行动等待"的 CE 语义与落地**
3. **刀刃寿命到期的清理路径**，证明**不走死亡结算**（不掉落、不给经验、不触发死亡 DF）
4. **autoID 从 false 变 true 的证明**，且是因为真实召唤（W-2 交接）
5. **实体级验收证据**：网格上真实刀刃的数量/位置/行为，而非飘字
6. 消魔、跨层、存档的处理与旧档兜底
7. 若共用 P4-2/P4-4 的召唤/分裂 helper，**明列怪物行为变化**
8. §2 线索的核实结果
9. §5 反查过程与实际跑的文件
10. 撞红清单与处置
11. §3 自检 + **§6 最终复跑声明**
12. 清单外改动申报
