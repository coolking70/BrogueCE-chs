# U17c：DF 载体第二族——触发/陷门/暗杆（⚠️ 可能移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U17 第三轮（U17a 统一事务、U17b 首族已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U17 行、§3.3 首族、§3.1 K19/K28 是权威规格**（`153dbb5`）：

> §3.3 触发/陷门/暗杆：**154 MACHINE_PRESSURE_PLATE_USED、17 和 152 TRAP_DOOR、95 WALL_LEVER、144 MACHINE_TRIGGER_FLOOR_REPEATING**。
> 各族恢复须把**数据起点、地形三链、DF 后继、最终消费者、可解性**一起闭合；每族 铺装→实体疏散/落物→晋升→后继→可解/显示；
> **禁止一次删完 30 条缺口登记**——本轮只动这 5 条；CE24 暗杆机器仍资格退池的，本轮只闭合载体，机器回池留 U19

## 1. 范围（**全部请自行核实 CE**）

- 这 5 个 DF 的 CE 目录条目（Globals/GlobalsBrogue 的 dungeonFeatureCatalog 与对应 tileCatalog 行）：tile、层、概率、flags、后继、描述/光
- 每条的**触发来源**（压力板踩踏、陷门机关、拉杆交互、可重复触发地板；接线 wired 晋升链）与**最终消费者**，全部走 U17a 统一事务
- 地形三链（tile 属性 → 晋升 → 显示/外观），目录、外观黄金表按 CE 源码生成扩展
- 若其中某条的上游触发来自后续族/未实现系统，登记而不伪造触发
- 这 5 条从 `DF_MISSING_TILES` 登记中移除（仅在闭合后）；其余 20 条不动
- 若激活后 autoGen / 生成期会用到（如陷门）→ 生成流变化

## 2. 生成流

先 drift 记录 → 单变量归因（每条 DF 一段）→ 独立守卫（每条"触发→最终格/实体状态"）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**地形名出现 ≠ 可交互**——每条要有用户可触发的端到端证据；
**反查闭包含 p1_30、U24、U01/U03 合同守卫、c_4a/b/c、c_7、r_1、f/g、V、W-13/14、U17a/U17b 与所有读源码守卫；陷门坠层复用 U17b 物品坠层队列与既有玩家/怪物坠落**。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-17c.report.md`。
