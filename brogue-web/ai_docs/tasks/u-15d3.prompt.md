# U15d-3：武器/护甲符文生成映射按 CE（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U15d 第三轮（U15d-1 武器符文效果、U15d-2 护甲符文效果已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U15d 行、§3.1 K24 是权威规格**（`153dbb5`）：

> **U15d 武器护甲符文**；K24｜**逐符文小轮：目标物种/几率/附魔、谱影和消魔、负面符文生成、持久化**
> K24：**缺 W_MULTIPLICITY/SLOWING/PLENTY、A_MULTIPLICITY/BURDEN/VULNERABILITY/IMMOLATION 等生成映射**（ItemLoader 约 :347–375）

## 1. 范围（**全部请自行核实 CE**）

- CE `makeItemInto` WEAPON/ARMOR 分支的符文生成（Items.c）：符文几率、好/坏符文分池（`goodRunic`/`badRunic` 按附魔正负）、
  武器符文受武器种类限制（投掷武器/某些武器不得带某些符文，按 CE 实际条件）、slaying 的 `vorpalEnemy` 物种类别抽取、
  护甲 immunity 的 `vorpalEnemy`、负面符文强制诅咒/负附魔的关系、`ITEM_RUNIC` 位
- web `ItemLoader.ARMOR_RUNIC_BY_CE_INDEX` / 武器对应表中的 `null` 缺位补齐为 U15d-1/2 已实现的效果；CE 索引顺序与抽签 RNG 消费同构
- 机器 Q 族（`MF_REQUIRE_GOOD_RUNIC`，U05）与普通出生共用同一符文生成
- 详情/鉴定揭示按已实现的 U15d-1/2；U01 字段合同；不做旧存档迁移

## 2. 生成流

先 drift 记录 → 单变量归因（武器好符文、武器坏符文、护甲好符文、护甲坏符文、vorpalEnemy 抽取各一段）→ 独立守卫（CE C oracle 抽签对照）→ 一次重捕获。
黄金 trace（UR2/UR4）若因正当行为变化翻红，按 U15b-2 同法**单变量归因后重录并登记**。📌 用户裁决：生成变化不是卡点。

## 3. 约束

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**反查闭包含 p1_30、U24、U01/U03、U05、U15d-1/2、armor_runic*、b_4a、W-20/23、U16、UR2/UR4 trace 与所有读源码守卫**；不产生 CRLF。

## 4. 门禁与报告

R∪S + build + test:drift；**全量 npm test 完整跑完**；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-15d3.report.md`。
