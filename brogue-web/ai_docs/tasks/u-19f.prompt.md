# U19f：电晶/虫道机器回池与剩余 autoGen 链（U19 收尾）（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U19 第六个、收尾子轮（U19a–e 已合并；U17f 已闭合虫道载体）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U19 行与其后子轮表（约 :197、:203）、§3.1 K28/K32 是权威规格**（`153dbb5`）：

> **U19 蓝图剩余算法/机器玩法**；K28/K32｜前置 U05/U17（已合并）、视图需 U18a（已合并）｜生成流：**直**｜
> R: BlueprintEngine/Architect/AutoGenerator、G、数据；S: IN_VIEW_OF_ORIGIN、pendingItems、pendingMonsters…｜
> **按子轮分别验，先闭包后解退池；每台必须有"用户触发→结果"的证据，不能只点亮身份覆盖**
> **U19f** 电晶/虫道及剩余 autoGen 植被火桥链（逐闭包独立）

## 1. 范围（**全部请自行核实 CE**；逐闭包独立归因）

- **CE52 电晶炮塔**：`blueprintQualifies` 退池理由"web 不能撞动不可通行的 TURRET_LEVER；领养钥匙在阻塞笼格被拒"——后者已被 U19e 删除；
  核对 CE 撞杆（bump）激活不可通行 lever 的入口（Movement.c 相关），补齐后解除退池，整机"撞杆→炮塔/电晶→取奖励"可解
- **CE55 虫道**：退池理由"lever/active-tunnel DF 链缺 tile"——U17f 已闭合 DF191/192；解除退池，整机"搜杆→撞杆→隧道打开→虫群→取钥匙"可解
- **剩余 autoGen**：逐行对照 CE `variants/GlobalsBrogue.c` autoGeneratorCatalog 全部 49 行与 web `AUTO_GENERATOR_CATALOG`，
  找出仍为 no-tile/未接线/被 carrier 过滤的行（X-0 §3.3 所述植被/火/桥链等），逐行按 CE 接回；每行"生成→玩家可交互"证据
- 结束时：web 蓝图目录中**除 D2 自创退池（RETIRED_INVENTED_BLUEPRINT_IDS）外不应再有显式退池**；autoGen 表与 CE 49 行逐行一致（或逐条写明 CE 依据的差异）

## 2. 生成流

先 drift 记录 → 单变量归因（CE52、CE55、每条 autoGen 行各一段）→ 独立守卫（每台整机可解；去掉修复即红）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**反查闭包含 p1_30、U24、U01/U03、V 全系、b2_transcription、blueprint_center、c_8、U05a、U17a–f、U25 观测守卫 与所有读源码守卫**；
可用 U25 的 `scripts/u25-machine-inventory.mjs` 追踪机器产物。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-19f.report.md`。
