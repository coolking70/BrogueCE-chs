# U08：蛛网与古灵藤蔓的 DF 施法

> **本地执行（`codex exec --worktree`）。X-0 路线；W-22 学习的前置之二。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.3 U08 行、§3.1 K07 是权威规格**（`153dbb5`）：

> **U08 蜘蛛网与古灵藤蔓 DF 施法**（W-22 前置二）；K07｜前置 **U17 必要副作用**、U18a｜生成流：间｜
> R: Bolt.ts、M、G、DF/TerrainCatalog；S: SPIDERWEB、ANCIENT_SPIRIT_VINES、DF_SPIDERWEB、DF_ANCIENT_SPIRIT_VINES｜
> **effect=null 拒绝留痕翻正**；**路径/终点不同语义**、藤蔓占格/死亡、网接触行为；
> **分清非可学习与可学习项**（SPIDERWEB 不可学、VINES 可学），不能让学习盲装无出口能力

K07：Bolt.ts 约 :212–213 的 SPIDERWEB/VINES `effect=null`，经 Monster :117 与 Game :5026 入口早退；
CE Globals.c 约 :525–526、:684–685 及 variants/GlobalsBrogue.c 的 boltCatalog。

## 1. 线索（**全部请自行核实**，读到执行路径）

- boltCatalog 里两者的 `pathDF` / `targetDF` 列：**哪个沿路径铺、哪个只在落点铺**；bolt 的 `BF_*` 旗标
  （是否穿透、是否在障碍前停、BF_NOT_LEARNABLE）
- CE `zap()` / `updateBolt` 里 pathDF 的铺设时机（每格一次？避开施法者格？）与 targetDF 的落点判定
- 藤蔓地形（ANCIENT_SPIRIT_VINES，Globals.c 约 :525）：是否是"会动/会攻击"的地形，还是生成了**怪物**？
  其 promote/燃烧后继（`DF_PLAIN_FIRE`、`DF_ANCIENT_SPIRIT_GRASS`）与生命周期
- 网（SPIDERWEB）的接触行为：`T_ENTANGLES`、`STATUS_STUCK`（**STUCK 计数属 U14b**，若 web 仍是近似，登记，不在本轮补状态）
- 施法 AI：蜘蛛/古灵何时施放（`monstUseBolt` 的目标资格、`specificallyValidBoltTarget` 对这两者的特殊规则）
- **U17（DF 通用副作用）尚未做**：若铺网/藤需要的疏散/刷新/聚怪等副作用缺失，**只接本效果必需的部分并登记**，
  不扩成 U17

## 2. 验收标准

- 蜘蛛真实施放网：路径/落点地形按 CE，目标被缠绕（用现有近似载体）、不穿透行为按旗标
- 古灵真实施放藤蔓：落点/路径、藤蔓后续行为与燃烧/晋升
- 学习资格：SPIDERWEB 仍被 BF_NOT_LEARNABLE 排除；VINES 现在有执行出口（给 W-22 恢复用），**本轮不开学习**
- `p4_1b` 等"effect=null 拒绝"留痕按 CE 翻正，逐条给依据

## 3. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- ❌ 不做学习（U11）、不补 STUCK 计数（U14b）、不做通用 DF 副作用（U17）
- 📌 用户裁决：不做旧存档兼容

## 4. 教训（已连续四十四轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**注释写的是意图，执行路径里可能有例外**；**前轮的留痕登记可能正是为你预约的**（W-22 §1/§4、U18a §6、U07 §8）；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；**做不到忠实时，缩小范围并明列**；
📌 **"看起来一样的两个效果"（网/藤）可能学习资格、铺设语义都不同**。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单，每条改义给 CE 依据。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证。）

## 7. 报告

写到 `ai_docs/reports/u-08.report.md`：两个 bolt 的 CE 全路径（pathDF/targetDF/旗标）；藤蔓/网的地形生命周期；
施法 AI；学习资格；需要但缺失的 U17/U14b 部分登记；留痕翻正依据；反查与两份清单；撞红处置；
§3 自检 + 最终复跑声明；W-22 恢复还差什么。
