# U18：深水游泳/卷物、网与开笼的消费者

> **本地执行（Windows 轨）。X-0 路线。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.3 U18 行、§3.1 K21 是权威规格**（`153dbb5`）：

> **U18 深水/网/开笼消费者**；K21｜前置 U18a/U14；网 DF U08｜生成流：间｜R: G、M、Grid、Item、关系 helper；
> S: DEEP_WATER、STUCK、SUBMERGED、keyMatchingEntry、becomeAllyWith、itemsAt｜
> **允许游泳不复活自创淹死，卷物及免疫、陷网挣脱时间、开笼统一关系；保存后同效**

K21：`canMoveTo` 仍排深水；CE 深水允许游泳并另处理卷物（掉落背包物品）。蛛网为近似；钥匙开笼仍有旧关系写法
（Game 约 :9711–9723、:9234 起，对 CE Time.c 约 :557–577、Monsters.c 约 :1313 起）。

前轮已完成：**U18a**（判据合同 TerrainRules，其报告 §6 给 U18 的接口）、**U14b**（STUCK 计数已按 CE）、
**U08**（网/藤 DF）、**W-17**（`becomeAllyWith` 统一转盟友）。

## 1. 线索（**全部请自行核实**，读到执行路径）

- 玩家进深水：CE 是否需要确认、游泳时背包物品被冲走的规则（Time.c 约 :557–577：几率、哪些物品、掉在哪、是否漂流）、
  护甲/重物影响、`T_IS_DEEP_WATER` 对怪物的回避与水栖怪（MB_SUBMERGED 若仍无载体——U18a/U07/U09 都登记过——
  本轮**可以建**潜水载体，但只接与本轮消费者直接相关的读写）
- 淹死：CE 深水**不直接杀死**玩家（web 若有自创淹死规则，删除并登记依据）
- 网：U14b 已按 CE 计数；本轮只处理剩余消费者（例如在网里被攻击、投掷物、网对物品的影响）——若 U14b 已全覆盖则写明
- 开笼：钥匙开笼路径（`keyMatchingEntry` 等）统一走 `becomeAllyWith` / 魔法释俘 helper（W-17、U15a 已有），
  删除旧的直接改关系写法

## 2. 验收

- 玩家游泳：多个背包状态下物品冲走的概率/选择/落点与 CE 同构（固定骰），存读档后同效
- 怪物深水回避/水栖分支按 CE
- 开笼：钥匙、魔法两条入口得到**同一**关系状态
- 删除的自创规则逐条给 CE 依据

## 3. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- 📌 **任务书与 CE 冲突时，以 CE 为准并在报告里写明**；不做旧存档兼容；保留现状不是卡点

## 4. 教训

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
📌 **反查闭包要包含 i18n 门（p1_30）与源码扫描类守卫**——改/删文案键时死键要移入 zh_CN.legacy.json（U15f 漏过）。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定（**必须包含 p1_30_i18n_gate 与所有读源码的守卫**），给出两份清单。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`；最终复跑声明「这是最终状态下的运行结果，不是中途快照」。

## 7. 报告

写到 `ai_docs/reports/u-18.report.md`：CE 游泳/卷物/水栖/开笼全路径；删除的自创规则与依据；潜水载体（若建）；
反查与两份清单；撞红处置；§3 自检 + 最终复跑声明；清单外改动与新发现登记。
