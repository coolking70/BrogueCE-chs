# U02a：随机数状态无损存档 + 64 位种子输入（不改生成算法）

> **本地执行（`codex exec --worktree`）。X-0 路线；U02 的第一次交付。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.2 U02 行、§3.1 K04 及 K01 随机部分是权威规格**（`153dbb5`）：

> **U02 RNG 与分层种子合同**：双流完整状态/计数保存、种子宽度与 CE 每层 seed 隔离｜
> R: Random.ts、G、App.vue/种子输入、generation_baseline、Random/p2_0；S: seed、levelSeed、RNG_SUBSTANTIVE、getState、seedRandomGenerator｜
> **分两交付：先可无损恢复当前算法状态，再单独切 CE 种子/层隔离**；…**不能把两次漂移混捕**

K04：每层 levelSeed、64 位 seed、完整 RNG 状态缺失；Random.ts 约 :57–76 只用低 32 位；G 约 :1179–1300 无隔离。
K01：loadSnapshot 重播种（读档 RNG 计数 51→0）。

📌 用户裁决：**不做旧存档兼容**；**地图大面积变化不是卡点**（2026-09-25）——但本轮**本来就不该**改生成。

## 1. 本轮（U02a）范围

1. **两条流（SUBSTANTIVE / COSMETIC）的完整状态**进存档：内部状态、`randomNumbersGenerated` 计数等，
   读档后**逐比特恢复**——读档后下一次抽取与"不存档直接继续"完全相同
2. `loadSnapshot` 不再重新播种（K01）；新局/读档/重放三条入口各自的播种规则写清楚（U00 已统一新局入口）
3. **64 位种子输入**：CE 支持 64 位 seed（`rogue.seed >> 32` 分支）；web 当前截断到低 32 位。
   本轮只做**输入与存储**不丢高位；**生成仍用当前算法**——若当前算法只能吃 32 位，写清楚 U02b 如何接
4. 显示：种子在 UI 的展示/输入（App.vue/种子输入框）不截断
5. ❌ **不做**每层 levelSeed 隔离（U02b）——那一轮才会改生成流

## 2. 验收标准

- 任意时刻存档→读档→继续 N 步，与不存档继续 N 步：地图、实体、两流状态**逐字段相等**（多个时刻、含战斗/掷骰密集段）
- 读档后录像 seek（U00 登记的问题除外）不因重播种而分叉
- 64 位种子：输入 `2^40+7` 之类，存读后原样；当前算法下与低 32 位种子的关系写明
- `test:drift` 绿，**基线不重捕**（本轮不改生成）

## 3. 硬约束

- ❌ 不改生成算法、不做层种子隔离
- ❌ 不做整局跨层存档（U03）

## 4. 教训（已连续四十九轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**基线变绿 ≠ 生成正确**；**前轮的留痕登记可能正是为你预约的**（U00 §9、U01 §3、U10 §3 都提到"整局 load 重播种属 U02"）；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
📌 **"恢复得像"不等于"恢复得一样"——用逐比特比较证明**。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证。）

## 7. 报告

写到 `ai_docs/reports/u-02a.report.md`：两流状态结构与存档字段；三入口播种规则；逐比特续跑证明；
64 位种子的存储/显示与当前算法关系；**给 U02b 的精确施工说明**（CE levelSeed 生成、`rand_64bits`、每层
`seedRandomGenerator(levelSeed)` 与 `oldSeed` 回切的完整路径、预计漂移面）；反查与两份清单；撞红处置；
§3 自检 + 最终复跑声明。
