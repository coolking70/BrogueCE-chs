# U25：机器产物追踪与性能观察（不移动生成流）

> **本地执行（Windows 轨）。X-0 路线。观测工具轮：不得消耗 RNG、不得改变任何生成/运行结果。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U25 行、§3.1 K33 是权威规格**（`153dbb5`）：

> **U25 机器产物追踪与性能观察**；K33｜可提前独立｜**无：观测不可耗骰；否**｜R: 现有 scripts、headless harness、BlueprintEngine/G 观测接口；
> S: featureSpawns、machineNumber、adoptedItem、MAXIMIZE、REPEAT_UNTIL_NO_PROGRESS｜定位/时间阈值守卫可能红；
> **seed/depth/CE id/feature/来源→最终格/怪/物可追踪，关观测同指纹**；串行重复计时先查慢点，**不能把历史超时写当前性能缺陷**

## 1. 范围

- **追踪接口**：可选的观测钩子（默认关闭，零开销），记录每台机器：seed、depth、CE 蓝图 id、machineNumber、每个 feature 的请求→结果
  （铺装格、生成的怪/物实例 id、领养/外包来源、MF_MAXIMIZE/REPEAT_UNTIL_NO_PROGRESS 的迭代次数、失败/回滚原因）
- **一致性证明**：开/关观测下，test:drift 的 4 seed × D1–26 指纹与完整 RNG 状态逐层相同（独立守卫）
- **报告工具**：一个脚本输出某 seed/depth 的机器产物清单（JSON + 可读文本），能回答"某件物品/某只怪来自哪台机器哪个 feature"
- **性能观察**：串行、重复（≥3 次）计时 generation 全流程与慢测试文件，定位热点函数；**只报告，不优化**（优化另立任务）；
  说明全量门禁里长测 900s 超时的实际原因（CPU 争抢 vs 真实耗时）
- 不改生成逻辑、不改现有守卫阈值

## 2. 约束

📌 **撞上守卫时改代码，不改守卫**；**观测不可耗骰**；**不得改变 test:drift**；**反查闭包含 p1_30、U24、U01/U03、U05a、V 系列、blueprint_center 与所有读源码守卫**；不产生 CRLF。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明。报告 `ai_docs/reports/u-25.report.md`（含性能数据表与热点）。
