# W-5：法器实例生成模型

> **本地执行（`codex exec --worktree`）。**
> ⚠️ **本轮是夜跑里唯一会移动生成流的一轮。见 §2。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-5 行是权威规格**（`823eb0e`）：

> **W-5 法器实例生成模型**｜只修既有种类的 staff E/charges、wand range 抽签、
> 实例保存/旧档兜底；**不增种类、不改 frequency**。E 与当前次数分离；
> **旧档按原容量给确定性迁移值**｜依赖 W-1｜生成流：**是：新增生成期掷骰**｜
> 改动面 L、I、G 快照与独立实例 helper → R(L,I,G)+S(Q,G)｜
> 撞红预期：**B-1a 固定 `?/2` 等夹具**、snapshotQuantity、itemFlavors、
> b_4a/b_4b、机器配发、**生成基线**；**迁移不抽 RNG**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. ⚠️ 本轮移动生成流——重捕获是最后一步

W-0 §2.1 的原话：

> W-5 恢复实例充能抽签，**会移动生成流**，即使还没有增加种类。

⇒ **重捕获 `generation_baseline.json` 放在所有改动之后**，之后 `test:drift` 必须绿。

**先测后改（沿用 B1 / 9e-1 的做法，那两轮做得很好）**：
改动**之前**先跑多 seed × D1-D26，统计法器（staff/wand）的**生成数量与分布**；
改完用**同样样本**复测，给前后对照表。

⚠️ 沿用既有归因纪律：
- **不要把差额算成战果**
- **未观察到的零值不能推断其 RNG 足迹为零**（B1 教训）
- 任何既有物品类别从非零掉到零，**查清原因再定是缺陷还是合法漂移**

## 2. 硬约束

- ❌ **不增种类**、**不改 frequency**（W-24~26 才做，且用户要求留到他在场）
- ❌ **不实现任何效果行为**（W-8~W-23）
- ❌ **自然回电与充能生命周期留 W-6**——本轮只做**初始值**
  （W-2 已守住这条边界，别在本轮越过去）
- ⚠️ **旧档迁移不许抽 RNG**（W-0 点名）：按原容量给**确定性**迁移值
- ⚠️ **E（附魔等级）与当前次数分离**（W-0 点名）——不要把两者混成一个字段

## 3. 教训（已连续十二轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**。

## 4. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定。
⚠️ **本轮移动生成流**，所以除闭包外**必须带上对生成结果取样的观测类测试**
——验收方在 9e-1 就是漏了这类（`c_4b` / `g_2` / `c_5_fall_subsystem`）
才跑出三条失败。请一并反查生成/物品分布类测试。

⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 5. 门禁

按 §4 自查清单 + `npm run build` + 重捕获后的 `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 6. 授权反驳

W-0~W-4 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 7. 报告

写到 `ai_docs/reports/w-5.report.md`，含：

1. staff E/charges、wand range 抽签的 CE 依据与落地
2. **§1 的法器生成前后对照表**（本轮最重要的证据）
3. **旧档迁移方案**，并证明**迁移路径不抽 RNG**
4. **E 与当前次数分离**的落点
5. **§2 边界证明**：怎么确保没做 W-6 的自然回电、没增种类、没改 frequency
6. §4 反查过程与实际跑的文件（**含观测类测试怎么补的**）
7. 基线重捕获：变化层数/字段数 + 与 §2 对照表的呼应
8. 撞红清单与处置（B-1a 的固定 `?/2` 夹具怎么处置的）
9. **§5 最终复跑声明**
10. 清单外改动申报
