# G-3：气体效果与生物侧（F-0 §5.3 的第 10、11 条 + 五种未接效果）

> 只改"允许修改"清单里的文件；**不要执行任何 git 写操作**（`status`/`diff`/`log` 只读允许）。

**授权反驳条款有效**：CE 源码优先。你在前几轮连续纠正并改进过验收方——
F-0 重排整条 F/G 链、F-1 拒照抄 `promoteChance=500`、F-2a 指出照字面改
`igniteForced` 会让禁改文件翻红、F-2b 查明 `StatusId` 撞点不存在、
G-1 指出 `DF_GAS_FIRE` 是 SURFACE 层并抓到 `updateVolumetricMedia` 的无条件掷骰、
**G-2 揪出躺在死分支里三轮的 `ALL_DIRS8` 错抄**。**请继续，包括反驳本任务书。**

**先读**：`ai_docs/g_2_gas_df_wiring_report.md` **§九（给 G-3 的 4 条登记）**、
`ai_docs/g_1_gas_volumetric_report.md` §十一.6-8、
`ai_docs/f_0_fire_gas_survey.md` §5.3 第 10、11 条。
凡本任务书与它们冲突，**以实测为准并在报告里指出**。

---

## 一、本轮定位：气体侧的最后一块，形状同 F-2b

火侧走过一模一样的路：F-2a 做机制（蔓延/寿命）→ **F-2b 做生物侧**（状态机 + 免疫）。
气体侧现在是：G-1 做机制（体积/扩散/消散）→ G-2 做来源（DF 接线）→
**G-3 做生物侧**（效果判定与伤害）。

本轮之后 G 链收口，只剩 F-2c（爆炸）。

---

## 二、范围

### 1. 效果判定口径重裁（§5.3-10）

| | CE | web 现状 |
|---|---|---|
| 判定 | **站进即上状态、无阈值**，每回合 `max()` 刷新（`Time.c:421-497`，自己核对） | POISON 无阈值；CONFUSION/STEAM **密度 > 20** 才生效 |

那个 `20` 是 **0-100 时代的 web 自创参数**，G-1 换成 CE 体积量纲后
**语义已经漂移**（G-1 §十一.6 登记：体积口径下更容易被穿越）。
按 CE 重裁。

### 2. 伤害/回复改成比例（§5.3-11）

CE 是 `max(1, maxHP/15)` 的比例伤害（`Time.c:592-655`，自己核对行号），
web 是定值（蒸汽 1、creeping death 10）。
**后果**：大怪在 CE 里更怕毒气——这是玩法变化，要实测报出来。

### 3. 五种未接效果 + 各自的 tile 迁移（G-2 §九.1：**必须同轮做**）

`ROT_GAS`（恶心）、`STENCH_SMOKE_GAS`（恶心）、`PARALYSIS_GAS`（麻痹）、
`DARKNESS_CLOUD`（黑暗）、`HEALING_CLOUD`（疗养）。

**★ 先做载体盘点再决定做哪几个**（G-2 拿这条挡住了五条空转链，本轮同样办）。
G-2 §二的盘点表已给出各自载体：zombie 双列、麻痹药水改线、黑暗药水需新增、
血花草需新增地形。**有载体的接，没载体的只登记**——不要造空转链。

其中 **麻痹药水**（web 已有 `potion_of_paralysis`）改产 `PARALYSIS_GAS` 云，
参照 CE `Items.c:6994/8118`（`DF_PARALYSIS_GAS_CLOUD_POTION`，1000 体积、半径 4）。

---

## 三、明确不做（写显式留痕测试）

- **爆炸 `GAS_EXPLOSION`**——归 **F-2c**（下一轮）。
  G-2 已备好 `DF_EXPLOSION_FIRE` 条目（start 60 / decr 17，tile null）。
- **不改火侧任何东西**（蔓延/寿命/燃烧状态机）——反向哨兵。
- **不改 G-1 的扩散/消散/混合算法**、**不改 G-2 的 DF 接线与蒸汽源**
  ——同样是反向哨兵。
- 不碰 G-1 §八.6 的"不可见残气"（按 CE 原样）。

---

## 四、文件边界（硬约束）

**允许修改**：
- `src/engine/Core/Game.ts`、`src/engine/Status/statusConfig.ts`
- `src/engine/Environment/Gas.ts`
- `src/engine/Map/TerrainCatalog.ts`、`DungeonFeatureCatalog.ts`、
  `DungeonFeature.ts`、`Grid.ts`、`Promotion.ts`
- `src/locales/zh_CN.json`（仅增键；新文案必须走 i18n）
- **以下既有测试（本轮必然到期的断言），提前授权，不要为此停下**
  ——**清单由验收方 grep 生成**（凡断言气体/体积/GAS 层/中毒/幻觉/迷惑状态的
  文件全在内），并按 G-2 §九 的预告核对过：
  - `src/test/g_1_gas_volumetric.test.ts`、`src/test/g_2_gas_df_wiring.test.ts`
  - `src/test/c_4a_0_layer_model.test.ts`（**新增任何地形必打红那两张穷尽式
    `toEqual` 全量表**）
  - `src/test/c_4b_dungeon_feature.test.ts`、`src/test/c_4c_promotion.test.ts`
  - `src/test/f_1_fire_as_terrain.test.ts`、`src/test/f_2a_fire_mechanics.test.ts`、
    `src/test/f_2b_creature_burning.test.ts`
  - `src/test/p1_24_death_sink.test.ts`、`src/test/p4_4_split_kamikaze.test.ts`
  - **`src/test/monster_stats_effect.test.ts`**、**`src/test/hunger_regen.test.ts`**
    （比例伤害会动到怪物的受伤统计与回血口径——**这两个是重型长跑测试**，
    翻红先单跑复核）
  **限定**：只改因本轮行为变化而到期的断言，**不许放宽守卫性质**
  （穷尽式 `toEqual` 仍要穷尽、阈值不许松、火侧断言一字不许动）。逐条写进报告。
- 新增测试文件

**禁止修改**（违反即本轮作废）：
- `src/engine/Map/` 下其余既有文件、`src/engine/Generator/`、
  `src/entities/`、`src/components/`
- **`BrogueCE-master/` 下任何文件**
- 任何 `src/data/*.json`、`src/engine/Random.ts`、`vite.config.ts`
- **`src/test/fixtures/*`**、`src/test/harness.ts`
- 上面未列出的任何既有测试文件

> ⚠️ 若你判断"必须给 `StatusId` 加键才能接恶心/麻痹/黑暗"——
> `src/entities/Creature.ts` 在禁改清单。**停下来论证并给替代方案**
> （F-2b 用 `statusDurations` 逃生舱键解决过同型问题，可参照）。

---

## 五、★ 「照抄留形」的死分支必须逐字重核（2026-09-17 新规矩）

`project_conventions.md` 刚立的一条：留形代码**写它的那轮无法测它**，
抄写错误可以躺很多轮——G-2 就揪出了 F-2a 留形的 `ALL_DIRS8` 把 `[1,-1]`
抄两次、漏 `[1,1]`，躺过三轮。

**本轮凡激活任何留形分支（前几轮标注"结构性不可达"的），
必须逐字符重核 CE 并在报告里声明核过。**

---

## 六、门禁

1. **反向哨兵——火侧逐位不变**：F-0 探针复跑，`[FIRE-NAT]` 三条曲线须与
   F-2a §一逐位一致。
2. **反向哨兵——G-1 扩散/消散算法与 G-2 的气源不得改动**：同输入下扩散结果一致。
3. **新效果的实测**：阈值取消前后的中招率对比、比例伤害对不同 maxHP 怪物的
   实际伤害对比（**CE 的"大怪更怕毒气"要量化**）。
4. 载体盘点表 + "做了/没做/为什么"。
5. 决定性复验；`generation_baseline` 应仍绿；坏层闸门仍为 0。
6. `npm run build` 绿；`npm test` 全绿。

**全量门禁用 `npx vitest run --fileParallelism=false`**（串行）。
`monster_stats_effect` / `hunger_regen` 是重型长跑，翻红先单跑复核再下结论。

---

## 七、测试要求

1. 每条断言必须能在某个具体的错误实现下失败。
2. **对抗性测试至少七条**，必须含：
   - 阈值没取消（低密度气体不生效）
   - 伤害仍是定值而非 `max(1, maxHP/15)` 比例
   - 比例伤害的下限 1 被漏掉（小怪受 0 伤害）
   - 每回合 `max()` 刷新写成叠加或覆盖
   - 火侧被意外改动（回归哨兵）
   - G-1 扩散算法或 G-2 气源被意外改动（回归哨兵）
   - 无载体的效果被接成空转链
3. **反向验证（强制）**：至少四条真的改坏、贴真实失败输出、还原。

---

## 八、交付

报告写入 `ai_docs/g_3_gas_effects_report.md`，必须含：
- 效果判定与比例伤害的 CE 行号与复核要点；
- **载体盘点表**与取舍理由；
- **§六.3 的量化对比**（阈值取消、大怪 vs 小怪的毒气伤害）；
- 留形分支逐字重核的声明（§五）；
- 两个反向哨兵的逐位比对输出；
- 改了哪些既有测试断言、逐条说明为什么到期、守卫性质未放宽的论证；
- 对抗性测试与反向验证的真实失败输出；
- 给 F-2c 的登记清单。

**已知陷阱**：深水枚举成员是 `WATER_DEEP` 不是 `DEEP_WATER`，写错**不报错**
（vitest 走 esbuild 只剥类型不检查）。**任何让人困惑的失败，先跑 `npm run build`。**

**留痕规矩**：遇到留痕与文件边界冲突，**停下来申报**，
**不要把代码扭曲成扫描正则看不见的形态**——那是自造假绿。
