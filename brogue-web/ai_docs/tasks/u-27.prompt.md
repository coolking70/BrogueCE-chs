# U27：完整可复现命令日志与回放 OOS 校验（不移动生成流）

> **本地执行（Windows 轨）。X-0 路线。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U27 行、§2 D23、§3.1 K38 是权威规格**（`153dbb5`）：

> **U27 完整可复现命令日志**；K38｜前置 U00/U02/U03（已合并）；纳入已落实物品交互｜R: G、Input、App、InventoryOverlay、ReplayControls、目标/确认 UI｜
> 旧录像 JSON/seek 行为红；**所有实际用户入口同一命令边界**，取消/确认/目标/选物可重演，**逐事件位置/tick/RNG 校验、OOS 报错**；
> **CE 二进制录像兼容须另行决定，不默认为此轮范围**

K38：仅 handlePlayerAction 记录，InventoryOverlay 直接调用 quaff/read/equip 等及目标结果未完整记录；重放不校验存入的 tick/loc（G 约 :3071–3098、:3182–3195）。

## 1. 范围

- 盘点**所有改变世界状态的用户入口**（键盘、鼠标点击移动/旅行、背包菜单的喝/读/装/卸/丢/投/用、目标选择、确认对话框、鉴定/附魔选物、搜索、休息、楼梯、自动探索等），
  统一经一个命令边界记录（命令 + 参数 + 确认/取消/目标/选物结果）
- 回放：逐命令重放，每条记录后校验玩家位置、`absoluteTurnNumber`/tick、RNG 两流状态（按 CE `Recordings.c` 的 OOS 思路），不一致即报 OOS（带首个分歧命令号）
- 录制起点必须是真实新局（U00），读档后继续录制的语义写清（或明确拒绝）
- 旧录像 JSON 不兼容时直接拒绝加载（不做旧档迁移，项目裁决）
- **不在本轮**：CE 二进制 `.broguerec` 兼容
- 验收：对若干真实交互序列（含背包操作、目标、确认取消、楼梯、坠落）录制→回放→逐事件一致；故意篡改一条记录→准确报 OOS

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**不得改变 test:drift**；回放不得消耗额外 RNG；**反查闭包含 p1_30、U24、U00/U01/U02/U03 合同守卫、p2_0、ReplayControls/App 源码读取守卫**；不产生 CRLF。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明。报告 `ai_docs/reports/u-27.report.md`。
