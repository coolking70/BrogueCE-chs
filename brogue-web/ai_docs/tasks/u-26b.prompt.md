# U26b：结局结算、分数与高分榜（不移动生成流）

> **本地执行（Windows 轨）。X-0 路线；U26 按"深层/分数"拆两轮，本轮为分数轮（深层 D27–40/宝石/超胜利生成另交 Mac 轨 U26a）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U26 行、§2 D22、§3.1 K37 是权威规格**（`153dbb5`）：

> **U26 深层/终局/分数**；K37｜前置 U03/U04/U20（已合并）｜R: G、ItemLoader、App、GameEndOverlay、新高分持久层；
> S: amuletLevel、deepestLevel、lumenstone、itemValue、superVictory、score｜**金钱/物品价值逐 CE 核，不照抄旧简写得分公式；死亡/普通胜利/超胜利分别验**

D22：深度/击杀/固定物品加分与胜利×2 为自定；无高分保存（G 约 :1338、:10522–10551；GameEndOverlay.vue；App.vue:238–248；
对 CE RogueMain.c:1046 起、1255–1341，Items.c:8861 起 `itemValue`）。**CE 以物品价值逐件结算**。

## 1. 范围（**全部请自行核实 CE**）

- 删自创计分公式，按 CE `gameOver`/`victory`（RogueMain.c）结算：死亡=金币；普通胜利（携护符逃出）与超胜利按 CE 对背包物品 `itemValue` 逐件累加
  （护符、宝石 lumenstone 等价值按 CE 表），以及 CE 实际的倍数/加成（**以 CE 源码为准，不照抄旧注释**）
- 结局文本/死因描述按 CE（`killedBy`、"Killed by a X on depth N" 等结构，走 i18n）
- 高分榜：CE `saveHighScore`/`printHighScores` 语义（条数上限、排序、日期、描述），web 用浏览器持久化（localStorage，读写包 try/catch），
  与存档独立；GameEndOverlay 显示本局分与榜单
- 超胜利判定条件在本轮只接**结算**（若 D27–40/宝石尚未生成，用夹具构造携宝石胜利验证结算），生成交 U26a
- U27 录像：结局事件需进入命令日志/回放不出 OOS
- 不做旧存档/旧高分迁移

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
死亡/普通胜利/超胜利各有黄金值（含具体背包组合）；**不得改变 test:drift**；
**反查闭包含 p1_24 死亡归宿、p1_30、U24、U00/U01/U03、U20、U27 与 GameEndOverlay/App 源码读取守卫**；不产生 CRLF。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明。报告 `ai_docs/reports/u-26b.report.md`。
