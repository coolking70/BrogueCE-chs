# U19b：前厅 HAS_ITEM 与 pending 产物占用（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U19 第二个子轮（U19a 视图约束已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U19 行与其后子轮表（约 :197、:203）、§3.1 K28/K32 是权威规格**（`153dbb5`）：

> **U19 蓝图剩余算法/机器玩法**；K28/K32｜前置 U05/U17（已合并）、视图需 U18a（已合并）｜生成流：**直**｜
> R: BlueprintEngine/Architect/AutoGenerator、G、数据；S: IN_VIEW_OF_ORIGIN、pendingItems、pendingMonsters…｜
> **按子轮分别验，先闭包后解退池；每台必须有"用户触发→结果"的证据，不能只点亮身份覆盖**
> **U19b** 前厅 HAS_ITEM 与 pending 产物占用

## 1. 范围（**全部请自行核实 CE**）

- CE `buildAMachine`/`cellIsFeatureCandidate` 中 `HAS_ITEM`（及 `HAS_MONSTER`/`HAS_PLAYER`/`HAS_STAIRS` 等）占用判据：CE 物品在 feature 建造时**即时**生成并写入 pmap，
  后续 feature 的候选格据此排除；web 走"蓝图请求→populateLevel 延迟实化"，请求阶段的物品/怪物尚未写入格子
- 让 BlueprintEngine 在请求阶段把 pending 物品/怪物的目标格计入占用（同一机器后续 feature、子机器、外包/领养、前厅 vestibule 与父机器之间），
  判据与 CE 即时生成完全等价；不改变 populateLevel 的实化时机（那是 U19c）
- 受影响蓝图（带物品/怪物 feature 后仍有候选扫描的机器，尤其前厅+奖励房组合）清单与"同格双产物"反例
- U19a 登记的 CE15 护符实化缺口（`Cannot generate machine item: AMULET/*`）若与本轮 pending 物品路径相关则一并闭合，否则保持登记

## 2. 生成流

先 drift 记录 → 单变量归因（物品占用、怪物占用各一段）→ 独立守卫（同格双产物为 0；反例：去掉占用即红）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**反查闭包含 p1_30、U24、U01/U03、V 全系、b2_transcription、blueprint_center、c_8、U05a、U17a–f、U25 观测守卫 与所有读源码守卫**；
可用 U25 的 `scripts/u25-machine-inventory.mjs` 追踪机器产物。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-19b.report.md`。
