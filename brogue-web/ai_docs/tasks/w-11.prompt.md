# W-11：传送目标安置

> **本地执行（`codex exec --worktree`）。W 链效果收口第四轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-11 行是权威规格**（`823eb0e`）：

> **W-11 传送目标安置**｜TELEPORT：合法位置、占位、IMMOBILE、**释放俘虏**、
> 移动后的地形/视野；teleportation 魔杖｜依赖 W-4｜生成流：**否**｜
> 改动面 G、必要的移动 helper/M → R(实际文件)+S(B,M,D,Q)
> 并搜 teleport / fall / captive｜
> 撞红预期：B-1a teleport 使用、C-5 坠落、P4-5 抓取、V-2b-5 俘虏；
> **未找到位置不能把目标放进墙/另一个生物**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. ⚠️ 验收方的 CE 事实只作线索，**不作依据**

上一轮（W-10）验收方在任务书里写了一条错的 CE 事实（"吸毒气会获得毒状态"——
实际是毒地衣才会），你纠正了。所以本任务书**尽量不陈述 CE 事实**，
下面几条都只是**线索，请全部自行核实**：

- W-2 报告曾写 TELEPORT 在 `Items.c:5220-5227`「**没有设置 autoID**」
- W-0 提到"**释放俘虏**"——俘虏（captive）被传送时的处理，请回 CE 查具体语义
- **W-12 依赖本轮的"安置原语"**（W-0 §2.3 的 W-12 行："W-11 的安置原语"）
  ⇒ **本轮的安置函数要设计成可复用的**，W-12 的 blink 与反向 beckoning 会调它

## 2. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ **不增种类、不改 frequency**
- ❌ **未找到合法位置时，绝不能把目标放进墙或另一个生物**（W-0 点名）
  ——找不到位置的 CE 行为是什么，**按 CE 原码**
- ⚠️ 移动后的地形效果（落进陷阱、深水、熔岩、坠落）要和 **C-5 坠落**正确交互
- ⚠️ 若改到怪物移动或俘虏逻辑的共用入口，**照 W-9 的方式明列变化**

## 3. 教训（已连续十八轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；
**CE 里"看起来不一致"的数值先查是不是承重的**（W-7）；
📌 **验收方写的 CE 事实可能是错的，一律自行核实**（W-10）。

## 4. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定，按 W-0 提示**搜 `teleport` / `fall` / `captive`**。
`scripts/w*-test-scope.mjs` 可复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 5. 门禁

按 §4 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 6. 授权反驳

W-0~W-10 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 7. 报告

写到 `ai_docs/reports/w-11.report.md`，含：

1. 合法位置的判定规则与 CE 依据
2. **找不到位置时的 CE 行为**，并证明不会把目标放进墙/生物
3. IMMOBILE、占位、释放俘虏的处理与 CE 依据
4. 移动后地形/视野的处理，与 C-5 坠落的交互
5. **安置原语的接口设计**（给 W-12 复用），以及怎么保证它通用
6. §1 三条线索的核实结果（哪些对、哪些错）
7. 若改到怪物/俘虏共用入口，**明列变化**
8. §4 反查过程与实际跑的文件
9. 撞红清单与处置
10. §2 自检 + **§5 最终复跑声明**
11. 清单外改动申报
