# U21c：地形外观、法术动画残留与 flare 表现

> **本地执行（Windows 轨）。X-0 路线；U21 第三轮（U21a 可见性、U21b 光照口径已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.4 U21 行、§3.1 K16 是权威规格**（`153dbb5`）：

> K16：flare/固有生物光/侧栏怪物条目不齐；**力场等无 terrainAppearance 专用 case 而落黑色空白**
> （G 约 :2937–2971、Appearance 约 :189–192、:458–481，对 CE Light.c 约 :208–281、IO.c 约 :4650 起）。
> **射线末帧残留**仍是历史浏览器登记，本轮未重现。

## 1. 范围

1. **全地形外观覆盖**：枚举 `TerrainType` 全部成员（U08 新增的藤蔓/草、W-14 力场、晶墙、碎石等），
   每个都要有与 CE `tileCatalog` 一致的 displayChar/前景/背景色；加一个**覆盖守卫**：新地形没有外观就红
2. **法术动画残留**：复现（W 链浏览器登记过射线末帧残留）→ 定位 → 修复；动画结束后画面与状态一致
3. **flare**：CE `createFlare` 的光照闪烁（W-21 登记只做了 180ms 字形闪光，非 CE 半径 6 flare）——
   用 U21b 修正后的光照管线实现 CE flare 生命周期（半径、衰减、持续）；若需改光照模型的大块，登记
4. **侧栏怪物条目**（K16 点名）：CE 侧栏列出视野内怪物及其生命/状态条——**按 U21a 的可见性谓词**，只列玩家可见的

## 2. 验收与约束

- 浏览器截图：力场、晶墙、藤蔓、施法前/中/后、flare、侧栏怪物条（1280 与 390；若浏览器不可用，用 headless 渲染
  `render_game_to_text` + canvas 像素采样代替，并如实说明）
- ❌ 不移动生成流；📌 **任务书与 CE 冲突时以 CE 为准并写明**
- **撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
  **反查闭包含 p1_30、U24 硬串守卫与所有读源码守卫**；不产生 CRLF

## 3. 门禁与报告

R∪S 自查 + `npm run build` + `npm run test:drift`；最终复跑声明。报告写到 `ai_docs/reports/u-21c.report.md`。
