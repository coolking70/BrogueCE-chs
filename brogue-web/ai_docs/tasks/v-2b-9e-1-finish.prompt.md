# V-2b-9e-1 补完轮（3 条）

> **在你上一轮的 worktree 基础上继续。产物完好，不要推倒重来。**

## 0. 先说清楚：这 3 条是**验收方任务书的漏项**，不是你没跑

你上一轮跑的 27 个文件**正是任务书 §7 点名的全部**，最终复跑声明与 SHA-256 自证都成立。

但验收方本地全量门禁跑出 **3 条失败**，落在三个 **§7 根本没列**的文件上：
`c_4b_dungeon_feature`、`g_2_gas_df_wiring`、`c_5_fall_subsystem`。

**成因是验收方的判断错误**：9c/9d 的清单里都有 `c_4b` 与 `g_2`，9e-1 因为
"本轮不碰地形/DF"就删掉了。但**生成流一动，生成出来的多层组合与随机数账
就会变**，而这三个文件钉的正是这些。

⇒ **规律**：凡是移动生成流的轮次，都必须带上**对生成结果取样的观测类测试**，
与本轮是否改地形数据无关。

## 1. 三条（验收方已诊断，但 §4 授权你驳回）

### 1.1 `c_4b` F3：新叠层组合（机械，但要核合法性）

```
F3 留痕（C-6 已到期翻转）：生产生成的多层格仅限草/树形态 + GAS 恒空
AssertionError: expected [ 103, 115, 116 ] to include 6
  （:1214 处 expect([...]).toContain(cell.layers[L.LIQUID])）
```

白名单当前是 `FLOOR_FLOODABLE(103)` / `MACHINE_FLOOD_WATER_DORMANT(115)` /
`MACHINE_FLOOD_WATER_SPREADING(116)`，实际出现了 **`WATER_SHALLOW(6)`**。

**成因判断**：`ce_61_swamp` 从 74 台涨到 145 台，浅水格大量增加，草长在浅水上。

⇒ **回 CE 核实这个叠层是否合法**（`DF_GRASS` 的 `propagationTerrain` 允不允许
浅水？CE `Globals.c` 的 GRASS 条目与 DF 目录），合法就**按逐字段真值加进白名单
并说明依据**，不合法就是落位缺陷。**不要泛化成"任意 LIQUID 都行"。**

### 1.2 `c_5_fall_subsystem`：钉死的 RNG 常量顺延

```
坠落回合的 RNG 消耗增量偏离（= 换层生成的固定消耗）: expected 16214 to be 10401
  （:139 的 .toBe(10401)）
```

该处注释自陈是 V-2b-8 重捕获后钉的固定增量。本轮机器总数 469 → 823，
换层生成的随机消耗自然变大。

**被测合同（"只有换层、没有怪物推进"）不变**，只是常量移动
⇒ **按顺延改为实测值，并照该处注释格式补一行本轮沿革**（写明是区域机器路由
带来的机器数增长）。**不要改成范围或 `toBeGreaterThan`。**

### 1.3 `g_2`：**测试舞台缺陷**，不是气体守恒坏了

```
泥格晋升命中后：沼气经 GAS 分支落地、gasGrid 镜像经 Game 对账分支同步
AssertionError: 2 体积扩散后不得凭空增加: expected 3 to be less than or equal to 2
  （:586）
```

**验收方已定位成因，请复核**：

- `openRoom`（:53-65）**只清理 x∈[1,20)、y∈[1,16) 这一角**，关卡其余部分是真实生成的
- 测试把 `TERRAIN_FLAGS[MUD].promoteChance` **全局**抬到 10000（确定性必中）
- `totalVolume`（:70-76）扫的是**整张网格**（`grid.width × grid.height`）

⇒ 本轮 `ce_61_swamp` 145 台之后，**关卡别处出现了泥格**，它们在同一个
`objectiveTimeBlock()` 里也被迫晋升产气，于是全图总量 > 2。

**处置方向（你定，给理由）**：把测量**限定到舞台范围**，或让舞台覆盖全图，
或把 `promoteChance` 覆盖限定到目标格。
⚠️ **不要把 `≤ 2` 放宽**——那是被测性质本身。
⚠️ 改完要保证：**若真的出现"凭空增量"缺陷，这条仍会红**。给出反向验证。

## 2. 本轮不做

- 不加新蓝图、不解锁 65/66、不补 CE 8（留 9e-2）
- 不碰区域生长实现本身（它已通过验收方全量门禁的其余 107 个文件）
- 报告里登记的"既存领养携带品重复输出"——**继续登记，不修**

## 3. 基线

§1.1/§1.2/§1.3 都是**测试侧**修正，**不应移动生成流**。
⇒ **不许重捕获 `generation_baseline.json`**；`npm run test:drift` 必须绿。
若它红了，说明你动到了生产代码，停下来查。

## 4. 授权反驳

§1 的三条成因都是验收方诊断的，**可能有错**：
- 1.1 的"草长在浅水上"是推测，**以你回 CE 核实的结果为准**
- 1.3 的舞台缺陷判断基于读代码，**若实测另有成因，驳回并给证据**

你上一轮纠正了验收方五处 CE 细节（房间分支起点、内循环条件是 `chooseBP`、
`:1160` 忽略返回值、`:1178-1193` 无"收集不足即失败"检查、9b 舞台缺陷已修）
——**继续这样做**。

## 5. 授权改动清单

`src/test/c_4b_dungeon_feature.test.ts`、`src/test/c_5_fall_subsystem.test.ts`、
`src/test/g_2_gas_df_wiring.test.ts`

**明确不许改**：任何生产代码、`blueprints.json`、`generation_baseline.json`。
清单外改动必须申报。`BrogueCE-master/` **只读**。**守卫顺延不放宽**。

## 6. 门禁跑法

```
npx vitest run src/test/c_4b_dungeon_feature.test.ts \
               src/test/c_5_fall_subsystem.test.ts \
               src/test/g_2_gas_df_wiring.test.ts
npm run test:drift      ← §3 的判据，必须绿且未重捕获
npm run build
```

**最终复跑（硬要求）**：所有编辑完成后重跑上述全部，给逐条结果并声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 7. 报告

写到 `ai_docs/reports/v-2b-9e-1-finish.report.md`，含：

1. 三条逐条：成因复核结果（同意/驳回 + 依据）→ 处置
2. **1.1 的 CE 依据**：浅水上长草是否合法，白名单加了什么
3. **1.3 的反向验证**：改完后若真出现"凭空增量"，这条还能不能红
4. §3 自检：`test:drift` 绿否、确未重捕获
5. **§6 的最终复跑声明**
6. 清单外改动申报
