# U26a：深层 D27–40、宝石与超胜利地图（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U26 深层轮（U26b 结算/分数/高分榜已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U26 行、§2 D22、§3.1 K37 是权威规格**（`153dbb5`）：

> **U26 深层/终局/分数**；K37｜**分深层生成/结局条件和结算/高分两轮**｜S: amuletLevel、deepestLevel、lumenstone、superVictory
> K37：D26 断下梯（U18a-3 已修）、**CE 最深 40/宝石/超胜利缺**

## 1. 范围（**全部请自行核实 CE**）

- `DEEPEST_LEVEL=40`、`AMULET_LEVEL=26`（GlobalsBrogue.c:43–44 等）：D27–40 可生成与进入；各层深度相关表（怪物 horde 深度、物品深度、蓝图深度、autoGen 深度、光照/黑暗）按 CE 使用实际深度
- **宝石（lumenstone，GEM 类）**：CE 在 D27–40 的生成方式（`populateItems` 中 D>AMULET_LEVEL 的宝石规则、数量、位置、是否机器守护等），ItemLoader 接 GEM 类生成；
  U26b 已接 GEM 计分（按件数兑换、itemValue 5000×quantity）
- **超胜利**：D40 底层传送门携护符触发 superVictory（Movement.c:2512–2542），U18a-3 已有 D40 portal；接入 U26b 的超胜利结算与高分描述（"Mastered"与宝石数三档）
- D27–40 的楼梯/上行、护符在 D26 的原规则保持；w_5 等"D1–26 类别覆盖"守卫的 GEM 例外（U26b 注明"由 U26a 生成"）在本轮按 CE 收口
- 深层自然样本：至少若干 seed 的 D27–40 生成可达性、宝石可拾取、超胜利可达成（夹具可定位但操作用真实命令）

## 2. 生成流

先 drift 记录（现有 drift 只覆盖 D1–26——若本轮改动不影响 D1–26 则应 0 变化，需证明）→ 单变量归因 → 为 D27–40 新增独立守卫/基线（不要把旧 drift 扩为 D40 时混入本轮行为变化）→ 如 D1–26 有变化则一次重捕获。📌 用户裁决：生成变化不是卡点。
黄金 trace（UR2/UR3/UR4）若因正当行为变化翻红，**单变量归因后按原方法重录并登记**。

## 3. 约束

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**反查闭包含 p1_30、U24、U00/U01/U03/U03b、U04、U18a-3、U26b、U27、w_5、UR2/UR3/UR4 trace、V 系列 与所有读源码守卫**；不产生 CRLF。

## 4. 门禁与报告

R∪S + build + test:drift；**全量 npm test 完整跑完**；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-26a.report.md`。
