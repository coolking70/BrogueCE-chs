# W-3：普通射线轨迹

> **本地执行（`codex exec --worktree`）。W 链第三轮，机制前置。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-3 行是权威规格**（已合入 `823eb0e`）：

> **W-3 普通射线轨迹**｜延伸越过瞄准格、CE 线选取/斜角、全层阻挡、穿怪、
> 遇障前停、同起终点；沿途 fire/electric/DF 和落点调用时序。
> **暂不做 blink/tunnel 特殊地形操作**｜依赖 W-1、W-2｜生成流：**否**｜
> 改动面 B、G；**尽量只调用现有 Map 原语** → R(B,G,实际 Map 调用文件)+S(B,D)｜
> 撞红预期：9c 电晶体与实墙截断、P4-1b/3；
> **两只共线怪、空射、贴脸障碍、门/水晶而非仅 WALL；不得重开全部机器**

**本任务书不复述技术细节**——验收方在这个子系统上估错过四次。
任务书若与报告冲突，**以报告为准**。

## 1. 前两轮的交接

- **W-1**（`a72ac60`）建了 `BoltResult` 契约：`caster` / `origin` / `aimPos` /
  按序 `hits` / `landingPos`。W-1 明确 **`hits` 只表示"旧路线接触到的生物"**，
  不代表效果成功或反射后的受伤者。本轮换真实轨迹后，**`hits` 的含义会变准**
  ——在报告里说明它现在到底代表什么。
- **W-2**（`53e9a27`）把 autoID 改成真实效果观察。**轨迹一变，哪些效果"可观察"
  也会变**（例如遇障前停会改变火/电的落点）。**复跑 W-2 的 autoID 用例并说明变化。**

## 2. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ **不新增 `arcana.json` 物品**（W-24~26）
- ❌ **不实现任何效果行为**（W-8~W-23）——本轮只做轨迹
- ❌ **不做 blink / tunneling 的特殊地形操作**（W-12 / W-13）
- ❌ 不要偷偷启用怪物 `BLINKING` / 蛛网 / 藤射线
- ⚠️ **反射留 W-4**。本轮遇障是"停"，不是"弹回"
- ⚠️ 报告点名：**不得重开全部机器**（别为了让轨迹穿过而放宽机器阻挡）

## 3. 报告点名的几种舞台，逐个要有用例

- 两只共线怪（穿怪 vs 停在第一只）
- 空射（没有目标）
- 贴脸障碍（起点紧邻墙）
- **门 / 水晶，而不是只测 WALL**
- 同起终点
- 延伸越过瞄准格
- 斜角线选取

## 4. 教训（已连续十轮验证，继续照做）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**。

## 5. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定，报告里列出实际跑了哪些、怎么查的。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**
⚠️ 本轮触及 Map 原语，`c_3_walls_doors` / `c_4a_*` / `c_8_connectivity` 一类
容易只在 S(pattern) 命中，别漏。

## 6. 门禁

按 §5 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 7. 授权反驳

W-0/W-1/W-2 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 8. 报告

写到 `ai_docs/reports/w-3.report.md`，含：

1. 轨迹各项的 CE 依据与落地（线选取、斜角、阻挡层、穿怪、遇障前停）
2. **`hits` 语义变化**（W-1 交接）
3. **W-2 autoID 用例的复跑结果与变化说明**（轨迹变了哪些效果的可观察性）
4. §3 七种舞台的用例与各自"什么缺陷下会翻红"
5. **§2 边界证明**：怎么确保没做 blink/tunnel 地形操作、没开反射、没放宽机器阻挡
6. §5 反查过程与实际跑的文件
7. 撞红清单与处置
8. §2 自检 + **§6 最终复跑声明**
9. 清单外改动申报
