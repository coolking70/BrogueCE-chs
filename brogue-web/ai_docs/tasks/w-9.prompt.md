# W-9：基础定向状态

> **本地执行（`codex exec --worktree`）。W 链效果收口第二轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-9 行是权威规格**（`823eb0e`）：

> **W-9 基础定向状态**｜SLOW/HASTE/HEALING/INVISIBILITY/DISCORD：
> **真正命中者、正确时长/百分比、互斥/资格**；对应现有 wand/staff，
> discord 杖待入池｜依赖 W-4、W-5｜生成流：**否**｜
> 改动面 G、必要的 C/M 状态入口 → R(实际文件)+S(B,C,Q,M)｜
> 撞红预期：P2-2 速度、P4-1b 友军治疗/discord、scroll_effects 共用状态；
> **miss 不给自己加 buff，反射可命中自己**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. ⚠️ 前几轮登记给本轮的旧错误——**本轮要收掉它们**

W-2（`53e9a27`）做 autoID 时如实登记了几处"**效果打错了对象**"的旧行为，
当时明确留给效果轮。它们全落在本轮：

| 旧行为 | W-2 的原话 |
|---|---|
| **heal / haste 作用于施法者自己** | "玩家原有 heal/haste 仍作用于自己" |
| **空射时自己隐形** | "原有空射自隐形仍存在……W-9 再改效果目标" |
| **discord 用错状态** | "玩家 discord 原有 confused 错配保留后续轮" |

W-0 的规格原话"**miss 不给自己加 buff，反射可命中自己**"正是针对前两条：
- 射偏了（miss）→ **不能**给施法者加 buff
- 被反射回来命中施法者 → **可以**（W-4 已让 `hits` 记录反射后的实际命中者）

⇒ **两者的区别是"射线有没有真的命中施法者"**，不是"施法者是谁"。
W-4 的 `hits` 契约现在能表达这一点，**用它**。

## 2. 公式参考（执行方请自行回 CE 核实）

`PowerTables.c` 已列出部分公式，**验收方只是转述，以你核实为准**：
- `staffHasteDuration = 2 + 4E`
- `staffDiscordDuration = 4E`
- SLOW / HEALING / INVISIBILITY 的公式**验收方未查**，请自行定位

⚠️ 报告要求"**正确时长/百分比**"——HEALING 很可能是百分比而非固定值，
**别凭印象写成固定点数**。

## 3. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ **不增种类、不改 frequency**——discord 杖"**待入池**"是 W-26 的事，
  本轮只把 discord **效果**做对
- ❌ 不改怪物的状态施加逻辑，**除非**它与玩家共用同一个状态入口
  （P4-1b 的友军治疗/discord 就是共用的，**改到了要明列**）
- ⚠️ `scroll_effects` 与本轮**共用状态**（W-0 点名）——共用入口的改动要确认卷轴行为不变

## 4. 教训（已连续十六轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；
**CE 里"看起来不一致"的数值先查是不是承重的**（W-7）。

## 5. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定。`scripts/w*-test-scope.mjs` 可复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 6. 门禁

按 §5 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 7. 授权反驳

W-0~W-8 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 8. 报告

写到 `ai_docs/reports/w-9.report.md`，含：

1. 五种状态各自的**时长/百分比公式** CE 依据与落地（含验收方没查的三种）
2. **§1 三条旧错误的收口**：heal/haste 自施、空射自隐形、discord 错配，各自怎么改的
3. **"miss 不加 buff、反射可命中自己"的实现与证明**（怎么用 W-4 的 `hits`）
4. 互斥/资格规则的 CE 依据
5. **与 `scroll_effects`、P4-1b 共用状态入口的处理**，证明卷轴/怪物行为不变或明列变化
6. W-2 的 autoID 用例是否需要随效果目标修正而更新（**效果对象变了，可观察性可能跟着变**）
7. §5 反查过程与实际跑的文件
8. 撞红清单与处置
9. §3 自检 + **§6 最终复跑声明**
10. 清单外改动申报
