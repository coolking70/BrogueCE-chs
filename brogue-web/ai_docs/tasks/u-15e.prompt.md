# U15e：药水语义（饮用与投掷）

> **本地执行（Windows 轨）。X-0 路线。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.3 U15e 行、§3.1 K34 是权威规格**（`153dbb5`）：

> **U15e 药水语义**；K34｜前置 U14/U17 必要 DF｜生成流：间；**lichen 等回池是**｜
> R: consumables.json、G、DF/Status；S: heal_full、life、confusion、hallucinating、darkness、creeping_death、throwItemAt｜
> **喝与扔分矩阵、CE maxHP/持续期/鉴定/取消**；lichen 闭包成功后单独回池与漂移

K34：life 只回满不增 maxHP；confusion 喝下给 hallucinating；多种持续期固定；缺 darkness/lichen 完整链
（Game 约 :3942、:3985–4005、:4016、:4034–4047，对 CE Items.c 约 :8067–8135）。

## 1. 范围

1. **逐药水矩阵**（CE `drinkPotion` 每个 case + 投掷碎裂 `throwItem`/`hitSomethingSolid` 的药水分支）：

| 药水 | 喝：效果/数值/持续期/消息/鉴定 | 扔：DF/气体/量/鉴定 | web 现状 | 本轮修正 |

   好药水（life、strength、telepathy、levitation、detect magic、haste、fire immunity、descent…）与
   坏药水（hallucination、incineration、darkness、creeping death/lichen、confusion、paralysis…）全列
2. U14a 已建：黑暗状态载体（光照消费留 U21b）、虚弱/恶心；**黑暗药水的饮用入口本轮接上**（状态施加），光照不接
3. **lichen（creeping death）**：若 U17 缺的 DF 副作用挡住了忠实实现，**不回池**，登记；若能完整做到，
   单独一个提交回池并按"先归因后重采"更新基线（用户已裁决生成变化不是卡点）
4. 取消语义（投掷取消、喝前确认——如 CE 有）：取消不耗物品/时间/RNG

## 2. 硬约束

- ❌ 除 lichen 回池（若做）外**不移动生成流**
- 📌 **任务书与 CE 冲突时以 CE 为准并在报告里写明**；不做旧存档兼容

## 3. 教训

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
**反查闭包必须包含 p1_30 i18n 门与所有读源码的守卫**；改/删文案键时死键移入 zh_CN.legacy.json；
**Windows 注意**：不要让证据文本变成 CRLF（U18 出现过）。

## 4. 授权测试清单与门禁

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定两份清单；`npm run build` + `npm run test:drift`；
最终复跑声明「这是最终状态下的运行结果，不是中途快照」。

## 5. 报告

写到 `ai_docs/reports/u-15e.report.md`：逐药水矩阵（CE 依据）；修正清单；lichen 决定与依据；反查与两份清单；
撞红处置；自检 + 最终复跑声明；登记（给 U17/U21b/U24）。
