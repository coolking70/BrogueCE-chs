# U15b-1：戒指效果——现有 6 种逐效果 CE 化（不移动生成流）

> **本地执行（Windows 轨）。X-0 路线；U15b 第一轮。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U15b 行、§2 D10、§3.1 K22 是权威规格**（`153dbb5`）：

> **U15b 戒指目录/附魔/效果**；K22｜前置 U13/U14/U21b；充能复用 W-6｜**先现有 6 种逐效果，后缺失 2 种入池另轮**；
> 实际 E/可见 E/充能、可观察性与旧档迁移分清

K22：awareness 错误给予 telepathy 与抗性、regeneration 忽略 E（固定 0.6 回满时间）；stealth/transference/clairvoyance 缺完整消费者
（G 约 :6142–6165、:6232–6242，ItemLoader 约 :90、:1428–1439，对 CE Items.c:8685–8748）。智慧充能已接，不重复修 W-6。

## 1. 范围（**全部请自行核实 CE**）

- web 目录现有 6 种戒指逐种对齐 CE `updateRingBonuses` 及各消费者（按 CE 实际读 `rogue.xxxBonus` 的位置）：
  - clairvoyance：`rogue.clairvoyance` → 透视半径/负值致盲区（CE `updateClairvoyance`、显示层）
  - stealth：`rogue.stealthBonus` → `currentStealthRange` 公式
  - regeneration：`rogue.regenerationBonus` → `updatePlayerRegenerationDelay`（`turnsForFullRegen` 公式，fixpt 精度同 CE）
  - transference：`rogue.transference` → 近战吸血/负值自伤（Combat.c 实际位置）
  - awareness：`rogue.awarenessBonus` → 搜索强度/陷阱密门感知（**不是 telepathy、不是抗性**）
  - wisdom：已接（W-6），只核对不重修
- 实际附魔 vs 可见附魔（`timesEnchanted`/未鉴定显示）按 CE；详情文本按 CE 数值
- **不在本轮**：普通戒指出生附魔（移动生成流，另交 Mac 轨）、light/reaping 入池
- 不做旧存档迁移（项目裁决）

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
每种效果要有"装备→消费者数值"的黄金值证据（多个 E，含负值）；**反查闭包含 p1_30、U24、U01/U03 合同守卫、b_1b/b_4a/W-6/7 与所有读源码守卫**；
**不得改变 test:drift**；不产生 CRLF。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明。报告 `ai_docs/reports/u-15b.report.md`。
