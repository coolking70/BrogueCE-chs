# U23：地图记忆与魔法测绘

> **本地执行（Windows 轨）。X-0 路线。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.4 U23 行、§3.1 K18 是权威规格**（`153dbb5`）：

> **U23 地图记忆/魔法测绘**；K18｜前置 U03；U21 知识判断｜生成流：无（不得偷耗实质流）｜
> R: Grid/Appearance/G、GameCanvas；S: hasMemory、rememberedTerrain、rememberedFlags、MAGIC_MAPPED、MB_SUBMERGED｜
> **离开视野后环境变化不泄露，测绘只给 CE 允许的信息；回访/读档一致**，水下生物展示另测

K18：`MAGIC_MAPPED`、`rememberedTerrain/flags`、`MB_SUBMERGED` 缺独立完整载体；hasMemory 下读现地形（Appearance 约 :359），
CE Items.c 约 :7731 保存历史值。

## 1. 范围（**全部请自行核实 CE**）

- CE `rememberedAppearance`（字形/前景/背景）、`rememberedTerrain`、`rememberedItemCategory`、`rememberedTerrainFlags` 的
  **写入时机**（`updateVision` / `refreshDungeonCell` 里何时更新记忆）与**读取**（`getCellAppearance` 的 restore memory 分支，
  IO.c 约 :1128 起，U21a 已读过这段）
- 记忆态显示：CE 对记忆格的变暗/去色规则（U21c 已接外观，U21b 已接光照——复用）
- 魔法测绘卷轴：`MAGIC_MAPPED` 只揭示哪些层（CE 注释：只能看 dungeon 和 liquid 层）、门/密门/陷阱的揭示规则
- 离开视野后地形变化（火烧、门开关、怪物移动、物品被拿走）**不泄露**：画面、render_game_to_text、hover 详情、自动探索都读记忆
- `MB_SUBMERGED`（水下生物）：若本轮需要可以建载体（U18/U07/U09 都登记过），只接展示与观察相关读写
- U03 整局存档：记忆字段入档，存读后记忆画面一致

## 2. 约束与教训

- ❌ 不偷耗实质 RNG 流；📌 **任务书与 CE 冲突时以 CE 为准并写明**；**UI 不能比玩家知道得更多**
- **撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
  **反查闭包含 p1_30、U24 硬串守卫、U03 字段/状态合同守卫与所有读源码守卫**（新增字段必须登记到 U01/U03 合同）；不产生 CRLF

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明；截图（离开视野前后、魔法测绘后）。报告 `ai_docs/reports/u-23.report.md`。
