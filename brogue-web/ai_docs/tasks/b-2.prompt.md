# B-2：投掷 —— 从"传送 + 落地"改成 CE 的弹道与命中

> 只改"允许修改"清单里的文件；**不要执行任何 git 写操作**（`status`/`diff`/`log` 只读允许）。

**授权反驳条款有效**：CE 源码优先。前几轮执行方纠正过验收方十余次，最近三次：
S-1 发现 `c_5` 対抗③ 的阈值**正好压在正确实现的真值上**（哨兵一直在掷硬币）、
B-1c 发现 **web 其实有加速药水**（id 是 `potion_of_haste`，B-0 与 B-1a 的登记同时作废）、
C-5 揪出 C-2 留形的 `pathingDistance` 把不可达写成 `-1`。
**请继续，包括反驳本任务书与任何前置文档。**

**先读**：`ai_docs/b_0_phase_b_survey.md` 投掷一节、
`ai_docs/b_1a_identification_report.md` §8「给 B-2」三条、
`ai_docs/b_1b_*` 与 `ai_docs/b_1c_*` 的登记清单。
凡冲突，**以 CE 源码为准并在报告里指出**。

---

## 一、本轮定位

**⚠️ 本轮与 C-7（光照，改生成）并行执行**，文件边界已划开。

B-0 实测："**投掷是传送 + 落地**——无弹道、无命中、扔剑砍不了人。"
但它同时指出**载体就绪度最高**：药水投掷的 7 条 DF 链 web 已接 4 条（F/G 链的遗产）。

---

## 二、范围

### 1. 弹道与命中

CE 的 `hitMonsterWithProjectileWeapon` 与投掷路径自己定位（`Items.c` 一带）。
要点：飞行路径逐格推进、命中掷骰、未命中则落地、投掷武器可回收。
**具体数值与分支自己抠，不要信我的转述。**

### 2. B-1a 登记的三条

1. **投掷命中要走同款 `decrementWeaponAutoIDTimer`**（CE `Combat.c:1427`）——
   投掷击杀也计入武器熟悉度；web 的钩子目前只挂在近战出口。
2. `throwItemAt` 的**药水自亮分支要细分**：按 `Items.c:6988-7050`，
   只有 7 种功能性药水才自亮（B-1a 保持了"全部亮"的简化，注释已标）。
3. **留痕"投掷仍是传送+落地"本轮反转**。

### 3. 键位：CE 的 `THROW_KEY = 't'`

`Rogue.h:1183`。**`t` 目前空闲**（P1-46 已把移动键回归纯 vi 键）。
⚠️ `p1_46_keybindings.test.ts:94` 有一条留痕钉着"`w`/`a`/`d` 三个命令键空闲"——
**`t` 不在那条里**，但接 `t` 键仍需在该文件补一条对应断言，
**该文件已列入允许清单**。

### 4. ★ 载体盘点

投掷武器在 `weapons.json` 里**只有 `dart`**（无 javelin / throwing axe）。
**先盘点清楚 CE 的投掷武器在 web 有哪些载体**，没有的只登记不实现
——不要造空转链（C-4c 硫矿的前车之鉴）。

---

## 三、明确不做（写显式留痕测试）

- **三占位卷轴**（B-3）、**生成规则对齐**（B-4，独占轮，含 P1-50 的
  "dart 照吃 20% 附魔"——CE 明令投掷武器不可附魔）。
- **不碰光照**——那是并行的 C-7 的地盘。
- 不碰火/气体/坠落/自动生成器。

---

## 四、★ 硬门禁：区分生成期与交互期的 RNG

**`generation_baseline` 对交互期掷骰是盲的**（`project_conventions.md`
2026-09-17 新立，B-1c 实证：往交互期注入掷骰、基线全程绿）。

投掷是**交互期**行为，所以：
- `generation_baseline` **必须保持绿**（本轮不该碰生成期）；
- **但它不够**——本轮如需断言"没有多消耗掷骰"，必须另立
  **交互期哨兵**：用 B-1b 验证过的 `rng.randomNumbersGenerated` **增量**口径，
  建在**构造场景**上（S-1 已把 `createHeadlessGame(seed, 'test')` 的
  合成层隔离通道摸清，可直接复用）。
- 立哨兵时**反向验证必须真往交互期注入一次掷骰**，确认它翻红
  ——B-1c 正是靠这一步发现自己的哨兵调错了函数层。

---

## 五、文件边界（硬约束，路径已 grep 核实）

**允许修改**：
- `src/engine/Core/Game.ts`（投掷本体与命中结算）
- `src/engine/Items/Item.ts`、`ItemLoader.ts`
- `src/engine/Combat/Combat.ts`（**仅**投掷命中与熟悉度钩子）
- `src/engine/Input.ts`（**仅**接 `t` 键）
- `src/components/InventoryOverlay.vue`（**仅**投掷入口）
- `src/locales/zh_CN.json`（仅增键）
- **既有测试**（两段 grep 生成）：
  `p1_46_keybindings.test.ts`（补 `t` 键断言）、
  `b_1_weapon_specials.test.ts`、`p4_7_player_weapon_geometry.test.ts`、
  `b_1a_identification.test.ts`（投掷自亮细分）、
  `armor_runic_effect.test.ts`、`p2_3_objective_time.test.ts`
  **限定**：只改因本轮行为变化而到期的断言，**不许放宽守卫性质**。逐条写进报告。
- 新增测试文件

**禁止修改**（违反即本轮作废）：
- **C-7 的地盘**：`src/engine/Lighting/`、`src/engine/Map/TerrainCatalog.ts`
- `src/engine/Generator/`、`src/engine/Map/` 其余文件、`src/engine/Environment/`
- **`BrogueCE-master/` 下任何文件**
- 任何 `src/data/*.json`（**投掷武器数据表归 B-4**）、`src/engine/Random.ts`、`vite.config.ts`
- **`src/test/fixtures/*`**、`src/test/harness.ts`、上面未列出的既有测试文件

**已知工具盲区**：i18n 扫描器**整体跳过模板字符串**，写在 `${}` 里的 `t()`
会被判成死键打红门禁——**改自己的写法绕开，不要改扫描器**。

---

## 六、测试要求

1. 每条断言必须能在某个具体的错误实现下失败。
2. **对抗性测试至少七条**，必须含：
   - 投掷仍是"瞬移到目标格"（无逐格弹道）
   - 命中必中（没掷命中骰）
   - 未命中不落地 / 落到错误的格
   - 投掷击杀不计入武器熟悉度
   - 药水自亮没细分（非功能性药水也亮）
   - `t` 键没接上 / 接错动作
   - **交互期多消耗掷骰**（§四的交互期哨兵）
3. **反向验证（强制）**：至少四条真的改坏、贴真实失败输出、还原。

---

## 七、交付

报告写入 `ai_docs/b_2_throwing_report.md`，必须含：
- CE 投掷路径的行号与复核要点；
- **载体盘点表**（CE 的投掷武器在 web 有哪些）；
- **交互期哨兵的设计与反向验证输出**（§四）；
- `generation_baseline` 保持绿的实际输出行；
- 改了哪些既有测试断言、逐条说明为什么到期；
- 对抗性测试与反向验证的真实失败输出；
- 给 B-3 / B-4 的登记清单。

**留痕规矩**：遇到留痕与文件边界冲突，**停下来申报**，
**不要把代码扭曲成扫描正则看不见的形态**。
