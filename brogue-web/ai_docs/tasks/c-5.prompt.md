# C-5：坠落子系统 —— 解禁 CHASM 与桥梁（吸收 P1-22）

> 只改"允许修改"清单里的文件；**不要执行任何 git 写操作**（`status`/`diff`/`log` 只读允许）。

**授权反驳条款有效**：CE 源码优先。你在前几轮连续纠正过验收方，最近三次是：
G-1 指出 `DF_GAS_FIRE` 是 SURFACE 层、G-3 指出"半径 4"是 `effectRadius`（视觉闪光）
而非铺展参数、**F-2c 指出爆炸伤害取 `maxHP/2` 而非当前血量的一半，
且免疫窗是生物状态而非按格记账（我在对抗清单里把它写反了）**。
**请继续，包括反驳本任务书。**

---

## 一、这一轮解开一个憋了很久的结

C-2 实现了完整的湖泊系统，包括 `buildABridge` 的全套 CE 判据
（比值 / exposure / pathingDistance），**但真实关卡里桥恒为 0**——
因为 CE 的 `T_CAN_BE_BRIDGED = T_AUTO_DESCENT`，**桥只架在深渊上**，
而 C-2 当时显式把 CHASM 剔出了候选（`LakeSystem.ts:18` 有留痕注释：
"web 无坠落子系统"）。

**本轮补上坠落，CHASM 与桥梁一起解禁。** 同时吸收路线图 **P1-22**
（下坠药水完全没实现，`Game.ts:2894` 只打了一行日志）。

---

## 二、要做什么（CE 出处我只给线索，数值自己抠）

### 1. 坠落本体

- `T_AUTO_DESCENT` 的消费点：`Movement.c:371`、`Movement.c:1314`（**玩家踩已知深渊
  前的确认提示 "Dive into the depths?"，注意那一串前置条件**）、`Time.c:171-176`；
- `MB_IS_FALLING`（`Rogue.h:2160`，注释写明"**在回合末**下坠"）——
  **坠落是回合末结算的，不是踩上瞬间**，这个时序请照抄；
- 坠落伤害：`Time.c:1144` 的 `randClumpedRange(fallDamageMin, fallDamageMax, 2)`
  ——`gameConst` 里的那两个值自己找；
- 落地后的状态清理：`Time.c:1137` 清掉 `MB_IS_FALLING | MB_SEIZED | MB_SEIZING`；
- **悬浮豁免**、怪物也会掉（`Time.c:1537` `monsterShouldFall`）。

### 2. 解禁 CHASM 与 CHASM_EDGE

`LakeSystem.ts` 的 `liquidType` 里 C-2 显式剔除了 CHASM 候选，并留了
"C-5 解除条件"的留痕测试。**按那条留痕的指示解禁**，桥梁随之自动出现
（`buildABridge` 已由 6 个构造场景钉死，本轮应当**不需要改它**——
若你发现需要改，停下来在报告里论证）。

### 3. P1-22 下坠药水

`Game.ts:2894` 现在只打日志。CE 的对应物自己找（`DF_HOLE_POTION` 一带），
接上真实的坠落。`Game.ts:5355` 还登记了 `pit_bloat` 的 `DF_HOLE_POTION` 缺口，
**一并盘点是否有载体**。

---

## 三、★ 一个需要你先分析、由验收方裁决的问题

坏层闸门（`p1_26` / `p1_29` / `p1_33`）断言的是"**从上楼梯能走到下楼梯**"。

**深渊解禁后这个判据的语义变了**：CE 里跳进深渊**也是一种下潜方式**。
那么"下楼梯不可达但有深渊可跳"的层，算不算坏层？

**请先查明 CE 自己怎么看**（CE 的生成期有没有等价的连通性保障？
`Architect.c:3002` 的 `mapToShore` 把 `T_AUTO_DESCENT` 与深水、岩浆并列，
这条线索值得跟），给出你的结论与依据，**但不要擅自修改闸门判据**——
在报告里给出建议，由验收方裁决。

**在裁决前，闸门按现有字面口径跑，红了如实申报。**

---

## 四、明确不做（写显式留痕测试）

- **不改火与气体的任何东西**（F/G 链刚收口）——反向哨兵。
- `C-6 runAutogenerators` / `C-7 光照` 不做。
- `p4_4` / 爆炸 / 燃烧状态机不动。
- 不实现 CE 的"坠落时物品散落"等衍生机制（若存在），只登记。

---

## 五、文件边界（硬约束）

**允许修改**：
- `src/engine/Map/LakeSystem.ts`（**仅**解禁 CHASM 的那处剔除；
  `buildABridge` 的判据不许动）
- `src/engine/Map/Grid.ts`、`TerrainCatalog.ts`
- `src/engine/Core/Game.ts`
- `src/entities/Monster.ts`（**仅**为怪物坠落所需的 bookkeeping 位；
  若需改 `Creature.ts` 的公共类型，**停下来论证**）
- `src/locales/zh_CN.json`（仅增键；新文案必须走 i18n）
- **以下既有测试（本轮必然到期的断言），提前授权**
  ——清单按交接文档「两段 grep」法生成：
  **① 结构性穷尽表**（凡新增地形/DF 必打红，与主题无关）：
  `c_4a_0_layer_model` / `c_4a_terrain_catalog` / `c_4b_dungeon_feature` /
  `c_4c_promotion` / `f_1_fire_as_terrain` / `f_2a_fire_mechanics` /
  `g_2_gas_df_wiring` / `g_3_gas_effects` / `p1_42_secret_door_search` /
  `p2_6_display_settings`
  **② 本轮主题（深渊/桥/坠落）**：`c_2_lakes` / `c_2_lakes_e2e` /
  `c_2_lakes_determinism` / `p1_29_lake_connectivity` / `p1_29_adversarial_gate` /
  `p1_24_death_sink`
  **限定**：只改因本轮行为变化而到期的断言，**不许放宽守卫性质**
  （穷尽式 `toEqual` 仍要穷尽、阈值不许松、火与气体的断言一字不许动）。
- 新增测试文件

**禁止修改**（违反即本轮作废）：
- **`src/test/fixtures/generation_baseline.json`** —— 它**必然全线变红**
  （深渊改变地图与 RNG 流），**这是预期，但不许你刷新**，如实报告，
  由验收方授权重捕获。
- `p1_26_invariants` / `p1_33_machine_chokepoint`（**见 §三，判据的裁决权在验收方**）
- `src/engine/Map/` 下其余既有文件、`src/engine/Generator/`、`src/components/`
- `src/entities/` 下除 `Monster.ts` 外的文件
- **`BrogueCE-master/` 下任何文件**
- 任何 `src/data/*.json`、`src/engine/Random.ts`、`vite.config.ts`
- `src/test/harness.ts`、上面未列出的任何既有测试文件

---

## 六、★ 「照抄留形」的死分支必须逐字重核

`project_conventions.md` 的规矩（G-2 揪出 F-2a 留形的 `ALL_DIRS8` 躺过三轮之后立的）：
留形代码**写它的那轮无法测它**。

**本轮激活 C-2 留形的 `buildABridge`（真实关卡里从未跑过）与 CHASM 相关分支，
必须逐字符重核 CE 并在报告里声明核过。**

---

## 七、门禁

1. **反向哨兵——火与气体逐位不变**：从 F-0 附录 A 恢复探针复跑，
   `[FIRE-NAT]` / `[DMG-FIRE]` / 气体曲线须与 F-2c 后的基线一致。
2. **实测报出**：深渊出现率随深度、桥梁实际生成数（**C-2 起一直是 0**）、
   坠落伤害分布、玩家/怪物坠落的实际发生率。
3. **§三 的分析结论与建议**（不要擅自改闸门）。
4. `generation_baseline` 预期全红——**如实报告，不许刷新**。
5. 坏层闸门按现有字面口径跑，红了如实申报并说明成因。
6. `npm run build` 绿。

**全量门禁用 `npx vitest run --fileParallelism=false`**（串行）。
**注意本轮与另一轮并行执行**，重型测试可能因抢 CPU 假红
（全是 `Test timed out`、零断言失败）——**先单跑复核再下结论**，
报告里区分"真失败"与"超时"。

---

## 八、测试要求

1. 每条断言必须能在某个具体的错误实现下失败。
2. **对抗性测试至少七条**，必须含：
   - 坠落在踩上瞬间结算而非回合末
   - 悬浮豁免失效
   - 坠落伤害的 clump 参数写错
   - 怪物不会掉
   - 已知深渊没有确认提示 / 未知深渊反而提示
   - 桥梁仍然为 0（解禁没生效）
   - 火或气体被本轮意外改动（回归哨兵）
3. **反向验证（强制）**：至少四条真的改坏、贴真实失败输出、还原。

---

## 九、交付

报告写入 `ai_docs/c_5_fall_subsystem_report.md`，必须含：
- 坠落本体的 CE 行号与复核要点（**尤其回合末结算的时序**）；
- **§三 的分析结论与建议**（坏层闸门在深渊存在时的正确语义）；
- §七.2 的实测数据（桥梁数从 0 变成多少，是本轮最直观的指标）；
- 留形分支逐字重核的声明（§六）；
- 火与气体哨兵的逐位比对输出；
- 改了哪些既有测试断言、逐条说明为什么到期、守卫性质未放宽的论证；
- 对抗性测试与反向验证的真实失败输出；
- 给 C-6 / C-7 的登记清单。

**已知陷阱**：深水枚举成员是 `WATER_DEEP` 不是 `DEEP_WATER`，写错**不报错**
（vitest 走 esbuild 只剥类型不检查）。**任何让人困惑的失败，先跑 `npm run build`。**

**留痕规矩**：遇到留痕与文件边界冲突，**停下来申报**，
**不要把代码扭曲成扫描正则看不见的形态**——那是自造假绿。
