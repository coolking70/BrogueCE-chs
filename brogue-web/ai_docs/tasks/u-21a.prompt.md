# U21a：展示可见性一致（隐形不泄漏）

> **本地执行（Windows 轨）。X-0 路线；U21 的第一轮（地形外观/动画残留为 U21c，光照口径为 U21b）。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.4 U21 行、§3.1 K17 是权威规格**（`153dbb5`）：

> **U21 展示可见性/地形外观/动画**；K16/K17｜生成流：无｜R: Appearance/GameCanvas/Sidebar、G；
> S: invisible、telepathy、canObserveBoltCreature、FORCEFIELD、DEFAULT_LOOK、render_game_to_text、boltAnimation｜
> **画面、文本、hover、侧栏、瞄准统一已知信息；隐形不泄漏，已揭示可见**

K17：`monsterAppearance` 与 `render_game_to_text` 按格可见返回怪物，不检查真正隐形；目标候选另走 `canObserveBoltCreature`
（Appearance 约 :462–476，GameCanvas 约 :491–494，CE IO.c 约 :1253）。U09 §7 另登记：临时 INVISIBILITY 状态
`visibleMonsters` 只按固有旗标过滤。

## 1. 范围

- 以 CE `canSeeMonster` / `canDirectlySeeMonster` / `monsterRevealed`（心灵感应、催眠揭示、检测）为**唯一判断**，
  web 建一个共用谓词，所有展示出口（地图渲染、render_game_to_text、hover 详情、侧栏怪物列表、瞄准候选、消息）都读它
- 临时隐形状态（U09 已能施放 INVISIBILITY）与固有 MONST_INVISIBLE 都覆盖；隐形怪在气体里的描边（CE 有此规则，请核实）
- 心灵感应/催眠揭示时的显示方式（CE 用什么字形/颜色）
- ❌ 不改 AI 感知与目标资格逻辑（U12a 已处理施法资格），只统一**展示**

## 2. 验收

- 隐形敌人：画面、文本、hover、侧栏、瞄准都不显示；被揭示后都显示，且方式一致
- 浏览器截图（隐形/揭示前后，1280 与 390）
- 硬串守卫（U24 新增）与 p1_30 继续通过

## 3. 硬约束与教训

- ❌ 不移动生成流；📌 **任务书与 CE 冲突时以 CE 为准并写明**
- **撞上守卫时改代码，不改守卫、不绕扫描器**；**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；
  **反查闭包含 p1_30 与所有读源码守卫**；Windows 注意不要产生 CRLF 证据文本
- 📌 **UI 不能比玩家知道得更多**

## 4. 门禁与报告

R∪S 自查 + `npm run build` + `npm run test:drift`；最终复跑声明。报告写到 `ai_docs/reports/u-21a.report.md`：
CE 可见性谓词路径；统一出口清单；截图；反查与两份清单；撞红处置。
