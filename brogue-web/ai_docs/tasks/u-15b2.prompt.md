# U15b-2：戒指出生附魔与 light/reaping 入池（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U15b 第二轮（U15b-1 六种戒指效果已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U15b 行、§2 D10、§3.1 K22 是权威规格**（`153dbb5`）：

> **U15b 戒指目录/附魔/效果**；K22｜**先现有 6 种逐效果（已完成），后缺失 2 种入池另轮**｜实际 E/可见 E/充能、可观察性分清
> D10：6 种目录缺 light/reaping；**出生未按 CE 生成戒指附魔**

## 1. 范围（**全部请自行核实 CE**）

- **普通戒指出生附魔**：CE `makeItemInto` RING 分支（Items.c 约 :347–373）`randClump(1,3,1)`、16% 诅咒（取负、ITEM_CURSED）、非诅咒侧 10% 递增长尾；
  U05 已为机器戒指实现同一出生资源（`spawnMachineRing`）——统一到普通 `spawnRing`，机器路径只保留 CE 实际差异（若 CE 无差异则合并）
- **light 与 reaping 入池**：CE ringTable 的 RING_LIGHT、RING_REAPING 条目（名称、频率、描述、`updateRingBonuses` 消费者：光半径 `lightMultiplier`、收割 `reaping` 命中充能），
  按 CE 目录顺序与频率加入生成池；消费者按 CE 公式实现（fixpt 同 CE），复用 U15b-1 的 `effectiveRingEnchant`/`ringBonus`
- 戒指 kind 抽签顺序、频率表与 CE 目录一致（U05 报告登记"现目录仅 6/8 种且顺序不同，不等于完整 CE 同 seed 抽种类"——本轮闭合）
- 详情/i18n 按 CE；U01 字段合同；不做旧存档迁移

## 2. 生成流

先 drift 记录 → 单变量归因（出生附魔、目录顺序/频率、light 入池、reaping 入池各一段）→ 独立守卫 → 一次重捕获。📌 用户裁决：生成变化不是卡点。
`ai_docs/reports/u-r2-trace.json` 等黄金 trace 若因本轮正当行为变化翻红，按其报告所述方法单变量归因后重录并登记。

## 3. 约束

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**反查闭包含 p1_30、U24、U01/U03、U05、U15b-1、b_1b、b_4a、W-6/7、w_5、UR2 trace 与所有读源码守卫**；不产生 CRLF。

## 4. 门禁与报告

R∪S + build + test:drift；**全量 npm test 完整跑完**；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-15b2.report.md`。
