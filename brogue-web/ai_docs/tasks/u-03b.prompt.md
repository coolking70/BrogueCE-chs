# U03b：普通跨梯跟随与离层环境补跑

> **本地执行（Mac 轨）。X-0 路线；U03 拆出的后半。**

## 0. 规格

**U03 报告 §7「U03b 和 U27 接口」**（`ai_docs/reports/u-03.report.md`）是本轮施工边界——它已核实 CE 路径，仍请自行复核：

- **普通跟随**：CE `STATUS_ENTERS_LEVEL_IN`、`MB_APPROACHING_UPSTAIRS/DOWNSTAIRS/PIT` 及坑出口位置；两张路径图/资格门；
  按客观块推进相邻层怪物；进层/挤占/坑降伤害。新数据入实体合同或层快照；`clearCorpseTargetOnLevelChange()` 复用 U10 原语
- **离层环境补跑**：`absoluteTurnNumber` 与各层 `awaySince`；独立的"不结算玩家回合"的环境事务；CE 新层 50 步、重访最多 100 步
  及 oldSeed 回切后的消费时点（U02b 报告 §5 也登记了此项）
- X-0 §3.1 K03（跨层时间/跟随）

📌 用户裁决：生成/交互变化不是卡点；不做旧存档兼容。

## 1. 验收

- 盟友/追击怪跟玩家上下楼：CE 资格（谁会跟、多远、多久后到）、落位、挤占、坑降伤害
- 回到旧层：按离开时长补跑环境（火/气体/晋升等）的步数与 CE 一致；**补跑不结算玩家回合、不推进玩家状态**
- 存读档中途（怪物在"进层途中"时存档）恢复等价
- 若改变生成/交互 RNG：先单变量归因再重捕获

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**反查闭包含 p1_30、U24 硬串守卫与所有读源码守卫**；**多个漂移杠杆分阶段归因**（跟随与补跑分开）。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明。报告 `ai_docs/reports/u-03b.report.md`。
