# U17d：DF 载体第三族——喷口/长明灯/毒气与火焰陷阱（⚠️ 可能移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U17 第四轮（U17a 统一事务、U17b/U17c 两族已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U17 行、§3.3 首族、§3.1 K19/K28 是权威规格**（`153dbb5`）：

> §3.3 喷口/长明灯：**179/180 甲烷、182 PILOT_LIGHT、183/185 麻痹、174/175 毒气；另 14 GAS_TRAP_POISON、19 FLAMETHROWER**。
> 各族恢复须把**数据起点、地形三链、DF 后继、最终消费者、可解性**一起闭合；每族 铺装→实体疏散/落物→晋升→后继→可解/显示；
> **禁止一次删完 30 条缺口登记**——本轮只动这 9 条；V-9e-2 麻痹机器等仍资格退池的，本轮只闭合载体与可触发链，机器回池留 U19

## 1. 范围（**全部请自行核实 CE**）

- 这 9 个 DF 的 CE 目录条目（Globals/GlobalsBrogue 的 dungeonFeatureCatalog 与对应 tileCatalog 行）：tile、层、概率、flags、后继、描述/光
- 每条的**触发来源**（隐藏陷阱搜出/踩踏、机器接线喷口、长明灯点燃甲烷、麻痹喷口；气体体积与扩散走既有 Gas 系统）与**最终消费者**，全部走 U17a 统一事务
- 地形三链（tile 属性 → 晋升 → 显示/外观），目录、外观黄金表按 CE 源码生成扩展
- 若其中某条的上游触发来自后续族/未实现系统，登记而不伪造触发
- 这 9 条从 `DF_MISSING_TILES` 登记中移除（仅在闭合后）；其余 11 条不动
- **顺带并目录**：U15d-2 把 CE `DF_ARMOR_IMMOLATION`（Rogue.h:1636，Combat.c:1087 调用）定义在效果侧、未入 DF 目录；按 CE 并入目录并按 U17a/b/c 的投影方式更新目录数量守卫（火焰族同属本轮）
- 若激活后 autoGen / 生成期会用到（如毒气/火焰陷阱 autoGen 行）→ 生成流变化

## 2. 生成流

先 drift 记录 → 单变量归因（每条 DF 一段）→ 独立守卫（每条"触发→最终格/实体状态"）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**地形名出现 ≠ 可交互**——每条要有用户可触发的端到端证据；
**反查闭包含 p1_30、U24、U01/U03 合同守卫、c_4a/b/c、c_7、r_1、f/g、V、W-13/14、U17a/b/c、g_1/2/3、f_2*、w_10/w_15 与所有读源码守卫；气体效果复用 U13/既有状态，不另写一套**。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-17d.report.md`。
