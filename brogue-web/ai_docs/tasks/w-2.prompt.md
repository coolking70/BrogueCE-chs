# W-2：施法选择与提交

> **本地执行（`codex exec --worktree`）。W 链第二轮，机制前置。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-2 行是权威规格**（已合入 `823eb0e`）：

> **W-2 施法选择与提交**｜已有 11 件 CE 法器：方向/格选择、敌友自动候选、
> 资格过滤、取消；确认后扣电/推进时间，以结果 autoID；保持未知态。
> 若接发现屏 allies 后缀，明确列入本轮｜依赖 W-1｜生成流：**否**｜
> 改动面 G、`Input.ts`、`GameCanvas.vue`、`InventoryOverlay.vue`；
> 后缀接线才含 L → R(这些文件)+S(U,Q,B)｜
> 撞红预期：**B-1a"用一次就亮"、B-1c 后缀恒 0 可能要翻正**；
> p1_46 键位、P2 耗时/锁输入、取消零消耗、已知/未知空杖耗时

**本任务书不复述技术细节**——验收方在这个子系统上估错过四次。
任务书若与报告冲突，**以报告为准**。

## 1. W-1 留给本轮的交接

W-1 报告（已合入 `a72ac60`）明确移交两件事，**读它的 §1 再动手**：

- `BoltOutcome` 声明了 `autoID: boolean` 与 `casterMovement`，但
  **两条旧出口目前都返回 `outcome = null`，表示「尚未评估」**
  ——W-1 明言"不能解释成 `autoID=false` 或成功"。**本轮要把 autoID 真正评估出来。**
- 原 `useArcanaItem` 的**提前鉴定行为**（用一次就亮）W-1 有意留给本轮处置

⚠️ CE 的 autoID 来自**实际效果观察**（`Items.c:5112-5119 / 5470-5474 / 5567`），
不是"用过就亮"。这正是 B-1a 那条旧断言可能要翻正的原因——
**但要按留痕反转改写并给 CE 依据，不是删掉**。

## 2. 硬约束

- ❌ **不移动生成流**。`npm run test:drift` 必须绿**且不许重捕获基线**
- ❌ **不新增 `arcana.json` 物品**（留 W-24~26）
- ❌ **不实现任何效果行为**（留 W-8~W-23）——本轮只做选择、提交、扣电、计时、autoID
- ❌ 不要偷偷启用怪物 `BLINKING` / 蛛网 / 藤射线
- ⚠️ **充能的初始值与自然回电留 W-5 / W-6**。本轮的"扣电"只是消费既有值，
  **不要顺手重做充能模型**

## 3. 教训（已连续九轮验证，继续照做）

- 闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望
- **「建成 0 台」≠「RNG 足迹为 0」**——看是否参与抽签
- 驳回时给**登记位置**，区分"仍有效"与"已过期"的登记
- **代码事实为真 ≠ 它是本次失败的成因**——诊断要实测

## 4. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 两层方法自己确定，
**在报告里列出你实际跑了哪些文件、反查怎么做的**。

⚠️ 报告自己的限定要遵守：**闭包是"可能受影响的结构集合"，
既不是预计必红数，也不是修改断言的授权。**

⚠️ 本轮改动面含 `.vue` 与 `Input.ts`，**UI 层的测试容易被 R 漏掉**（无 import 关系），
请用 S(pattern) 搜键位、输入锁、耗时、鉴定后缀等行为断言补齐。

## 5. 门禁

按 §4 自查清单跑 + `npm run build` + `npm run test:drift`。
⚠️ **不要跑不带参数的全量 `npx vitest run`**。

**最终复跑（硬要求）**：所有编辑完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 6. 授权反驳

W-0 / W-1 报告都是你自己写的，**实现时发现判断有误照样驳回并说明**。

## 7. 报告

写到 `ai_docs/reports/w-2.report.md`，含：

1. 选择/候选/资格过滤/取消的 CE 依据与落地
2. **autoID 的真正评估**：CE 依据、与旧"用一次就亮"的差异、B-1a/B-1c 旧断言怎么翻正的
3. 扣电与推进时间的落点（**并说明为何没有碰 W-5/W-6 的充能模型**）
4. **§4 的反查过程与实际跑的文件清单**（含 UI 层怎么补的）
5. 撞红清单与处置
6. §2 自检：drift 绿否、未重捕获、arcana.json 零改动
7. **§5 的最终复跑声明**
8. 清单外改动申报
