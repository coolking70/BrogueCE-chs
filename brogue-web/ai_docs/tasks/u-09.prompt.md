# U09：学习能力消费者矩阵

> **本地执行（`codex exec --worktree`）。X-0 路线；W-22 学习恢复的第三步（U07、U08 已完成执行出口）。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.3 U09 行、§3.1 K08/K10 局部/K25 是权威规格**（`153dbb5`）：

> **U09 学习能力消费者矩阵**：先逐能力资格，后补已证实的怪物效果/永久特性；K08/K10局部/K25｜前置 U06/U07/U08｜
> 生成流：间｜R: M、Bolt、G、Creature；S: monsterCanLearnBolt、negatable、TELEPORT、POLYMORPH、invisible、
> weakness、TRANSFERENCE、TURRET｜W-19/21/23 及 p4_1b/3；**每位"可学习/可施放/可消魔/需同步"有 CE 证据和消费者**；
> TRANSFERENCE/TURRET 未核部分

## 1. 本轮要交付的矩阵

**W-22 报告 §4**（`59e172c`）已列出：6 项 abilities/behaviors + 22 项可学 bolt 的出口表。以它为起点，**逐项重核并补齐**：

| 能力 | CE 可学（LEARNABLE_* / !BF_NOT_LEARNABLE） | 怪物可施放（`monstUseBolt`/专调度/被动） | 可被消魔（W-23 规则） | 学会后需同步的永久状态 | web 执行出口现状 | 本轮补 / 登记 |

- **U06**（怪物 BE_DAMAGE）、**U07**（怪物 blink）、**U08**（网/藤）刚补了出口——矩阵要反映最新状态
- **TUNNELING/OBSTRUCTION**：CE 明确禁止怪物施放（W-22 §4.2 已核），矩阵里标为 CE 内在例外，不开出口
- W-22 §4.2 列出的缺怪物出口项（TELEPORT、SLOW10、POLYMORPH、DOMINATION、INVISIBILITY、LIGHTNING、POISON、
  ENTRANCEMENT、CONJURATION）：逐个按 CE `monstUseBolt` / `specificallyValidBoltTarget` / 命中分派补齐
  **怪物施放出口**（目标资格 + 命中效果复用已有玩家效果原语）
- behaviors/abilities 学会后的同步：FLIES/IMMUNE_TO_FIRE/INVISIBLE 的永久状态、REFLECT_50 的反射率、
  TRANSFERENCE 的吸血通路、CAUSES_WEAKNESS 对怪物受害者（U14a 已接 weaknessAmount）——**逐个验证消费者**
- K25：web 自创的 legacy abilities/onHitStatus/statusImmunities——只做与学习相关的归属审计，**不全清**

## 2. 边界

- ❌ **不做学习状态机**（尸体资格/选择/走尸/吸收/安装/扣次数 = U11）、不加吸收字段（U10）
- ✅ 可以用"手工给怪物安装一项能力"的测试夹具验证出口（U08 先例），但**不得开启自然学习抽签**
- 若某项出口需要大块新系统（例如 CONJURATION 的怪物召唤落点），做不到忠实就**明列为 U11 前仍缺**，不硬做

## 3. 硬约束

- ❌ **不移动生成流**：`test:drift` 绿、不许重捕获
- ❌ 不改怪物施法选择的总体调度（U12）——只补"该 bolt 被选中后能正确执行"与其专属资格门
- 📌 用户裁决：不做旧存档兼容

## 4. 教训（已连续四十五轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**撞上守卫时改代码，不改守卫、不绕扫描器**；
**注释写的是意图，执行路径里可能有例外**；**前轮的留痕登记可能正是为你预约的**（W-22 §4、U07 §8、U08 §3）；
**守卫靠旧规则才绿时**，先反事实证明，再只修前提、不动期望，交验收方裁决；**做不到忠实时，缩小范围并明列**；
📌 **玩家施放的效果原语可以复用，但"谁是施法者"会改变归属/阵营/死因**（W-22 §4.2 的 DOMINATION 归玩家、刀刃 leader 固定玩家先例）。

## 5. 授权测试清单：**自行反查**

按 X-0 §4.1 的 R(files)+S(pattern) 自行确定，给出"预计改义断言"与"只回归不可放宽断言"两份清单，每条改义给 CE 依据。

## 6. 门禁

§5 自查清单 + `npm run build` + `npm run test:drift`。⚠️ 不要跑不带参数的全量 `npx vitest run`。
**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（SHA-256 自证。）

## 7. 报告

写到 `ai_docs/reports/u-09.report.md`：**完整矩阵**；本轮补的出口及 CE 依据（含施法者归属问题）；
同步消费者验证；K25 归属审计；仍缺项（U11 前）；反查与两份清单；撞红处置；§3 自检 + 最终复跑声明；
**W-22 恢复还差什么**（应只剩 U10 + U11）。
