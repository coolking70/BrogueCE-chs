# W-17：支配

> **本地执行（`codex exec --worktree`）。W 链效果收口第十轮。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-17 行是权威规格**（`823eb0e`）：

> **W-17 支配**｜DOMINATION 概率、无效目标、**成功转队/清 discord**、**俘虏/携物/leader**；
> domination 魔杖待入池｜依赖 W-4、**W-9**、**W-11**｜生成流：**否**｜
> 改动面 G、M、阵营 helper/保存 → R(实际文件)+S(M,B)｜
> 撞红预期：P4-2 minions、P4-5 抓取/携物、V-2b-5；
> **HP=满/20%/低于20% 三边界，失败不变队**

**本任务书不复述技术细节**。冲突时**以报告为准**。

## 1. ⚠️ 验收方今天凭记忆写错过两次 CE 事实

- W-10：说"吸毒气会获得毒状态"——**错**，是毒地衣
- W-16：说"刀刃有寿命、到期消失"——**错**，CE 刀刃无寿命，靠 1 HP / 消魔 / 离开领袖而死

⇒ **本任务书的所有 CE 陈述只作线索**，请全部自行核实，在报告里说明哪些对哪些错。

## 2. 线索（**全部请自行核实**）

- `PowerTables.c` 附近可能有 `wandDominate(monst)`，验收方印象中是：
  **HP 低于 20% 时必成；否则按损失比例**——请自行核，并给出 W-0 点名的
  **"满血 / 恰好 20% / 低于 20%"三边界**的实测
- **W-9**（`e36d575`）做过 discord 的正确状态（`discordant`）。支配成功要**清 discord**，
  用 W-9 的那个状态，别另造
- **W-11**（`f141c64`）做过"释放俘虏"（`freeCaptive`）。**被支配的是俘虏时怎么处理**，按 CE
- **携物**：被支配的怪物身上带着物品（例如钥匙），转队后物品归属按 CE
- **leader 关系**：被支配的怪物原本是某个怪物群的成员/首领，转队后群关系怎么变，按 CE

## 3. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
- ❌ **不增种类、不改 frequency**——domination 魔杖"待入池"是 W-24 的事
- ⚠️ **失败不变队**（W-0 点名）：支配失败时目标的阵营、状态一律不变
- ⚠️ 阵营 helper 若与 P4-2 minions、P4-5 抓取/携物、V-2b-5 俘虏共用，**明列怪物行为变化**
- ⚠️ **存档**：被支配的盟友存读档后仍是盟友

## 4. 教训（已连续二十四轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**；CE 里"看起来不一致"的数值先查是不是承重的；
**验收方写的 CE 事实可能是错的，一律自行核实**；
**撞上守卫时改代码，不改守卫、不绕扫描器**；
**复用前轮原语发现不够通用就如实登记前轮缺口**；
**前轮的留痕登记可能正是为你预约的**，动手前 grep 相关注释；
**照搬上一轮的做法前，先确认 CE 在这里是不是也这么做**；
**公式有定点/舍入细节时，编译 CE 原函数取黄金值**；
📌 **自己 W-0 的规格也可能写得不准**（W-16 你纠正了 W-0 的"绑定玩家/不追踪"）。

## 5. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定。`scripts/w*-test-scope.mjs` 可复用。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**

## 6. 门禁

按 §5 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。
若遇超时，照 W-12 的做法处理（串行重跑、如实区分、不改超时上限）。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 7. 授权反驳

W-0~W-16 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 8. 报告

写到 `ai_docs/reports/w-17.report.md`，含：

1. **`wandDominate` 公式核实**与"满血 / 20% / 低于 20%"三边界实测
2. 无效目标（哪些怪不能被支配）的 CE 依据
3. 成功转队：阵营、状态、清 discord（用 W-9 的状态）
4. **俘虏、携物、leader 关系**的处理与 CE 依据
5. **"失败不变队"的证明**
6. 存档与旧档兜底
7. 若共用 P4-2/P4-5/V-2b-5 的阵营 helper，**明列怪物行为变化**
8. §2 线索的核实结果（哪些对、哪些错）
9. §5 反查过程与实际跑的文件
10. 撞红清单与处置
11. §3 自检 + **§6 最终复跑声明**
12. 清单外改动申报
