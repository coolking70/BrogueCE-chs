# U05：机器物品实化——STAFF/WAND 配发与 Q 族品质筛选（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U05 行、§3.1 K11 是权威规格**（`153dbb5`）：

> **U05 机器物品实化/Q 族**：STAFF/WAND 身份、Q 筛选重掷、实例流转；K11｜前置 U05a/U02｜生成流：**直**｜
> R: BlueprintEngine/G/ItemLoader/Arcana*；S: itemQualifiers、MF_REQUIRE_GOOD_RUNIC、spawnBlueprintItem、STAFF、WAND｜
> **类别→种类→CE 资源→筛选→归属逐步观测，不能从最终名称反推抽取正确；重掷上界与 CE 失败传播须先核**

K11：机器 STAFF/WAND 显式 id 及类别请求都返回 null；Q 族 itemQualifiers 只是指令，没有任何 Game/ItemLoader 读方，地面也未消费；
携带收口还丢品质字段（G 约 :1035–1144、:1468、:1496–1503；BlueprintEngine 约 :1623–1629、:1783–1785；CE Architect.c 约 :1504–1509）。
U05a 报告 §6 登记了 **67 次 null 实化**（STAFF 33、STAFF/RING/CHARM 18、WAND 8、WEAPON/ARMOR/WAND 7、CHARM 1）。

## 1. 范围（**全部请自行核实 CE**）

- CE `buildAMachine` 物品块（Architect.c 约 :1494–1519）：`generateItem(category, kind)` → **失败重掷循环**（failsafe 1000）：
  诅咒、`MF_REQUIRE_GOOD_RUNIC`、`MF_NO_THROWING_WEAPONS`、`MF_REQUIRE_HEAVY_WEAPON`（且正附魔）、`itemIsADuplicate`（戒指/杖等去重）
- STAFF/WAND/RING/CHARM 类别请求按 CE 生成（W 链已有 `spawnStaff/spawnWand` 与 W-5 实例资源、W-24/25/26 目录）
- 品质字段在所有归属（地面/怪身/外包/领养）上都保留（U05a 的单一所有权合同）
- CHARM：U15c 未做前，护符模型仍偏离 CE——**能生成就生成，效果问题登记给 U15c**，不在本轮修护符
- 每类请求的抽签顺序与 RNG 消费与 CE 同构程度写清楚

## 2. 生成流

先 drift 记录 → **分阶段单变量归因**（STAFF/WAND 实化、RING/CHARM 实化、Q 筛选重掷各自一段）→ 独立守卫（67 次 null → 0 次，
每件的类别/种类/资源/品质/归属逐步观测）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**不能从最终名称反推抽取正确**；**反查闭包含 p1_30、U24、U01/U03 合同守卫、U05a 唯一性与所有读源码守卫**。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-05.report.md`。
