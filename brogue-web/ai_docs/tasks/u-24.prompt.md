# U24：文案与 i18n 校正

> **本地执行（Windows 轨）。X-0 路线。**

## 0. 规格以 X-0 勘察报告为准

**`ai_docs/reports/x-0-survey.report.md` 的 §4.1、§4.4 U24 行、§3.1 K26/K41 是权威规格**（`153dbb5`）：

> **U24 i18n 与物品文字校正**；K26/K41｜相应玩法先稳定｜生成流：无｜
> R: G/M、components、DetailGenerator、locales/i18n；S: spawnFloatingText、logger.log、defaultValue、runic、猛烈突刺｜
> **逐出口真实 zh_CN 渲染、不漏变量、不显示内部枚举，文本准确描述已实现效果；自由硬串不靠键扫描**

K41：i18n 静态门只覆盖可解析 t 调用，硬串与描述语义不受保护；Game 约 :6355/:6394/:6401 英文浮字、Sidebar 约 :32–37 中文硬串。
K26：完整 CE 数值详情、词表和退池旧描述未齐；符文浮字仍英文，详情与实际 E/资源需逐项核。

## 1. 本轮收集的已登记问题（**逐条核实后处理**）

- **U22b 发现屏**（`ReferenceOverlay`/`Discoveries`）：中文界面下物品名显示英文大写（CE 的 `upperCase` 适用于英文）；
  中文应显示与背包/详情一致的中文名
- 符文/战斗浮字英文硬串（K41 点名位置）；Sidebar 中文硬串改走 i18n
- 物品说明与实际实现不符的描述（K26；W-15 `staff_of_light` 退池旧描述；W-26 §8"完整 CE 数值详情"）——
  **文本描述已实现的效果，不反推规则**
- 内部枚举泄漏到界面（`MA_*`/`MONST_*`/runic 内部名等）
- 可自行扫描 `defaultValue` 英文回退被实际显示的出口

## 2. 方法

- 以**真实 zh_CN 渲染**为准（浏览器或 headless 渲染 + `render_game_to_text`），不是只看键存在
- 新增一个**硬串扫描守卫**（对 spawnFloatingText/logger.log/Vue 模板中的非 i18n 字面量），白名单要说明理由
- 变量插值不漏（`{{name}}` 等）

## 3. 硬约束

- ❌ **不移动生成流**；❌ 不改玩法规则
- 📌 **任务书与 CE 冲突时以 CE 为准并在报告里写明**；删改文案键时死键移入 zh_CN.legacy.json（p1_30 门）

## 4. 门禁与报告

按 X-0 §4.1 的 R(files)+S(pattern) 自查（**含 p1_30 与所有读源码守卫**）+ `npm run build` + `npm run test:drift`；
最终复跑声明。报告写到 `ai_docs/reports/u-24.report.md`：问题清单与处理、硬串守卫设计与白名单、浏览器截图、
仍需玩法轮先落地的文案登记。
