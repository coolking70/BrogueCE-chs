# U15d-1：武器符文效果 CE 化（不移动生成流）

> **本地执行（Windows 轨）。X-0 路线；U15d 第一轮（护甲符文、生成映射另轮）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U15d 行、§3.1 K24 是权威规格**（`153dbb5`）：

> **U15d 武器护甲符文**；K24｜前置 U13/U01；MULTIPLICITY 需 U16 生命周期（已合并）｜**逐符文小轮：目标物种/几率/附魔、谱影和消魔、负面符文生成、持久化；
> 禁一轮全表只测目录数量**

K24：缺 W_MULTIPLICITY/SLOWING/PLENTY 等生成映射；slaying 物种目标、speed 额外一击、mercy 复活至 1HP 等偏差
（ItemLoader 约 :347–375，G 约 :6347–6422，对 CE Combat.c:681–844、921 起）。

## 1. 范围（**全部请自行核实 CE**）

- CE 全部武器符文（W_SPEED、W_QUIETUS、W_PARALYSIS、W_MULTIPLICITY、W_SLOWING、W_CONFUSION、W_FORCE、W_SLAYING、W_MERCY、W_PLENTY）
  按 CE `magicWeaponHit`（Combat.c 约 :681–844）逐个对齐：
  - 触发几率 `runicWeaponChance`（fixpt、慢速武器/攻击速度修正、W_SLAYING 恒触发条件等按 CE）
  - 效果：speed 的额外回合/行动、quietus 即死与免疫条件、paralysis/slowing/confusion 持续期公式、multiplicity 谱影刃数/持续/生命周期（复用 U16）、
    force 击退距离（复用已有 weaponForceDistance）、slaying 物种目标（`vorpalEnemy`）、mercy 治疗目标（**CE 实际公式**）、plenty 克隆目标
  - 符文揭示/自动鉴定消息与时机（`autoIdentify` 路径）
- **不在本轮**：生成映射（缺失符文入池会移动生成流，另交 Mac 轨）、护甲符文
- 消魔对符文的作用若已有入口，核对是否与 CE 一致；新增字段纳入 U01/U03 合同；不做旧存档迁移

## 2. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
每个符文要有"命中→触发几率/效果终态"黄金值（多个附魔，含 RNG 消费次数）；**不得改变 test:drift**；
**反查闭包含 p1_30、U24、U01/U03 合同守卫、armor_runic、b_4a、W-20/23、U16 与所有读源码守卫**；不产生 CRLF。

## 3. 门禁与报告

R∪S + build + test:drift；最终复跑声明。报告 `ai_docs/reports/u-15d.report.md`。
