# X-0 勘察：全项目 CE 对齐度与后续路线

> **本地执行（`codex exec --worktree`）。只读勘察 + 出方案，不改生产代码、数据或测试。**
> 用户授权（2026-09-24）：W 链收官后先做全项目勘察，再定路线与 `Game.ts` 是否拆分。

## 0. 为什么要这一轮

W-0 勘察（`823eb0e`）让其后 26 轮几乎全部首提全绿——**因为规格可信**。
W 链只覆盖杖/魔杖；项目其他部分从未被同样系统地对照过。验收方的粗估**在 W-0 前连错三次**
（grep 注释当实现、数字差 4 倍）。⇒ **不要相信本任务书、交接文档、路线图或任何历史报告里的
数字与"已完成"说法，逐条读代码核实。**

参照源：`../BrogueCE-master/src/brogue/*.c`（Architect、Combat、Items、Monsters、Movement、Time、
IO、Light、Grid、Dijkstra、Recordings、RogueMain 等）与 `src/variants/GlobalsBrogue.c`、`Globals.c`、`Rogue.h`。
web：`src/`（`engine/**`、`entities/**`、`data/**`、`components/**`、`locales/**`）。

## 1. 要产出的四份东西

### 1.1 子系统覆盖表（按 CE 职能划分，不按 web 文件划分）

至少覆盖以下域，可自行增补或合并，**说明划分依据**：
地牢生成（房间/湖/机关蓝图/自动生成物/楼梯）· 地形与 DF/火/气体/光照 · 怪物目录与生成（horde、突变、俘虏、
休眠）· 怪物 AI（移动、逃跑、追踪、施法、召唤、跟随、盟友行为）· 战斗（命中/伤害/暴击/偷袭/武器与护甲
特性/符文）· 状态效果 · 物品：武器/护甲/戒指/护符/药水/卷轴/食物/钥匙/杖/魔杖（W 链已收口，只需复核口径）·
物品生成与鉴定 · 玩家系统（力量、营养、搜索、潜行、再生、升级/经验若有、负重）· 盟友/俘虏/复活 ·
换层与跨层状态 · 存档/读档 · 胜负/结局/分数/高分榜 · 回放/录像/种子 · UI（侧栏、消息、背包、物品详情、
发现屏、目标选择、地图记忆、帮助）· i18n · 随机数与确定性。

每个域一行：**CE 权威位置** / **web 实现位置** / **对齐度判定**（✅ 基本对齐 / 🟡 部分 / 🔴 缺失或自创）/
**判定依据**（读到的执行路径，不是注释或名字）/ **主要缺口** / **已有守卫测试**。

⚠️ "✅" 必须给出至少一条你实际核过的执行路径证据；给不出就不能判 ✅。

### 1.2 未闭合项总清单（合并去重）

起点：`ai_docs/reports/w-26.report.md` §8 的 26 项 + 交接文档/路线图（`ai_docs/dev_roadmap_2026Q3.md`、
`ai_docs/SESSION_HANDOFF.md` 的"未决条目"、V 链登记的债项）+ **本轮新发现**。
每项：**是否仍有效**（读代码复核；过期的标"已过期"并给证据）/ 来源 / 影响面（玩家可见？影响生成流？
影响存档？）/ 依赖 / 粗估规模（S/M/L，给出依据）。

### 1.3 建议路线：派发单元表

像 W-0 §2.3 那样给出**可直接派发**的单元：
范围 / 依赖 / **是否移动生成流** / 改动面 → R(files)+S(pattern) 反查方法 / 撞红预期 / 收口要求。
- 按"玩家可感知的价值 × 风险"给出**优先级分组**（例如：P0 影响可玩性或存档安全、P1 明显偏离 CE 的玩法、
  P2 表现/UI、P3 内部债），并说明理由
- W-22 的三个前置（怪物 blink 调度、ANCIENT_SPIRIT_VINES/网 DF 旁支、吸收字段存档）要作为候选单元排进去
- 标出哪些单元**应当在用户在场时做**（改生成池、改存档格式、大面积改 UI 等）

### 1.4 `Game.ts` 拆分评估

`src/engine/Core/Game.ts` 当前约 10,559 行（请自测）。给出：
- 职责分区清单（行号区间 → 职责），以及与其他模块的耦合方式（私有字段访问、测试里 `(g as any)` 的调用点数量）
- 1.3 路线中每个单元**预计改动 Game.ts 的哪些区**——据此判断拆分对后续工作的收益
- 若建议拆分：分几步、每步的行为不变证明方法（哪些测试/drift/快照能证明零行为变化）、对历史测试中
  `(g as any).privateMethod` 的影响、预估撞红面
- 若建议不拆或延后：给出理由与触发条件
- **结论给出推荐，但写明这是用户决策**

## 2. 方法要求

- 对每个 🟡/🔴 判定，至少引用一处 CE 行号和一处 web 行号
- 区分**"web 缺失"**、**"web 自创（CE 没有）"**、**"web 有但语义偏离"** 三类
- 对历史报告中"已完成"的结论，**抽样复核**（每个域至少一条），抽中的不符项单列
- 可以写只读探针脚本（放 `scripts/x0-*.mjs`/`.ts`），可以跑现有测试做观察，**不改任何生产代码/数据/测试**
- 勘察报告会成为后续几十轮的规格：**写不准的地方写"未核实"，不要猜**

## 3. 硬约束

- ❌ 不改生产代码、数据、测试、基线、任务书、历史报告
- ✅ 只 `npm run build` 证明未破坏；可选跑部分测试做观察（不作为门禁）
- 输出只有：本报告、`scripts/x0-*`、`ai_docs/reports/x-0-evidence/`

## 4. 教训（已连续三十四轮验证）

**验收方写的 CE 事实可能是错的，一律自行核实**；**注释写的是意图，执行路径里可能有例外**；
**代码事实为真 ≠ 它是本次失败的成因**；**grep 命中注释 ≠ 实现存在**；
**"看起来不重要"的 CE 判定位置可能是关键**；**自己之前的规格也可能写得不准**；
**基线变绿 ≠ 生成正确**；**做不到忠实时，缩小范围并明列**。

## 5. 报告

写到 `ai_docs/reports/x-0-survey.report.md`，含：

1. 方法与域划分依据
2. **§1.1 子系统覆盖表**
3. **§1.2 未闭合项总清单**（含过期项及证据）
4. **§1.3 派发单元表与优先级分组**（含"用户在场"标记）
5. **§1.4 Game.ts 拆分评估与推荐**
6. 历史"已完成"结论抽样复核结果
7. 本轮未能核实的项
8. `npm run build` 结果与自检（无生产改动的 git 证据）
