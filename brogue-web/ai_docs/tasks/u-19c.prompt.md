# U19c：horde 个体实化时机与机器失败回滚（⚠️ 移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U19 第三个子轮（U19a 视图、U19b pending 占用已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U19 行与其后子轮表（约 :197、:203）、§3.1 K28/K32 是权威规格**（`153dbb5`）：

> **U19 蓝图剩余算法/机器玩法**；K28/K32｜前置 U05/U17（已合并）、视图需 U18a（已合并）｜生成流：**直**｜
> R: BlueprintEngine/Architect/AutoGenerator、G、数据；S: IN_VIEW_OF_ORIGIN、pendingItems、pendingMonsters…｜
> **按子轮分别验，先闭包后解退池；每台必须有"用户触发→结果"的证据，不能只点亮身份覆盖**
> **U19c** horde 个体实化时机/失败回滚

## 1. 范围（**全部请自行核实 CE**）

- CE `buildAMachine` 在 feature 块中**即时** `spawnHorde`/`generateMonster`（Architect.c 约 :1591–1660），怪物立即占格、消耗 RNG、入 `spawnedMonsters`；
  机器失败时 `abortItemsAndMonsters` 删除已生成怪物与物品。web 当前在 `populateLevel` 延迟实化，RNG 消费时序与失败回滚语义不同
- 目标：机器怪物（及其 horde 成员、携带物、休眠/沉睡/逃跑标志、machineHome、leader 关系）的**生成时序与 RNG 消费位置**与 CE 同构；
  机器失败回滚按 CE 删除本机已生成实体，无幽灵实体/占用
- 若改为即时实化，同步处理：U05 机器物品实化（CE 同样即时）、U19b pending 占用读口（即时后可能直接读真实实体）、U25 观测钩子、U05a 所有权
- X-0 K30/K31 相关的"延迟 horde 失败无携带者"等已登记边界在本轮闭合或明确保留
- 若全面即时化改动过大：可分阶段（先怪物、再物品），每阶段单独归因，但本轮至少完成怪物

## 2. 生成流

先 drift 记录 → 单变量归因（怪物即时实化、物品即时实化、失败回滚各一段）→ 独立守卫（机器失败后无残留实体；RNG 消费位置与 CE oracle 一致；反例即红）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**反查闭包含 p1_30、U24、U01/U03、V 全系、b2_transcription、blueprint_center、c_8、U05a、U17a–f、U25 观测守卫 与所有读源码守卫**；
可用 U25 的 `scripts/u25-machine-inventory.mjs` 追踪机器产物。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-19c.report.md`。
