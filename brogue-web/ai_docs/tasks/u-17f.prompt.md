# U17f：DF 载体第五族（收尾）——破壳/雕像/可淹地板/传送门光/棺木/虫道（⚠️ 可能移动生成流）

> **本地执行（Mac 轨）。X-0 路线；U17 第六轮、收尾（U17a 统一事务、U17b/c/d/e 四族已合并）。**

## 0. 规格

**`ai_docs/reports/x-0-survey.report.md` 的 §4.3 U17 行、§3.3 首族、§3.1 K19/K28 是权威规格**（`153dbb5`）：

> §3.3 破壳/虫道/其余：**155 RAT_TRAP_WALL_CRACKING、187 STATUE_CRACKING、148 COFFIN_OPEN、191 WORM_TUNNEL_MARKER_ACTIVE、87 FLOOR_FLOODABLE、88 PORTAL_LIGHT**。
> 各族恢复须把**数据起点、地形三链、DF 后继、最终消费者、可解性**一起闭合；每族 铺装→实体疏散/落物→晋升→后继→可解/显示；
> **禁止一次删完 30 条缺口登记**——本轮动剩余全部 6 条（清空 DF_MISSING_TILES）；CE55 虫道、V-9e-2 裂雕像等整机回池/资格留 U19，本轮只闭合载体与可触发链（K28：麻痹喷口/裂雕像 payoff 须闭合）

## 1. 范围（**全部请自行核实 CE**）

- 这 6 个 DF 的 CE 目录条目（Globals/GlobalsBrogue 的 dungeonFeatureCatalog 与对应 tileCatalog 行）：tile、层、概率、flags、后继、描述/光
- 每条的**触发来源**（鼠陷墙破裂放出鼠群、雕像开裂放出休眠怪、棺木开启、虫道标记激活开隧道、可淹地板被淹、传送门光）与**最终消费者**（怪物出场/唤醒、隧道通行、淹水地形），全部走 U17a 统一事务；另核 X-0 §3.3 所述 DF_RUBBLE 载体已具备但 autoGen 未接回（:200），按 CE 接回
- 地形三链（tile 属性 → 晋升 → 显示/外观），目录、外观黄金表按 CE 源码生成扩展
- 若其中某条的上游触发来自后续族/未实现系统，登记而不伪造触发
- 这 6 条从 `DF_MISSING_TILES` 登记中移除（仅在闭合后）；登记清空后，保留缺口守卫本身（改为断言为空），不删守卫
- 若激活后 autoGen / 生成期会用到（如可淹地板/虫道）→ 生成流变化

## 2. 生成流

先 drift 记录 → 单变量归因（每条 DF 一段）→ 独立守卫（每条"触发→最终格/实体状态"）→ 一次重捕获。📌 用户裁决：生成变化不是卡点。

## 3. 约束与教训

📌 **任务书与 CE 冲突时以 CE 为准并写明**；**撞上守卫时改代码，不改守卫**；**守卫靠旧规则才绿时**先反事实证明、只修前提、交验收方裁决；
**多个漂移杠杆分阶段归因**；**地形名出现 ≠ 可交互**——每条要有用户可触发的端到端证据；
**反查闭包含 p1_30、U24、U01/U03 合同守卫、c_4a/b/c、c_7、r_1、f/g、V、W-13/14、U17a/b/c/d/e、v_2b_9* 雕像/休眠、U16、V 系列 与所有读源码守卫；休眠怪唤醒/鼠群/虫群出场复用既有实现，不另写一套**。

## 4. 门禁与报告

R∪S + build + test:drift；最终复跑声明；基线前后哈希。报告 `ai_docs/reports/u-17f.report.md`。
