# C-7：光照目录 + 矿灯深度衰减（C 链收口轮）

> 只改"允许修改"清单里的文件；**不要执行任何 git 写操作**（`status`/`diff`/`log` 只读允许）。

**授权反驳条款有效**：CE 源码优先。前几轮执行方纠正过验收方十余次，最近三次：
S-1 发现 `c_5` 対抗③ 的阈值**正好压在正确实现的真值上**、
B-1c 发现 web 其实有加速药水（`potion_of_haste`）、
C-5 揪出 C-2 留形的 `pathingDistance` 不可达值写错。**请继续，包括反驳本任务书。**

**先读**：`ai_docs/c_6_autogenerators_report.md` §九「给 C-7」的登记。
凡冲突，**以 CE 源码为准并在报告里指出**。

---

## 一、本轮定位：C 链最后一块

**⚠️ 本轮与 B-2（投掷，交互期）并行执行**，文件边界已划开。

C 链把 CE `digDungeon` 的步骤逐个补齐，C-6 落地后只剩光照。
C-6 的登记里提到"藻井链(10) 与菌类(2) 也依赖光照"——本轮接上后它们才有载体。

---

## 二、范围

### 1. 光照目录

CE `Globals.c:955` 的 `lightCatalog[NUMBER_LIGHT_KINDS]`，**60 条**。
结构在 `Rogue.h` 的 `typedef struct lightSource`：
`lightColor` / `lightRadius`（randomRange）/ `radialFadeToPercent` /
`passThroughCreatures`（注释说明矿灯是少数为 true 的）。

地形的 `glowLight` 字段（`floorTileType` 第 10 列）指向目录条目
——**C-4a 的 `TerrainCatalog` 已经抄了这一列没有？请先查明**，
没有的话本轮补上（`TerrainCatalog.ts` 已在允许清单）。

### 2. 矿灯深度衰减

`Light.c:120` 的 `updateMinersLightRadius`，调用点在 `Items.c:4692/8090/8728`。
**自己读完再实现**——注意它与深度、与装备（光明戒指）的关系。

### 3. ★ 载体盘点（本项目硬规矩）

60 条目录里绝大多数引用的地形/机制 web 未必有。
**逐条给出"web 今天有没有载体"，只接有载体的，其余登记。**
C-6 靠这条挡住了 49 条里的大部分，G-2/G-3 各挡住四五条空转链；
C-4c 的硫矿"每回合掷骰 11 次全部缓办"就是没盘点的下场。

**特别注意**：光照是**渲染可见**的机制，但 web 的渲染层
（`GameCanvas.vue` 等）在禁改清单里——**接了目录但渲染不消费，就是空转链**。
请判断哪些能在引擎侧产生可观测效果（如 FOV、怪物视野、潜行判定），
哪些必须等渲染轮。**只做前者。**

---

## 三、明确不做（写显式留痕测试）

- **渲染层的光照表现**——`src/components/` 在禁改清单，归 UI 轮
  （已积五条：P1-47 裸键名、EMBERS/ASH/PLAIN_FIRE 渲染、燃烧无视觉、
  `onConfirmRequest`、地面符号）。
- **不碰物品/投掷**——那是并行的 B-2 的地盘。
- 不碰火/气体/坠落/自动生成器的既有行为。

---

## 四、门禁

1. **反向哨兵**：火、气体、坠落、自动生成器的行为断言必须不变。
   **S-1 刚把 8 个哨兵改造成对 RNG 流位移免疫**——本轮若改生成，
   它们应当**保持绿**；若有翻红，那是真阳性或改造不彻底，**停下来报告**。
2. `generation_baseline` 若因新增光照数据而变红，**如实报告不许刷新**，
   由验收方授权重捕获。
3. 坏层闸门 `p1_26` / `p1_29` / `p1_33` 仍为 0。
4. **载体盘点表** + "接了/没接/为什么"。
5. `npm run build` 绿。

**全量门禁用 `npx vitest run --fileParallelism=false`**（串行）。
**本轮与 B-2 并行**，重型测试可能因抢 CPU 假红——先单跑复核再下结论。
注意 `armor_model_effect` 自带 timeout（验收方已上调到 360s）。

---

## 五、★ 「照抄留形」的死分支必须逐字重核

本链已三次应验：G-2 揪出 `ALL_DIRS8` 抄重方向、C-5 揪出 `pathingDistance`
不可达值写错、S-1 揪出 `c_5` 対抗③ 阈值压在真值上。
**本轮凡激活任何前几轮标注"结构性不可达"的留形分支，必须逐字符重核 CE 并声明。**

---

## 六、文件边界（硬约束，路径已 grep 核实）

**允许修改**：
- `src/engine/Lighting/LightMap.ts`
- `src/engine/Map/TerrainCatalog.ts`（**仅**补 `glowLight` 列）
- `src/engine/Map/` 下**新增**文件（光照目录放这里）
- `src/engine/Core/Game.ts`（**仅**矿灯半径与光照更新的接线）
- `src/locales/zh_CN.json`（仅增键）
- **既有测试**（两段 grep 生成）：
  **① 结构性穷尽表**：`c_4a_0_layer_model` / `c_4a_terrain_catalog` /
  `c_4b_dungeon_feature` / `c_4c_promotion` / `f_1_fire_as_terrain` /
  `f_2a_fire_mechanics` / `g_2_gas_df_wiring` / `g_3_gas_effects` /
  `p1_42_secret_door_search` / `p2_6_display_settings`
  **② 本轮主题**：`c_6_autogenerators` / `invented_content_pool` / `scroll_effects`
  **限定**：只改因本轮行为变化而到期的断言，**不许放宽守卫性质**。逐条写进报告。
- 新增测试文件

**禁止修改**（违反即本轮作废）：
- **B-2 的地盘**：`src/engine/Items/`、`src/engine/Combat/`、`src/engine/Input.ts`
- `src/components/`（渲染层，归 UI 轮）
- `src/engine/Generator/`、`src/engine/Environment/`、`src/entities/`
- **`BrogueCE-master/` 下任何文件**
- 任何 `src/data/*.json`、`src/engine/Random.ts`、`vite.config.ts`
- **`src/test/fixtures/*`**、`src/test/harness.ts`、上面未列出的既有测试文件

---

## 七、测试要求

1. 每条断言必须能在某个具体的错误实现下失败。
2. **对抗性测试至少六条**，必须含：
   - `radialFadeToPercent` 的衰减写反（中心暗边缘亮）
   - `lightRadius` 的 randomRange 取成定值
   - `passThroughCreatures` 对矿灯写成 false
   - 矿灯深度衰减的方向写反（越深越亮）
   - `glowLight` 没接上（发光地形不发光）
   - 无载体的目录条目被接成空转链
3. **反向验证（强制）**：至少三条真的改坏、贴真实失败输出、还原。

---

## 八、交付

报告写入 `ai_docs/c_7_lighting_report.md`，必须含：
- 光照目录的 CE 出处、条目数与你复核出的字段语义；
- **载体盘点表**与"引擎侧可观测 / 必须等渲染轮"的判断；
- `updateMinersLightRadius` 的 CE 行号与复核要点；
- 留形分支逐字重核声明（§五）；
- 反向哨兵的逐位比对输出（**S-1 改造后它们应当免疫流位移**）；
- 改了哪些既有测试断言、逐条说明为什么到期；
- 对抗性测试与反向验证的真实失败输出；
- **C 链收口总结**：还剩哪些登记项没做、各归哪一轮。

**已知陷阱**：深水枚举成员是 `WATER_DEEP` 不是 `DEEP_WATER`，写错**不报错**
（vitest 走 esbuild 只剥类型不检查）。**任何让人困惑的失败，先跑 `npm run build`。**

**留痕规矩**：遇到留痕与文件边界冲突，**停下来申报**，
**不要把代码扭曲成扫描正则看不见的形态**。
