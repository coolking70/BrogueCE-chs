# W-4：通用反射与命中分派

> **本地执行（`codex exec --worktree`）。W 链第四轮，机制前置。**

## 0. 规格以 W-0 勘察报告为准

**`ai_docs/reports/w-0-survey.report.md` §2.3 的 W-4 行是权威规格**（`823eb0e`）：

> **W-4 通用反射与命中分派**｜射线反射路径、返回 caster/旁人、
> **玩家也能成为命中者**；`BF_NEVER_REFLECTS`、反复反射终止。
> **先保留各效果旧公式**｜依赖 W-3｜生成流：**否；交互 RNG 会变**｜
> 改动面 B、G、必要的反射 helper；**如改 Combat 明列** → R(实际文件)+S(B,C,M)｜
> 撞红预期：P4-3 反射/无敌、护甲反射、P4-4 分裂；
> **不要把旧"反伤自己"当弹道完成；物理箭不能自动归伤害魔法**

**本任务书不复述技术细节**。任务书若与报告冲突，**以报告为准**。

## 1. 前三轮的交接

- **W-1**（`a72ac60`）：`BoltResult` 契约。W-1 明确 `hits` **不代表反射后的受伤者**
  ——本轮反射落地后，**`hits` 要能表达"谁真的被打到"**，在报告里说清最终语义。
- **W-2**（`53e9a27`）：autoID = 真实效果观察。W-2 的表里已有
  "伤害接触（**含旧反射**）鉴定"一条 ——本轮换真实反射后，
  **复跑 W-2 的 29 个 autoID 用例并说明变化**。
- **W-3**（`aa77220`）：真实轨迹 + 遇障前停。W-3 有意**没做反射**，遇障就是停。
  本轮要在它的轨迹上接反射。W-3 还留了一条：
  `openPath` 只查看候选之前的格 —— **反射会让"之后的格"重新变得相关，注意别踩回去**。

## 2. 硬约束

- ❌ **不移动生成流**。`test:drift` 必须绿**且不许重捕获基线**
  （⚠️ W-0 标注本轮"**交互 RNG 会变**"——那是交互期，不是生成期。分清楚）
- ❌ **不新增 `arcana.json` 物品**（W-24~26）
- ❌ **不改任何效果公式**（W-8~W-23）——报告原话"**先保留各效果旧公式**"
- ❌ 不要偷偷启用怪物 `BLINKING` / 蛛网 / 藤射线
- ⚠️ **不要把旧"反伤自己"当弹道完成**（W-0 点名）
- ⚠️ **物理箭不能自动归伤害魔法**（W-0 点名）—— `BE_ATTACK` 与 `BE_DAMAGE` 是两回事，
  W-1 的别名表已区分，**别在反射里合并它们**

## 3. 教训（已连续十一轮验证）

闭包走四条边；退池留形 = 加引擎过滤不改数据；覆盖门换样本不改期望；
**「建成 0 台」≠「RNG 足迹为 0」**；驳回给登记位置并区分仍有效/已过期；
**代码事实为真 ≠ 它是本次失败的成因**。

## 4. 授权测试清单：**自行反查**

按 W-0 §2.2 的 R(files)+S(pattern) 自行确定，报告里列出实际跑了哪些、怎么查的。
⚠️ **闭包不是预计必红数，也不是修改断言的授权。**
⚠️ 若改 `Combat/`，W-0 要求**明确列出**——护甲反射与 P4-3/P4-4 都在那一带。

## 5. 门禁

按 §4 自查清单 + `npm run build` + `npm run test:drift`。
⚠️ 不要跑不带参数的全量 `npx vitest run`。

**最终复跑（硬要求）**：编辑全部完成后重跑，逐文件结果 + 声明
**「这是最终状态下的运行结果，不是中途快照」**。（继续用 SHA-256 自证。）

## 6. 授权反驳

W-0~W-3 都是你自己写的，实现时发现判断有误照样驳回并说明。

## 7. 报告

写到 `ai_docs/reports/w-4.report.md`，含：

1. 反射路径、返回 caster/旁人、玩家成为命中者的 CE 依据与落地
2. `BF_NEVER_REFLECTS` 与**反复反射终止**条件的 CE 依据
3. **`hits` 的最终语义**（W-1 交接）
4. **W-2 的 29 个 autoID 用例复跑结果与变化说明**
5. **§2 边界证明**：怎么确保各效果旧公式未动、`BE_ATTACK`/`BE_DAMAGE` 未合并
6. §4 反查过程与实际跑的文件（**若改 Combat/ 明确列出**）
7. 撞红清单与处置
8. §2 自检 + **§5 最终复跑声明**
9. 清单外改动申报
